<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 应用默认主题：无 ActionBar，避免顶部残留系统标题「Mao」。 -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:statusBarColor">#f5f5f7</item>
        <item name="android:navigationBarColor">#f5f5f7</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- 不要用 @drawable/splash 作为窗口背景：
             BridgeActivity 在 super.onCreate() 之后才 setTheme(AppTheme.NoActionBar)，
             导致 splash PNG 永远残留在窗口背景层。 -->
        <item name="android:windowBackground">#f5f5f7</item>
        <item name="windowSplashScreenBackground">#f5f5f7</item>
        <!-- SplashScreen 结束后切到无 ActionBar 主题，防止标题栏「Mao」残留。 -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
