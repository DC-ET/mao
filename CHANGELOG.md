# Changelog

项目级发版说明。用户可见版本号 `0.0.x` 由首条 `##` 决定，安卓 OTA / 桌面 Electron 等构建脚本自动读取。

**书写约定**（新版本推荐分节；历史条目保持扁平列表）：

- **前端（桌面 / Web / 安卓）** — 三端共用的 UI / 交互改动（安卓与 Web 同源远程加载，部署 Nginx 后刷新即可）
- **后端** — API、引擎、数据库等服务端改动（独立部署，不进 OTA 用户文案）
- **安卓原生** — Capacitor 壳、系统栏、OTA 插件等
- **桌面 Electron** — Electron 壳、本地工具、自动更新等
- **管理后台** — admin 专用改动
- **终端 CLI（mao-agent）** — `agent-cli/` 终端对话客户端

发版前在顶部新增 `## 版本 (日期)` 条目；前端部署 Web 后安卓/Electron 刷新即可；仅安卓原生壳变更时 `cd android && bash build-apk.sh`。

---

## 0.0.144 (2026-09-18)

### 后端

- CLOUD shell / 交互终端在 ECP 飞书登录开启且配置了 `larkAppId` 时，自动注入 `LARKSUITE_CLI_APP_ID` 与 `LARKSUITE_CLI_USER_ACCESS_TOKEN`（用户飞书 UAT），供 lark-cli 以用户身份使用；UAT 走 ECP 换票接口 + 进程内存缓存，ECP 会话失效时不注入。
- 历史里某个 tool_call 缺少对应输出时（含并行工具结果被图片消息拆开），不再把 `No tool output found for tool call` 的 400 卡死会话：补占位结果、重排 tool 组，原会话可继续或重试。
- 飞书进度卡片执行失败时，正文改为展示具体 `error.message`（与客户端对话页「执行异常」一致）；无错误信息时仍回退「抱歉，处理您的消息时出现了错误，请稍后再试。」

### 管理后台

- ECP 飞书登录配置新增「lark App ID」；留空则不注入 lark-cli 凭证。

## 0.0.143 (2026-09-18)

### 管理后台

- 移除首页「数据概览」，「用量分析」成为新的后台首页：登录、Logo 点击与 404 兜底均进入用量分析；侧边栏将用量分析置于首项，原「运行」分组中的独立入口合并到首页位。非管理员访问首页时按权限软回退到会话 / Agent / 用户 / 设置等首个可用页，避免登录即无权限。
- 移除「运行监控」页面与侧边栏入口；失败率 KPI 下钻改到会话列表（阶段=FAILED）。后端 `/admin/runtime/sessions` 与 CLI `mao runtime` 仍可用。

## 0.0.142 (2026-09-17)

### 管理后台

- 调用流水「用户」「模型」筛选由数字 ID 输入框改为可搜索下拉（用户取自用户列表，模型取自模型配置，含已停用）；此前输入名称会被当成非数字 ID 静默丢弃导致筛选不生效。会话 ID 仍支持直接输入。
- 调用流水列表与详情的「缓存」列展示缓存命中率百分比（`缓存Token (命中率%)`，与桌面端使用记录一致）。

## 0.0.141 (2026-09-17)

### 管理后台

- 用量分析「统计周期」新增今日 / 昨日 / 3 天，与原有 7 / 30 / 90 天并列；默认选中「今日」，环比对照前一日。
- KPI 环比色改为红=好、绿=坏（对调原绿好红坏），与业务习惯一致；好坏语义仍由指标本身决定（活跃降=坏，Token 降=好）。

### 后端

- `/admin/analytics/summary` 新增可选参数 `endOffset`（默认 0，clamp 0–365）：将统计窗口结束日相对今天前移 N 天，用于「昨日」等固定日窗口；原有 `days` 1–90 语义不变。

### 终端 CLI（mao-cli）

- `mao analytics summary` 支持 `--end-offset`，可查询今日（`--days 1`）与昨日（`--days 1 --end-offset 1`）。

## 0.0.140 (2026-09-17)

### 前端（桌面 / Web / 安卓）

- 新建会话居中输入框字号对齐会话态输入框：正文与占位符由 17px（`--aw-text-body`）改为 14px（`--aw-text-caption`），行距 1.5 不变，快捷指令/文件标签随正文同步缩小；桌面与移动端居中态现在同字号。
- WebSocket 自动重连握手失败会结束在途连接 Promise，避免网络指示已恢复但原发送一直挂起。
- 切换会话后，待办、消息队列和历史上拉的慢响应只写入原会话缓存，不再覆盖当前会话。
- 聚焦模式已加载时新建任务会立即进入聚焦列表。
- 编辑最后一条用户消息并重发时，失败回滚和收尾刷新都绑定开始编辑的那条会话，切换走后不会把当前会话的消息整表换成编辑前快照。
- 边路任务停止、以及边路/子代理重试会等待发送结果：网络不可用时提示失败并恢复按钮，不再把停止入口关掉或把重试按钮卡死。

### 桌面 Electron

- 本地 shell 在 `wait_for` 命中后立刻 `exit` 时先排空 stdout 再判定结果，避免 Linux 上进程退出早于管道输出导致漏掉匹配。

### 后端

- CLOUD shell 同样等 stdout 排空后再把「wait_for + 立即退出」标为完成，避免误报仍在运行或丢掉匹配文本。
- 后台子代理重试收尾先落库并入队再释放运行标记，避免父代理提前结束把结果标为抑制；正式结果使用完整回答，不再截成 2000 字进度预览。
- `task_create` 按会话当前最大 `sort_order` 追加待办，不再从 0 重计，避免新事项插到既有列表中间。
- 云端 `write_file` / `edit_file` 写回时保留原文件的 CRLF 与 BOM，与 LOCAL 一致，避免 Windows 检出被整文件改成 LF。
- 文件变更行数统计与读文件、CLI 对齐：末尾换行不再算成多出来的空行。

### 管理后台

- 保存角色权限期间禁用勾选和角色切换，避免在途保存响应覆盖刚勾选的权限。

### 终端 CLI（mao-agent）

- 同步 `vendor/localShell.cjs`：与桌面本地 shell 一致，立刻退出的命令不再漏掉 `wait_for` 匹配。
- LOCAL `write_file` / `edit_file` 按路径串行化读写，同一轮并行改同一文件时两次修改都会落盘。
- `--max-duration` 从自己这条执行开始计时；等待其他客户端占用会话时超时只结束本地等待，不会 cancel 对方，随后自己的任务仍受墙钟限制。

## 0.0.139 (2026-09-16)

### 安卓原生

- 启动图标更新为新版品牌标识（深色圆角容器 + 金色羽毛）：各密度 `ic_launcher` / `ic_launcher_round` 与自适应图标前景图重新导出，自适应图标背景色对齐 `#1C1C1E`，桌面与启动器显示更清晰。需发新 APK 生效。
- 本次 OTA 仅原生壳图标资源变更；其余前端改动（如全局下拉列表样式）由 App 远程加载页面生效，刷新即得，无需更新 App。

## 0.0.138 (2026-09-16)

### 前端（桌面 / Web / 安卓）

- 全局统一下拉列表样式：所有 `el-select` 下拉选项固定为 14px（`--aw-text-caption`）+ 应用字体 + 常规字重 + 深色文字，选中项统一为「淡蓝底 + 常规字重 + ✓」（不再用 Element Plus 默认的加粗主色）。原因是全局 `--el-font-size-base` 被覆盖为 17px，EP 下拉项默认取它，导致下拉文字比输入框和「模型选择」列表大一圈。各组件自带的 popper 定制（输入区紧凑下拉 12px、仓库选择器等宽布局）优先级更高，保持不变。涉及设置页用量筛选等所有下拉。

## 0.0.137 (2026-09-16)

### 前端（桌面 / Web / 安卓）

- 工作区下拉列表文字过大偏粗的真正原因：全局把 Element Plus 的 `--el-font-size-base` 覆盖成 17px（对齐 App 正文），而 `el-select` 下拉项正好用这个变量，于是比输入框（大号 14px）和「模型选择」列表大一圈。现显式把工作区下拉项定为 14px + 应用字体（`--aw-font-text`）+ 常规字重 + 深色文字，与「模型选择」列表完全一致（0.0.136 只改了字重/选中色，未解决字号）。

## 0.0.136 (2026-09-16)

### 前端（桌面 / Web / 安卓）

- 工作区下拉列表样式与「模型选择」列表对齐：选中项不再用 Element Plus 默认的加粗 + 主色文字，改为深色常规字重 + 淡蓝底标示，普通项文字统一为深色常规字重（适用于移动端「工作区设置」抽屉与新建会话配置条的下拉框）。

## 0.0.135 (2026-09-16)

### 前端（桌面 / Web / 安卓）

- 新建会话居中输入卡：模型名字号回落到与会话态输入框一致（12px），保留放宽后的可视宽度（148px），仍不按字数硬截断。
- 移动端「工作区设置」底部抽屉：工作区下拉框与 Git 地址/分支输入框由小号改为大号（40px、14px 字号，与智能体搜索框一致）；「云端/本地」「现有/空白/Git」分段按钮同步放大到 40px 高的 14px 文字，与输入框对齐。

## 0.0.134 (2026-09-16)

### 前端（桌面 / Web / 安卓）

- 新建会话居中输入卡样式修复：工具条上的模型名放大到 14px、可视宽度放宽到 148px，并去掉「固定 8 字 + 省略号」的硬截断（改由 CSS 省略号按实际宽度截断，长名仍可悬停看全名），不再只露出 `ds-v4.1-…`；发送按钮恢复品牌蓝（居中态此前被覆盖成黑色，现与底部会话态一致）；移动端「选择智能体」底部抽屉的搜索框由小号改为大号（40px、14px 字号），并与列表之间补上间距，列表改为占满抽屉剩余高度。

## 0.0.133 (2026-09-16)

### 前端（桌面 / Web / 安卓）

- 修复飞书话题群新建任务后「工作区」名跟着话题标题变的问题：话题多会话下会话标题是话题首条消息摘要，不是群名。右侧工作区标签、输入区 chip、左侧 `FEISHU_GROUP` 分组标签均改为按工作区路径合成稳定标识（如 `飞书群{botId}·{chatId前缀}` / `Agent:飞书群…`），不再读取 `session.title`。磁盘上仍是一群共享一个工作区目录，与设计一致。

### 后端

- 飞书任务进度卡片新增「会话详情」按钮（`open_url`）：点击在浏览器打开网页端对应会话页 `/tasks/{id}`。执行中与「取消任务」并排，终态卡片仅保留该按钮；「取消任务」回调返回的终态卡同步带上。站点地址取 ECP 配置的 `desktopCallbackUrl` origin（私有化部署需保证该地址为正确公网域名），解析失败时不渲染按钮。定时任务结果回流卡片同样生效。

## 0.0.132 (2026-09-16)

### 前端（桌面 / Web / 安卓）

- 品牌 Logo 重设计：主标识改为深色圆角容器上的金色羽毛，替换原双菱形波浪标。已更新 README 图、桌面/管理后台 `app-icon-small.png` 与 favicon、安卓各密度启动图标；安卓自适应图标背景色对齐为 `#1C1C1E`。设计说明见 `docs/assets/logo-design/`。

### 管理后台

- 同步替换侧边栏与登录页使用的应用图标资源。

## 0.0.131 (2026-09-16)

### 后端

- LLM 调用不再向任何协议（OpenAI Chat / Responses / Anthropic Messages）发送 `max_tokens` 与 `max_output_tokens` 参数，输出上限交由上游/网关默认策略决定；删除 `RESPONSES_MAX_OUTPUT_TOKENS`、`ANTHROPIC_MAX_OUTPUT_TOKENS` 常量，连通性探测客户端同步去掉。

## 0.0.130 (2026-09-16)

### 前端（桌面 / Web / 安卓）

- 新建会话改为中心化 Composer：桌面主区垂直居中展示「问候语 + 大输入框 + 快捷起步」；智能体/模式/工作区收进工具条 chip（点开浮层配置），不再默认铺开智能体网格墙。已有会话仍为底部输入。
- 窄屏（≤768px，含移动 Web/安卓壳）新建会话使用上锚定紧凑态：缩短编辑区、chip 可换行、智能体与工作区改为底部抽屉，避免软键盘顶出发送按钮。

## 0.0.129 (2026-09-16)

### 终端 CLI（mao-cli）

- `MAO_BASE_URL` / `--base-url` 与 `mao-agent` 共用：可写站点根、`/api` 或 `/api/v1`，CLI 自行归一到 `/api/v1`。

### 后端

- 飞书通道 ECP/绑定引导改为交互卡片按钮跳转（`open_url`），不再把超长授权 URL 写进文本；私聊与群聊一致。卡片发送失败时回退文案提示去桌面/网页登录，避免链接被飞书截断。
- 飞书 ECP 引导卡片橙色标题为「新用户绑定」；正文「请点击下方按钮完成ECP用户绑定」，按钮「点我绑定」。

### 终端 CLI（mao-agent）

- 改读 `MAO_BASE_URL`，移除 `MAO_AGENT_BASE_URL`。同样接受站点根、`/api`、`/api/v1`。

## 0.0.128 (2026-09-16)

### 后端

- 修复飞书通道 ECP 登录引导只有文案没有链接：ECP 返回的 OAuth `state` 超过 `feishu_pending_binding_message.state` 的 64 字符上限，写入待重放记录失败后把已拿到的授权 URL 也清掉了。现将列扩至 768（V116），落库失败仍把登录链接发给用户。
- 修复 Responses 协议会话中途切到 Chat Completions 协议后请求 400（`messages[N]: missing field \`type\``）：Responses 流式 toolCall 补写 `type: 'function'`；Chat Completions 序列化对历史缺失 `type` 的 tool_calls 兜底为 `function`。升级后端后原会话可继续使用，无需清空历史。
- 修复定时任务执行失败后仍向飞书/微信推送上一轮对话旧回复的问题：`liveExecution`（WS 执行路径）内部 catch 吞掉异常并落会话 FAILED 终态后正常返回，`executeTask` 误判为成功、标记 COMPLETED 并调用结果推送；`sendFeishuReplyIfApplicable` / `sendWeixinReplyIfApplicable` 从会话历史取最后一条 ASSISTANT 消息（本次失败未产出新消息，取到的是上一轮旧回复）推给用户。现 `liveExecution` 返回后回读会话真实终态，FAILED/CANCELLED 时不标 COMPLETED、不推送回复。
- 修复飞书群聊【群内最近消息】中机器人卡片仍显示“请升级至最新版本客户端，以查看内容”的问题：此前仅修了引用预取的消息详情接口，`im.message.receive_v1` 事件里 interactive 卡片 content 被飞书降级为该文案后仍会原样落入群消息日志；现归一化与卡片文本提取均过滤该降级文案，入库占位为 `[卡片消息]`，并后台按 messageId 拉详情（`card_msg_content_type=user_card_content`）回填真实卡片文本。

## 0.0.127 (2026-09-26)

### 后端

- 子代理角色对齐 Codex 并 thin 扩展：内置角色改为 `default` / `explorer` / `worker` / `reviewer`；新增通用 `default`（完整继承父会话）；`researcher`→`explorer`、`coder→worker` 运行时归一并由 Flyway 迁移历史 `agent_type`；`reviewer` 保留为正式 thin 角色（仅 description + system prompt，无工具黑名单）。
- `spawn_subagent` 工具 schema 暴露四角色枚举，工具说明与 PromptEngine 写入委派纪律（选型、并行写入范围、worker 归属权、审查闭环 reviewer → worker → followup reviewer）。
- 未知 `agent_type` 仍报错；仅旧名 researcher/coder 归一；崩溃恢复与 followup 兼容历史类型。

### 前端（桌面 / Web / 安卓）

- 子代理工具卡片继续透传 `agent_type` 英文名（本轮不做中文映射）；相关测试样例改为新角色名。

## 0.0.126 (2026-09-15)

### 前端（桌面 / Web / 安卓）

- 用户设置「使用记录」列表新增「缓存」「首字」列，展示缓存命中 token 数（含命中率百分比）与首 token 延迟；筛选区与分页改为小尺寸，表格列宽收紧以适配内容，每页条数支持 10/20/50/100。

## 0.0.125 (2026-09-15)

### 前端（桌面 / Web / 安卓）

- 修复云端工作区聊天内「文件变更」面板右键「下载此文件」提示「当前模式不支持下载」：`fileProvider` 注入的是 ComputedRef，原先未解包导致下载能力判断失败。

## 0.0.124 (2026-09-16)

### 后端

- 飞书话题群多会话：话题群内一个话题自动映射为一个 Mao 会话，用户在新话题发消息即创建新会话，在已有话题发消息自动路由到对应会话，多会话独立并行执行。
- 修复话题内消息 `parent_id` 误判为用户引用：话题内回复的 `parent_id` 恒指向话题根（飞书官方行为），现在仅当 `parent_id ≠ root_id`（显式引用话题内某条回复）时才注入引用内容。
- 话题内消息无需 @机器人 即可触发（话题会话映射命中时跳过 mention 门禁）；非话题群行为不变。
- 新增 `feishu_thread_session` 映射表；`feishu_group_message_log` 新增 `thread_id` 列，话题内上下文按话题过滤注入。

## 0.0.123 (2026-09-15)

### 后端

- 新增 `llm_call` 表与统一落库：`LlmAdapterFacade` / `LlmChatClient` 装饰器记录每次 LLM 调用的场景、Token、首字/总耗时、成败与重试次数；对话主链路、压缩、标题、Git 提交信息、危险评估、语音合成、飞书摘要、连通性测试均写入。
- 新增 `GET /v1/admin/llm-calls`（管理员全站）与 `GET /v1/llm-calls/me`（当前用户）分页查询接口。

### 管理后台

- 新增「运行 → 调用流水」页：按用户/会话/模型/场景/时间筛选，展示 Token 与延迟明细。

### 前端（桌面 / Web / 安卓）

- 设置新增「使用记录」Tab，查看当前用户的 LLM 调用流水，支持跳转关联会话。

### 终端 CLI（mao-cli）

- 新增 `mao llm-call me` / `mao llm-call list` 查询调用流水（见 `skills/mao-cli/reference/llm-call.md`）。
- 产品文档与 CLI 帮助改为用 `mao.example.com` 占位；开源示例域名仅在配置说明中作为最低优先级回落提及。

### 终端 CLI（mao-agent）

- `--help` 不再展示开源示例域名，改为提示私有化必须设置 `MAO_AGENT_BASE_URL`。

## 0.0.122 (2026-09-15)

### 后端

- CLOUD 会话技能同步由「复制目录」改为「符号链接」：runtime 下的 `skills/<name>` 现在指向技能源目录（全局技能 / 用户技能），不再拷贝文件。定时清理删除 runtime 副本只删链接、不动源，此前「技能内 CLI 用 `npm install . -g` 全局安装后，runtime 副本被清理导致 CLI 失效」的问题不再出现；技能源更新后链接即时生效，无需重新同步。旧版本遗留的实体副本目录与悬空链接会在下次同步时自动替换为正确链接。

## 0.0.121 (2026-09-15)

### 后端

- 修复 ECP 会话 renew 必然失败：`renewSession` 不再复用登录解析，ECP renew 响应不含用户信息，此前被误判为「ECP 响应缺少用户邮箱」→ 会话被标记 `FAILED` → CLOUD shell 不再注入 `ECP_TOKEN`，`bigdata-cli` 等内部 CLI 报「ECP 登录态已失效」。
- ECP 会话不可用（renew 失败 / 已过期 / 解密失败）时清理虚拟 HOME 的 AccessOne 布局，避免下游 CLI 读到已作废旧票而误报登录态失效；同时输出跳过注入的原因日志。
- ECP 开启时飞书通道入站增加有效票闸门：已绑定但没有可用 `user_ecp_session`（缺票 / 过期 / renew 失败）也视为未授权，引导改为 ECP 飞书登录（不再优先 Mao 飞书 OAuth）。登录成功后按邮箱对齐已绑用户并重放原消息；邮箱对不上则不改绑，飞书侧提示联系管理员。

### 管理后台

- ECP 配置面板补充：开启后飞书机器人通道会检查发送者是否有有效 ECP 票。

### 前端（桌面 / Web / 安卓）

- 设置页「飞书Bot」在 ECP 开启时说明：仅绑定飞书仍不能触发任务，还需用同一邮箱完成 ECP 飞书登录。

## 0.0.120 (2026-09-14)

### 后端

- 飞书进度卡片终态补充任务耗时：完成/失败/取消时状态行改为「状态：处理完成 · 共 8 轮 · 耗时 8 分 26 秒」（不足 1 分钟只显示秒、超过 1 小时显示「x 小时 y 分」）；执行中仍为「第 n 轮」且不显示耗时。耗时从卡片就绪（任务开始执行）时刻起算，排队等待不计入；崩溃恢复续跑的任务从崩溃前的开始时间起算。

## 0.0.119 (2026-09-14)

### 后端

- 取消 ECP 与其它登录方式互斥：密码/LDAP、Mao 飞书、公司 SSO 换票、ECP 飞书可同时可用；仅 ECP 飞书登录会写入 `user_ecp_session` 供 CLOUD shell 注入。
- CLOUD shell / 云端终端：用户有有效 ECP 票时除 AccessOne 布局外，额外注入环境变量 `ECP_TOKEN`（与 `MAO_TOKEN` 并列）。
- 修复 ECP 飞书登录回调报「state 不存在」：OAuth state 改为使用 ECP `feishu-authorizations` 返回值（不再预生成 UUID）。
- 迁移 V112：`ecp_oauth_state.state` 扩至 `VARCHAR(768)`，修复 `/auth/ecp/feishu/start` 写入 ECP state 时 500。

### 管理后台

- ECP 配置面板说明改为「新增登录入口、不关闭其它方式」。

### 前端（桌面 / Web / 安卓）

- ECP 与 Mao 飞书并存时登录页分别展示「飞书登录」「ECP 飞书登录」两个按钮；飞书 Bot 未绑定引导优先 Mao 飞书 OAuth，无配置时再回退 ECP。
- 修复安卓软键盘遮挡新建会话输入区：键盘弹出后可视高度骤减时，输入卡片改为参与收缩，新建会话的智能体/模式/工作区配置区内部滚动，输入框与发送按钮始终完整可见（进入会话后原本就正常）。

## 0.0.118 (2026-09-14)

### 后端

- 新增 ECP 原生飞书登录：管理后台「集成配置 → ECP 飞书登录」开启后，密码/LDAP/Mao 飞书/公司 SSO 换票入口关闭；飞书授权经 ECP `feishu-authorizations` + `sessions` 换票，按邮箱映射 Mao 用户并加密存储 `sessionToken`；常驻 `EcpRenewScheduler` 在到期前约 30 分钟 renew；CLOUD shell/云端终端向虚拟 HOME 写入 AccessOne 兼容目录供 `bigdata-cli` 等读取 Bearer。迁移 V111（`auth.ecp.config`、`user_ecp_session`、`ecp_oauth_state`）。设计见 `docs/plan/ecp-native-login-technical-design.md`。
- ECP 开启时飞书 Bot 未绑定引导改为 ECP 飞书授权链接；登录成功后按消息事件中的 `union_id` 写入 `feishu_binding` 并重放原消息。设置页飞书绑定仍走 Mao 飞书 OAuth（仅绑定、非登录），ECP 模式下回调豁免。
- 新增 `POST /v1/auth/admin/login`：ECP 开启时管理后台仍可用账号密码登录；`POST /v1/auth/login` 继续对桌面端拒绝。

### 管理后台

- 系统设置新增 ECP 飞书登录配置面板；ECP 开启时登录页保留账号密码，并可选飞书登录；回调路由 `/admin/auth/ecp/feishu-callback`。账号密码走 `POST /v1/auth/admin/login`（桌面端仍仅 ECP 飞书）。

### 前端（桌面 / Web / 安卓）

- ECP 开启时登录页仅飞书入口；Web/Electron 回调 `/auth/ecp/feishu-callback`；Electron 授权窗支持 ECP 回调路径并可直接完成换票。设置页「飞书Bot」在 ECP 模式下说明绑定与全站登录分离。

### 桌面 Electron

- `openFeishuAuthWindow` 支持自定义回调路径，ECP 飞书登录完成后提取 `code`/`state` 交回主窗口。

## 0.0.117 (2026-09-14)

### 前端（桌面 / Web / 安卓）

- 修复 Web Embed SDK 对话窗口回显时执行过程未自动折叠：后端每个工具轮次落一条无正文 ASSISTANT，历史加载后会拆成一叠「1 个工具调用」。现在相邻纯工具 assistant 消息合并为一组折叠卡片（对齐桌面端 `mergeAdjacentToolMessages`），带正文的消息不吸收、流式气泡不参与合并。

## 0.0.116 (2026-09-14)

### 前端（桌面 / Web / 安卓）

- Web Embed SDK 页面操作修复 Element `el-select`（`filterable` + `multiple`）下拉点不中：`page_click` 改为派发完整鼠标序列（pointer/mousedown/mouseup/click），点中下拉 option 后回读并返回 `observation.selected`，避免 Agent 把「只填了关键字」当成已选中服务。快照为 Element 表单项内过滤框补充 form-item 标题标签。新增多选下拉集成测试覆盖「过滤框 fill → 重新 inspect → 点 option → 确认选中」完整路径。
- 同上场景继续修复「填了服务名但下拉仍不展开、没有可点选项」：`page_fill` 对 combobox/搜索框在建议为空时自动对容器派发鼠标序列展开弹层并重放输入，返回 `observation.dropdownOpened`，避免只 focus/input 导致 Element 多选弹层未打开。

### 后端

- 修复会话压缩后 Agent 把系统交接指令当成用户原话、停下当前任务：压缩指令改为 `<system-notice>` 系统通知（明确不是用户说的话，禁止写入交接正文）；压缩后若增量历史里没有真实用户消息，自动把最新一条用户原话接回交接消息之后，后续真实请求不再被「不要继续执行」盖掉。
- 修复后端重启后续跑飞书任务时进度卡片停在重启那一刻：进度卡映射查询被 `Db` 转成 camelCase 后仍按 snake_case 取值，拿不到 bot/卡片 ID，恢复监听挂不上。现在按实际返回字段读取，字段不全视为无映射。

## 0.0.115 (2026-09-14)

### 后端

- 修复飞书排队卡片点「立即发送」后闪一下又回到「排队中」、消息看起来没发出去：`card.action.trigger` 回调必须在 3 秒内带回新卡片，只 PATCH、返回空体会被飞书客户端还原为点击前内容。现在插队/取消都会在回调里返回 JSON 2.0 卡片；插队不再额外 PATCH，避免盖掉随后升级的进度卡。
- 修复后端重启后飞书进度卡「取消任务」无效、任务一直卡住：崩溃恢复续跑只把取消标志挂在引擎上，飞书按钮原先只查内存 flag，查不到就当成已结束。现在与桌面端停止对齐——置位引擎取消标志，若内存没有执行则把仍为 RUNNING/RESUMING 的会话落成 CANCELLED，排队消息可继续消费。

## 0.0.114 (2026-09-11)

### 后端

- Agent 新增「推荐问题」配置：新建/编辑/复制时随请求体嵌套 `suggestedQuestions` 全量同步，单 Agent 最多 5 条、单条 1～100 字（超限硬拒绝，新增错误码 3028/3029）；随 `GET /v1/agents/{id}` 详情返回，列表接口不返回；删除 Agent 时级联物理删除其推荐问题。新增迁移 `V110__agent_suggested_questions.sql`。
- `session` 表新增 `source` 列（迁移 V109，`web`/`embed`，默认 `web`）标记会话来源，并建 `(user_id, agent_id, source, updated_at)` 索引服务浮窗历史列表查询。
- `POST /v1/sessions` 支持可选 `source` 字段（仅接受 `web`/`embed`）；新增 `PUT /v1/sessions/{id}/source` 供 SDK 惰性补标存量常驻会话（仅允许 `web → embed` 单向、幂等、校验归属）。
- `GET /v1/sessions` 新增可选 `agentId` / `source` 过滤参数与独立 `offset`/`limit` 分页（上限 50，默认 20），用于 SDK 历史列表；不传新参数时行为完全不变。会话 VO 透出 `source` 字段。
- 修复 GLM（如 glm-5.3）多轮工具调用报 400「'messages.N.reasoning_content' must be a gateway-issued carrier」：`reasoning_content` 回传改为按模型白名单生效，仅 DeepSeek 系模型（含 `deepseek` 子串 / `ds-` 前缀）继续原样回传，其余模型（GLM、GPT 等）在序列化请求体时剥离该字段，避免被把该字段视为自家签发载体的网关拒绝。历史存储与恢复链路不变。

### 管理后台

- Agent 新建/编辑/复制弹窗新增「推荐问题」Tab：增删、上下移排序、字数与条数校验（≤5 条、单条 ≤100 字），允许 0 条；复制 Agent 一并复制推荐问题。

### 前端（桌面 / Web / 安卓）

- Web Embed SDK 对话浮窗新会话空白态展示 Agent 推荐问题（最多 5 条，按配置排序），点击后填入输入框并聚焦，由用户确认后手动发送；发出首条消息后隐藏，点「新对话」重新出现。未配置或拉取失败时展示与之前完全一致。
- Web Embed SDK 对话浮窗新增历史对话列表：标题栏「历史」入口展开当前 Agent 的会话列表（仅显示 SDK 浮窗创建的会话，按最近活跃排序，标题 + 相对时间，当前会话高亮），点击即切换，触底自动加载下一页；面板顶部提供「新对话」。历史会话与桌面端会话列表共享同一份数据，切换不影响桌面端使用。
- Web Embed SDK 创建会话不再固定传「网页助手」标题，改走后端默认占位符：首条消息后由 LLM 自动生成会话标题，历史对话列表与会话列表的条目从此可辨识。存量「网页助手」标题的会话不追溯改写。
- Web Embed SDK 切换历史会话时进行中的任务不中断（服务端继续跑完），切回后自动对账还原消息与工具状态；流式输出进行中也可点「新对话」或切换会话，规则统一。新增 `MaoChat.toggleHistory()` 实例方法供宿主控制面板开合。
- 存量场景：升级前由 SDK 创建的常驻会话在首次打开浮窗时自动补打来源标记，历史列表即可见；更早被「新对话」替换掉的旧会话无法识别来源，不追溯。

## 0.0.113 (2026-09-10)

### 前端（桌面 / Web / 安卓）

- Web Embed SDK 内置页面截图不再把 HTML `innerHTML` 直接塞进 SVG：改为 XML 序列化（补齐 void 标签、HTML 实体、`xlink` 命名空间），避免业务页常见的 `<br>` / `<input>` / 图标导致 `screenshot_render_failed`。
- Web Embed SDK 页面截图会剔除跨域图片和 CSS `url()`，避免污染 canvas 后 `toBlob` 失败；若仍无法导出则回退为 DOM 色块+文字绘制，保证工具不中断。
- Web Embed SDK 在工具结果中直接展示页面截图；回复里的 `attachment://` 等虚构图片链接会去掉以免破图。嵌入会话会提示模型不要用 Markdown 再贴一次截图。
- Web Embed SDK 页面截图随工具卡默认折叠，展开后显示小缩略图，点击可看大图。
- Web Embed SDK 对话浮窗不再展示「LLM 等待中（response_headers Ns）」等内部等待诊断；上游真正重试时仍显示重试提示。
- Web Embed SDK 去掉输入区下方「连接已断开，发送时将自动重连」横幅，断线只在标题栏用指示灯和「连接中…」提示。
- Web Embed SDK 页面操作日志会写明点了哪个控件、填了什么；任务进行中在输入框上方显示「正在操作当前页面」，绿点成功、红点失败。
- Web Embed SDK 输入区对齐桌面端：圆角卡片、占位「告诉 Agent 你想做什么...」、发送/停止在底部工具栏；页面授权改为下拉切换，不再单独提供「停止操作」，终止页面动作与终止任务共用停止按钮。
- Web Embed SDK 支持 Vue 等远程搜索下拉：填写 combobox 时保持焦点并等待建议，快照会采集打开的下拉选项，需点击选项而不是调用原生 `page_select`。
- Web Embed SDK 输入框支持直接粘贴剪切板中的图片或文件（无需上传按钮，与桌面端一致）：粘贴后显示缩略图/文件 chip，可逐个移除，最多 10 个，超过后台大小上限的当场提示并忽略。图片随消息发给多模态模型，其他文件上传到会话 runtime 临时目录并以 `@{绝对路径}@` 引用写进正文；用户气泡显示图片缩略图与文件名 chip，刷新后从历史还原。单个附件上传失败只提示失败文件名，全部失败且无正文时不发送。

### 后端

- 公司 SSO 改为只按可信邮箱关联 Mao 账号：测试/生产等环境的 `claims.id` 可以不同，同一邮箱复用同一用户；不再把公司用户 id 当作身份键。
- 嵌入会话在系统提示词和 `page_screenshot` 工具说明中明确：截图已展示给用户，不要用 Markdown / `attachment://` 再贴一次。
- 嵌入会话（持有 `page_*` 工具）改为注入网页浮窗通道基座：页面上下文协议、可见范围、page 工具纪律、截图展示、安全边界与窄浮窗交互；不再灌入编程助手的工作目录、`read_file`/`cat` 用法、上传文件说明和 `AGENTS.md`。管理后台「角色定义」只需写身份、业务目标与文风。
- 嵌入会话提示词补充浮窗粘贴附件的说明：粘贴的图片作为视觉输入直接交给模型，非图片文件以 `@{绝对路径}@` 引用指向云端临时目录、按绝对路径读取；仍不注入编程助手的上传目录与工作区说明。
- 嵌入会话页面提示词与 `page_fill` / `page_select` / `page_inspect` 说明补充远程搜索下拉的操作顺序：先填写并等待建议，再 inspect 后点击选项。
- 修复蓝绿部署/重启后续跑飞书任务完成后进度卡片仍停在「正在处理」：崩溃恢复成功结束时会 PATCH 卡片为已完成（失败路径同步等待失败终态），与正常飞书执行收尾一致。

### 管理后台

- Agent「角色定义」说明改为：通道规则由系统注入，此处只填角色与风格；已写过页面上下文或工具纪律的提示词可删掉重复段。

---

## 0.0.112 (2026-09-08)

### 前端（桌面 / Web / 安卓）

- Web Embed SDK 新增页面操作 Agent：Agent 可在用户授权下读取当前页面可见交互元素快照、截取当前视口截图，并执行滚动、聚焦、填写、下拉选择、勾选、键盘输入和点击等动作。业务页面只需 `MaoChat.init()`，无需修改按钮、表单、路由或回调，也无需增加 `data-*` 标记。
- 页面操作默认「每次确认」，用户可在浮窗中切换为「本次任务授权」或「完全授权」（完全授权按用户身份持久化，刷新后仍有效），并可随时撤销授权或停止页面任务；浮窗显示目标元素高亮、动作日志和执行状态。
- 页面动作执行前校验快照与元素引用，执行后回读结果；页面导航或结构变化后旧引用失效，需重新观察，避免误操作新页面对象。
- 页面快照默认脱敏密码、验证码、银行卡等敏感字段；`per_action` 下截图默认遮罩敏感区域，发送未遮罩原图需用户确认。
- 支持主文档、同源 iframe 与开放 Shadow DOM；跨域 iframe、闭合 Shadow DOM、文件选择器、新标签页/下载链接、坐标点击和任意脚本执行会明确拒绝并返回原因。

### 后端

- 新增 `page_inspect`、`page_screenshot`、`page_scroll`、`page_focus`、`page_fill`、`page_select`、`page_check`、`page_uncheck`、`page_click`、`page_keyboard`、`page_wait`、`page_observe`、`page_actions` 页面工具，仅对绑定了嵌入式页面连接的会话可见，其他会话（桌面/CLI/微信/子代理）不可见。
- 页面工具通过 `page_tool_request` / `page_tool_result` 与浏览器 SDK 双向通信：请求只发往该会话绑定的页面连接，回包必须来自同一连接，按连接隔离，超时、用户停止、连接断开时统一收敛为可读错误。

## 0.0.111 (2026-09-08)

### 前端（桌面 / Web / 安卓）

- Web Embed SDK 任务执行中的 loading 改为与桌面端相同的三点脉动，不再使用闪烁光标；工具正在执行时不叠一层等待动画。

- Web Embed SDK 浮窗标题栏改为显示当前 Agent 名称（管理后台配置），不再固定为「网页助手」；名称未配置时仍显示「Mao 助手」。

- Web Embed SDK 在 Agent 输出过程中不再禁用输入框，可继续编辑下一条消息；本轮结束前不会发出（无消息队列），空内容时仍显示停止。

- Web Embed SDK 发出带页面选中引用的消息后，用户气泡回显「讨论」块，刷新历史仍可看到这段文字已交给 Agent；页面上下文 JSON 仍不上屏。

- 关闭最后一个终端标签页后，终端面板自动收起，不再残留空面板。

- 长任务执行中若模型流中断并自动重试，已输出的说明和已完成的工具调用不再从画面消失，只丢掉本次未完成的生成；刷新仍可从历史恢复完整过程。Web Embed SDK 浮窗同样保留已完成步骤。

- 补齐飞书文档读取、文件下载、图片发送和文件发送工具的中文名称与执行中参数预览，不再只显示英文工具名。

- 左侧任务列表切换历史会话时，对话窗口显示转圈与“正在加载历史对话…”提示；无消息时居中展示，有缓存时保留内容并轻量提示，加载结束后自动消失。

### 管理后台

- 公司 SSO 的宿主 Origin 白名单支持精确 HTTPS、`https://*.acg.team` 子域模式和 `*` 全来源，表单同步校验与风险提示；子域模式不包含根域且严格匹配端口。

- 系统设置的集成配置新增「公司 SSO」：统一编辑启用开关、校验域名与宿主 Origin 白名单、Access 有效期和校验超时，完整保存后对新换票即时生效。

- Agent 编辑表单收敛为基本信息、角色提示词、最佳实践三个 Tab，工具能力与运行设置并入基本信息；角色提示词默认 15 行。头像改为紧凑预览卡片，更换与移除操作横向排列，格式限制分行说明并适配窄屏。

### 后端

- 公司 SSO 校验上游 `Content-Type` 时允许 `application/json;charset=UTF-8`（分号前无空格）；换票 503 日志带不含凭证的 `detail`。checkToken 响应上限从 64KB 提到 1MB（公司 claims 常超过 64KB），超限时 `detail` 为 `oversized:<实际字节>`。

- 公司 SSO 首次换票允许按可信邮箱绑定已有启用的管理员账号，不改角色、密码或飞书绑定；新建账号仍只授普通用户。禁用/删除账号与邮箱冲突仍拒绝。

- CORS 预检改为反射客户端请求头，不再用静态 `allowedHeaders` 白名单。宿主页注入的 SkyWalking `sw8` 等自定义头可通过跨源换票与 REST，无需为每个头改后端；`Authorization` 仍由反射覆盖（`*` 对它无效）。

- 会话压缩摘要请求补回与主请求相同的 `prompt_cache_key`，Responses 协议下压缩调用可走同一前缀缓存路由，避免因缺会话粘性键而随机未命中。

- 为四个飞书内置工具补齐结果摘要：文档链接与字符数、下载文件路径、发送目标文件名以及成功 / 失败状态；不改写历史已保存摘要。

- 公司 SSO 换票与 CORS 共用 Origin 通配匹配规则；`*` 放开网页来源但不放开校验域名、HTTPS 或身份认证，保存后的配置对新请求生效。

- 公司 SSO 配置迁入系统设置 `auth.companySso.config`（V107），移除 `SSO_*` 环境变量读取且不自动导入；按请求读取完整快照用于 CORS、白名单和换票，保存后无需重启。HTTPS 与受信代理基础设施边界不变，关闭配置不立即撤销已签发凭证。

- 修复工具执行后部分模型渠道返回 `400 messages.*.tool_calls.*.summary`：Chat Completions 请求不再发送内部工具摘要 `summary`，保留会话内部摘要及工具调用参数、结果；流式与非流式请求均生效。
- 修复模型调用失败时重复叠加 `LLM call failed:` 的问题：Agent 错误回调原样透传异常，保留上游状态码、错误详情及异常类型。
- Shell：`wait_for` 命中后若 bash 已因 `exit`/`exec` 退出，立即 `completed:true` 并交付剩余输出，不再误报「命令仍在运行」；后续 `await_async` 可收取已死会话的最后输出，而不是「会话不存在或已关闭」。常驻会话里请用 `break` 而不是 `exit` 结束循环。

### 桌面 Electron

- 本地 Shell 与云端对齐：`wait_for` 命中后进程已退出时按命令结束交付，`await_async` 不再对刚 `exit` 的会话报「不存在」。

### 终端 CLI（mao-agent）

- LOCAL Shell 同步上述等待语义（`vendor/localShell.cjs`）。

## 0.0.110 (2026-09-07)

### 前端（桌面 / Web / 安卓）

- Web Embed SDK 入口支持鼠标/触摸拖动，松手自动左右贴边并记住高度；拖动不误触打开，刷新恢复位置，窗口缩放及移动端可视区域变化时修正边界。浮窗按入口上下空间展开；隐藏自带入口时保留原定位。

- 修复主聊天切换会话偶现不定位到底部的问题：恢复期布局滚动不再误判为用户上滑，等待历史消息加载后收尾，晚到的 Markdown 渲染继续跟随；旧会话分页与滚动回调不再覆盖新会话位置，主动上滑仍暂停跟随。

- Web Embed SDK 公司 SSO 的校验地址改由 `MaoChat.init` 的 `auth.checkUrl` 指定；服务端以 `SSO_ALLOWED_DOMAINS` 支持完整域名或上级域名白名单，匹配自身及子域，取消写死校验地址，仍使用公司 checkToken 协议。

- Web Embed SDK 新增公司 SSO 初始化模式，宿主提供校验地址和当前 SSO Token，无需后台后端换票；支持主动续期、同连接 WS 认证更新、身份切换清理与按服务/用户/Agent 隔离缓存。SSO 有效期间不限制连续使用时长，故障时不无限续签。

- Agent 选择入口与 Web Embed SDK 支持展示管理后台配置的头像，跨端共用 Agent API 的 `avatarUrl`。

- Web 嵌入式对话 SDK 入口重设计为「对话轮廓 + M 形声波 + 连接圆点」矢量标识，圆形底座加入轻高光与细内描边；浮窗标题栏复用同一标识，保留主色自适应、运行环、未读提示和键盘操作。

- Web 嵌入式对话 SDK 改为按事件时序穿插显示思考、正文与工具调用，不再按类别集中排列；思考和连续工具调用默认折叠，工具摘要显示数量、执行中及失败状态，展开可查看参数与结果，正文保持可见。刷新后恢复历史工具组及结果，缺失结果明确标识；历史仅能恢复持久化轮次顺序，不伪造单条聚合消息内的细粒度交错。

### 管理后台

- Agent 支持上传、更换及移除头像，后台表格与移动卡片同步展示；创建、编辑、复制表单改为基本信息、角色提示词、最佳实践、工具能力、运行设置五个 Tab，统一保存校验并定位错误分组。

- 统一会话、用户、Agent、提示词版本、运行监控、定时任务、审计日志及会话消息的时间展示为 `yyyy-MM-dd HH:mm:ss`，同时修复移动卡片与完结时间提示；按北京时间展示，空值显示 `-`，非法时间明确标识。

- Agent 管理新增「提示词版本」入口，可查看版本号、保存时间、操作人及回滚来源，预览完整提示词并二次确认回滚；仅恢复系统提示词，不影响其他 Agent 配置。

### 后端

- 修复后台子代理追问/纠偏后右侧进度累积上一轮待办的问题：新一轮创建时事务内软删除该子会话旧待办，并在执行前推送空列表；保留聊天与执行历史，被拒绝或事务失败的追问不清空原任务，同轮重试不受影响。

- 增加公司 checkToken SSO 校验换票、可信邮箱关联普通账号及外部身份映射迁移；凭证与用户普通登录等权，实际有效期不超过 SSO 上界，不发 refresh token。增加精确 Origin、换票限流、认证审计、凭证日志脱敏与受信代理 IP 配置；SSO WS 到期拒绝新访问但不取消已受理任务。上线前须核实公司身份/邮箱生命周期并完成真实接口联调，现有 shell 凭证不纳入统一注销。

- Agent 新增 `avatarUrl` 字段及数据库迁移，列表 / 详情 / 创建 / 更新共用；新增需 `agent:write` 的头像上传接口，限制 2 MiB PNG/JPEG/WebP、校验图片内容并重编码 PNG，头像通过公开上传路径展示。

- 飞书群聊消息上下文顺序调整为「群内最近消息 → 引用的消息 → 用户消息」，让引用内容紧邻本次请求；私聊仍为引用内容在用户文本前。
- 修复飞书私聊引用历史消息时消息归属查询误读字段、无法切换会话的问题：当前会话运行中也可切回历史空闲会话并执行，目标会话本身忙碌时仍按目标会话排队。取消排队消息新增明确反馈；卡片更新校验飞书业务响应码，更新失败时提示消息已取消但卡片未刷新。

- Agent 系统提示词自动记录版本：创建及现有数据迁移保存 v1，内容变化时追加版本，回滚保留历史并记录来源版本。提示词与版本记录事务写入，按 Agent 行锁串行编号；历史查询与回滚接口要求 `agent:write` 权限。

- `grep_search` 在未安装 ripgrep（`rg`）时改用异步流式逐行读取，不再静默跳过超过 10 MiB 的文件；保留上下文、正则、大小写、glob 过滤与输出截断，文件读取或目录访问错误明确返回 `error`。本次流式化仅涉及无 `rg` 分支。
- `glob_search`（CLOUD）统一异步按需遍历目录，不再依赖 `rg`；使用 minimatch 支持 `**`、花括号、字符组与字面点，包含隐藏文件，不读取 `.gitignore` / `.ignore`，固定排除依赖、构建等目录（见源码 `glob-search-tool.ts` 的 `IGNORED_DIRS`），遍历不跟随符号链接，访问失败返回 `error`。`head_limit` 须为正整数，仅发现超出上限的额外匹配时才标记 `truncated`；`total_matched` 为返回数而非全量匹配数。与仍区分 `rg` / 无 `rg` 分支的 `grep_search` 不同，以上规则统一适用于 CLOUD 文件名搜索。

---

## 0.0.109 (2026-09-05)

### 前端（桌面 / Web / 安卓）

- Web 嵌入式对话 SDK：`theme.primary` 传入白色、浅黄等高亮度主色时不再出现「文字消失」——按 WCAG 相对亮度派生前景色（浅色主色下自动改用深墨色文字）、可读的主色文字/描边色，以及主色接近白时的一圈描边；浅底色与聚焦环也改为基于该可读色计算。支持 `#rgb`/`#rrggbb`/`rgb()`/颜色关键字。
- Web 嵌入式对话 SDK 移动端适配：浮窗高度改用 `100dvh` 计算，手机浏览器地址栏不再把浮窗底部顶出屏幕；窄屏（≤480px）下按钮、发送键、选项等点击目标放大到触控推荐尺寸，布局与字号不变。
- Web 嵌入式对话 SDK：刷新或重连后重拉历史时，用户消息气泡只显示真正输入的文本，不再回显拼在正文前的 `[页面上下文]` / `[用户选中文本]` 块（与发送时的乐观气泡一致）。
- Web 嵌入式对话 SDK 浮窗信息密度：正文行距与段落间距收紧，400px 宽浮窗内同屏可见内容更多。

### 后端

- 修复蓝绿部署后崩溃恢复可能对同一会话并发续跑两次的问题：延迟恢复的「快照重放」与随后的「全库补扫」两轮会扫到同一会话，首轮已把会话 phase 写回 RUNNING，导致恢复前的 phase 重查无法识别，同一批消息被两个执行并发重放、终态互相覆盖。现按会话做实例内 in-flight 去重。该缺陷的用户可见症状是：任务实际成功完成，却收到一条「执行失败」的钉钉/飞书推送（并发的另一路执行失败并抢先落了 FAILED 终态与通知）。

---

## 0.0.108 (2026-09-05)

### 前端（桌面 / Web / 安卓）

- Web 嵌入式对话 SDK（`sdk/embed`）浮窗界面按 Mao 桌面端设计语言重做视觉：主色默认改为与桌面端一致的 `#0066cc`（宿主仍可用 `theme.primary` 覆盖，hover/浅底/聚焦环会跟随主色自动重算）；标题栏改为「头像 + 会话标题 + 在线/正在处理状态」两行结构；助手回复去掉外层灰底气泡，表格与代码块可用满面板宽度；工具卡状态改为色块标签（执行中/完成/失败），参数与结果用等宽字体；思考过程折叠项加三角指示；空态换成图标 + 标题 + 引导文案；错误/重试横幅、追问卡片、引用 chip、输入框与发送按钮统一圆角与描边层级，并补上悬浮、按下、键盘聚焦反馈与浮窗入场动画（`prefers-reduced-motion` 下自动关闭动效）。

---

## 0.0.107 (2026-09-05)

### 后端

- 离线提问 Webhook 通知：Agent 调用 `ask_user_questions` 时若用户不在线（无 WebSocket 连接），复用任务通知的钉钉/飞书 Webhook 推送"提问通知"，提醒用户回到对话页面回答；问题保持挂起，用户重连后自动重新展示。此前离线时提问直接失败。问题被回答/超时后未发出的通知自动抑制，配置了消息通知的用户默认生效

---

## 0.0.106 (2026-09-05)

### 前端（桌面 / Web / 安卓）

- 修复 Agent 执行过程中上一轮思考块一直显示「思考中...」、直到下一轮思考开始才变为「思考完成」的问题：思考块转圈判定改为要求会话确实处于思考阶段，不再跟随「会话仍在执行」状态。

### 管理后台

- 模型测试仅保留基础调用，移除 mid system message 支持检测、对应输出和「部分通过」状态；基础调用成功即显示测试通过，语音合成测试不变。

### 后端

- 移除模型连通性测试中的 mid system message 额外请求及结果字段，文本模型仅发送一条 `Hi` 用户消息进行基础调用。

- 修复思考块转圈的前端根因配套问题：`thinking_start` 事件改为惰性发送，仅当模型真正输出首个推理增量时才标记思考开始（此前每轮 LLM 调用前无条件发送，模型不输出推理内容的纯工具调用轮会让 thinking 状态横跨整轮，前端据此把上一轮已完成的思考块重新点亮为「思考中」）；`thinking_end` 对称地仅在思考块打开时发送。

- 修复蓝绿部署窗口内新建的会话随旧实例停止而永久卡 RUNNING 的边界问题：崩溃恢复延迟阶段在快照重放后新增一次全库补扫（15s 后触发），兜住「部署窗口内新建、随后随旧实例排空死亡」且不在初始快照里的会话；补扫仅执行一次，恢复前仍重查会话终态，不影响新实例上活跃执行的会话。

---

## 0.0.105 (2026-09-04)

### 前端（桌面 / Web / 安卓）

- Web 嵌入式对话 SDK（`sdk/embed`，产物 `/embed/mao-chat.js`）按评审报告整体加固，修复以下用户可感知问题：
  - 工具调用参数渲染成重复拼接的乱码（后端 `tool_call_args_delta` 下发的是累积全量快照，此前被当增量追加）
  - 浏览器本地记录的会话被删除后永久卡在错误横幅、只能手动清 localStorage（会话失效判据改为按后端业务码 3002 / 1002 判定，自动清记录并新建）
  - 断网重连后工具卡片永久转圈、流式光标常亮，且断线期间产生的回答不显示（`session_snapshot` 终态就地终结残留工具，重连后重拉历史并与本地未落库消息合并对账，不再擦掉正在进行的这一轮，也不会把已落库的中间轮次重复上屏）
  - 首次打开浮窗即产生重复的工具卡片（订阅帧重复发送 + 工具卡未按 `tool_call_id` 幂等）
  - 断网发送后残留一条用户消息与永久闪烁的空回复气泡（发送失败与服务端拒绝会回滚气泡并复原页面上下文与引用文本；落库长时间未回执只提示「发送未确认」并收口空气泡，不删除可能已在执行的消息，服务端一旦有产出或回执迟到该提示会自动撤下，断线期间暂停计时并在重连后按历史对账）
  - 断线后连接指示灯仍显示绿色、输入框未禁用、无断连提示（连接态改以「已鉴权」为准双向同步）
  - 追问回答的数据形状与后端工具期望不一致，导致 LLM 拿不到问题与答案的对应关系；单选题改用单选控件、补「其他（可自行填写）」自定义输入框；提交失败保留卡片可重试，提交成功后禁止重复提交
  - 点击「停止」后按钮不立即恢复；断网点停止会把之后收到的内容全部吞掉（改为发送成功才进入抑制态并乐观切回发送态，失败仅提示）
  - 「LLM 重试中」提示在重试成功后不消失；`llm_stream_reset` 后残留孤儿工具卡
  - 自己发出的消息会在浮窗收起时产生「新消息」红点；微信 / 定时任务等外部渠道进来的用户消息不显示
  - 关闭引用 chip 后消息仍携带被引用文本；复制浮窗内助手回答会误产生引用；引用过一次的文本重新选中后可再次引用（不再永久屏蔽）
  - SPA 路由切换后即使 `context()` 返回值不变也会重新注入新 url；页面上下文与引用文本的总长度统一受 8KB 约束
  - 375px 窄视口浮窗横向溢出被裁切；流式输出时用户上滚回看被反复拽回底部；思考过程改为流式期间默认展开、本轮结束自动折叠
  - 助手回复中的链接改为新窗口打开（`target="_blank" rel="noopener noreferrer"`），点击不再离开宿主页面
  - 支持 Escape 关闭浮窗（不拦截宿主页自己的 Escape 处理）、展开浮窗自动聚焦输入框、消息区补 `aria-live` 播报；断线期间仍可输入（发送时自动重连补发）；WS 鉴权失效横幅在重新鉴权成功后自动消失；「新对话」会退订旧会话；连接类失败改为中文可读文案（原始错误仍通过 `onEvent` 透传给宿主埋点）
  - 会话标题变更（`session_title_updated`）现在会更新浮窗标题
- 宿主 `onEvent` 新增实际派发的 `unread` 事件（此前仅声明未实现）

### 后端

- 修复跨源预检可能拒绝 `Authorization` 头的问题：CORS `allowedHeaders` 由 `'*'` 改为显式列出 `Authorization, Content-Type, Accept, X-Requested-With`（`Authorization` 属 CORS non-wildcard request-header，`*` 对它无效），影响第三方域名下嵌入式 SDK 的 REST 调用
- WS 连接注册表的客户端类型归一化补充 `embed` 分支，与握手处理口径一致（此前 embed 连接在注册表统计里被记成 browser）
- 修复许多模型不支持中途 system message 导致请求 400 的问题：Agent 执行过程中注入的后台任务结果、后台子代理结果、MCP 警告等临时通知由 `role: 'system'` 改为 `role: 'user'`，并在内容外包裹 `<system-notice>` 标签帮助模型区分系统注入与真实用户输入；同步清理 Anthropic 与 Responses 适配器中已无需要的中途 system message 降级逻辑

## 0.0.104 (2026-09-04)

### 前端（桌面 / Web / 安卓）

- 对话页 Agent 思考中时，「思考中...」右侧空白区实时滚动展示最新思考片段（单行横向缓动跟随、两端渐隐），点击仍可展开查看完整思考内容

## 0.0.104 (2026-09-04)

### 后端

- 模型「推理力度」（reasoning effort）配置化：`llm_model` 表新增 `effort` 字段（可选值 `none` / `low` / `medium` / `high` / `xhigh` / `max`，留空使用协议默认值 `high`），创建/更新模型接口支持传入并严格校验；主对话请求的 reasoning effort 改为按模型「API 协议」驱动——OpenAI 兼容与 Responses 协议按模型配置发送（替代此前按模型 ID `gpt-` 前缀硬编码 `high` 的判断），Anthropic 协议不发送；修正 ChatCompletions 序列化格式：`reasoning.effort` 对象改为 OpenAI 官方规格的顶层字符串参数 `reasoning_effort`，Responses 协议仍为 `reasoning: { effort }` 对象；Git 提交信息、会话标题等辅助调用的推理禁用行为不变

### 管理后台

- 模型表单新增「推理力度」下拉框（默认（high）/ None / Low / Medium / High / X-High / Max），仅当模型类型为文本且 API 协议非 Anthropic 时显示

---

## 0.0.103 (2026-09-04)

### 后端

- 文件上传大小上限默认值从 50MB 提升到 1GB（1024MB）；`/upload/config` 接口新增返回 `maxSizeMb` 字段，前端据此动态显示限制而非硬编码；管理后台「单文件上限」配置项补充默认值提示；Nginx `client_max_body_size` 部署模板同步从 `50m` 改为 `1024m`（线上需手动改 `/etc/nginx/conf.d/mao.conf` 后 `nginx -t && systemctl reload nginx`）

### 前端（桌面 / Web / 安卓）

- 输入框粘贴/拖拽文件的大小限制从硬编码 10MB 改为读取管理后台 `file.maxSizeMb` 配置（默认 1GB），超限提示动态显示配置值

### 后端

- 修复管理后台/Skill 页面重新上传技能后，技能内 CLI 脚本丢失可执行权限（Agent 调用报 Permission denied）的问题：上传落盘时按扩展名（`.sh`）与文件头（shebang `#!` / ELF）恢复 0755 执行位

### 终端 CLI（mao-agent）

- 技能包同步（zip 解压）按条目自带的 Unix mode 恢复脚本执行位（原固定 0600，技能 CLI 无法直接执行）

## 0.0.102 (2026-09-04)

### 管理后台

- Agent 编辑支持配置默认模型：管理员可在 Agent 表单中选择启用的文本模型；该 Agent 的会话未手动选模型时优先使用（原为直接回退系统默认模型），留空/未配置仍跟随系统默认

### 后端

- `agent` 表新增 `default_model_id` 列（迁移 V102），创建/更新 Agent API 支持 `defaultModelId`（校验模型存在且启用）；会话执行模型解析链调整为：会话显式指定 → Agent 默认模型 → 全局默认模型，会话列表/详情的模型展示同步该回退链

### Embed SDK

- 修复宿主页面接入后浮动按钮不可见的问题：Shadow DOM 根元素漏挂 `mao-root` 类导致全部主题变量（主色背景、层级、阴影）解析失败，按钮以"透明背景 + 白色图标"渲染形同隐形；同时补上 `theme.primary` 主色配置的实际生效逻辑（此前仅类型声明），版本副本更新为 `mao-chat.v0.1.1.js`

---

## 0.0.101 (2026-09-04)

### Embed SDK（新增）

- 新增 Web Embed SDK：内部 Web 系统引入一个 `<script>` 即可在页面右下角获得 Mao agent 对话浮窗（浮动按钮 + 会话浮窗），支持流式输出、思考过程、工具执行状态、追问作答、手动停止、"新对话"与页面上下文注入（`context()` 变化检测 + 选中文本引用）。产物 `https://mao.etarch.cn/embed/mao-chat.js`（含 `mao-chat.v0.1.0.js` 版本锁定副本），接入示例见 README「Web Embed SDK」章节
- 会话为"每用户每 agent 一个常驻会话"（仅复用 SDK 自建会话），desktop 端会话列表可见并可继续同一会话；同一浏览器多页签通过 BroadcastChannel 协调避免重复建会话

### 前端（桌面 / Web / 安卓）

- 任务阶段枚举 `TaskPhase` 收口至共享契约包 `@mao/contracts`（`ws.ts` 模块），desktop 与 embed SDK 单一来源；纯类型重构，行为不变

### 后端

- WS `auth` 帧的 `client` 字段新增 `embed` 类型（大小写不敏感归一化），仅用于连接注册表与日志识别，无行为差异
- 飞书私聊多会话（任务级会话隔离）：私聊发送 `---`（trim 后为 3 个及以上 `-`/`—` 的宽松变体）即新建会话并回复确认，后续消息在新会话上下文处理，指令消息本身不进入任何会话；回复/引用历史消息（含机器人文本回复、进度卡片、排队卡片、定时任务结果卡片、Agent 发送的图片/文件）自动切换到该消息所在会话并回复确认，未命中映射时静默保持当前会话。同一私聊所有会话共享根工作区（`private-{userId}` 固定目录），新会话以首条消息前 20 字命名；旧会话执行中的任务与排队消息不受切换影响、允许双会话并行；群聊行为完全不变。新增 `feishu_p2p_message` 消息→会话归属映射表（V101），`feishu_chat.session_id` 升级为可变活跃指针；入站归属判定使用 chat 级互斥保证并发消息按到达顺序归属

## 0.0.100 (2026-09-04)

### 前端（桌面 / Web / 安卓）

- 顶部「搜索会话」结果中，已归档会话显示「已归档」标签，点击仍可正常打开查看（此前归档会话完全搜不到）

### 后端

- `GET /v1/sessions/search` 纳入已归档会话：候选 SQL 移除 `status = 'ACTIVE'` 过滤，父会话已归档的边路任务同样可搜到；结果项新增 `status` 字段（`ACTIVE` / `ARCHIVED`），其余行为（按用户消息匹配、最多 20 条、按更新时间倒序）不变

## 0.0.99 (2026-09-04)

### 安卓原生

- 修复弱网（如 5G-A）下 App 冷启动白屏且永不恢复的问题。根因是 WebView 被设置为禁用全部 HTTP 缓存（`LOAD_NO_CACHE`），弱网下每次冷启动都要全量重新下载数 MB 的页面资源，且资源加载失败时仍被判定为「首屏完成」，原生遮罩隐藏后只剩空白页。修复：恢复标准 HTTP 缓存（与手机浏览器一致，弱网下命中已缓存资源即可正常渲染）；首屏完成判定改为轮询验证 Vue 应用真正渲染（`#app` 有内容 / `__MAO_APP_MOUNTED`），8 秒未完成显示「重新连接」入口，重试会绕过缓存重拉入口文档；回前台存活探测从「JS 引擎有无回调」改为「应用是否仍挂载」，页面空白也会自动刷新自愈。需发新 APK 生效
- 手机浏览器打开不白屏、App 白屏的差异即来自上述缓存禁用；Web / 桌面端行为不受影响

### 前端（桌面 / Web / 安卓）

- Vue 应用挂载成功后置 `window.__MAO_APP_MOUNTED = true` 标记，供安卓原生壳存活探测使用（无 UI 变化；未部署该版本时安卓探测自动回退为 DOM 检查）

## 0.0.98 (2026-09-04)
### 后端

- 修复后台子代理执行失败后点击「重试」，主代理通过 `check_subagent` / `wait_subagents` 查询状态始终显示 FAILED、且收不到重试结果的问题：重试现在复用原执行记录并同步其状态（RUNNING → 终态），重试结果在父会话仍活跃时会正确投递给主代理

### 前端（桌面 / Web / 安卓）

- 修复手机 / 平板上在终端里输入 `/` 等符号时，会把之前已输入的内容重复输出一遍的问题：移动端输入法提交字符时 xterm 会把隐藏输入框里累积的历史内容整段当成新输入发给终端（[xterm.js#6078](https://github.com/xtermjs/xterm.js/issues/6078)），现在触屏端会在每次按键后清理该累积值。中文拼音等组词输入不受影响，桌面端行为不变

## 0.0.97 (2026-09-03)

### 后端

- 云端任务支持远程终端：CLOUD 任务可在客户端直接打开一个跑在服务端的交互式 bash，工作目录为任务工作区，环境变量与 Agent 的 Shell 工具一致（虚拟 HOME、`GIT_TOKEN_*`、`GIT_ASKPASS`、`MAO_TOKEN`、`MAO_TASK_NAME`），首次使用会写入一份默认 `.bashrc`
- 新增权限 `terminal:use`（默认仅管理员角色拥有），REST 接口与 WebSocket 接入都会校验；终端归属校验到「当前用户 + 当前任务」
- 新增终端接口：`POST/GET /api/v1/sessions/{id}/terminals`、`DELETE /api/v1/sessions/{id}/terminals/{terminalId}`；新增 WebSocket 通道 `/api/ws/terminal`（首帧鉴权，单连接可复用多个终端）
- 断线不杀终端：网络抖动或切页后重连会自动重新接入原终端，并回放最近输出（默认 256KB 环形缓冲，超出会提示已截断）；输出过快时丢弃部分内容并提示，避免连接被压垮
- 终端自动回收：空闲超时、最长存活时间、每任务与全局数量上限均可在管理后台配置；删除任务会一并关闭其终端。后端重启后已有终端不会恢复
- 终端的创建、关闭、接入、回收写入审计日志
- 新增系统设置项 `terminal.maxSessionsPerTask`（5）、`terminal.maxSessionsGlobal`（50）、`terminal.idleTimeoutMinutes`（120）、`terminal.maxLifetimeHours`（24）、`terminal.outputBufferBytes`（262144），修改后需重启后端生效

### 前端（桌面 / Web / 安卓）

- 终端按钮在云端任务下可用（此前仅桌面客户端的本地任务可用）：Web、安卓与 Electron 均可使用云端终端；无 `terminal:use` 权限或未打开任务时按钮置灰并说明原因
- 终端标签按任务隔离：切换任务只显示该任务的终端，并自动与服务端对齐（已被回收的终端会提示并移除）
- 切换任务或收起终端面板时会解绑云端终端（服务端进程继续运行），切回后自动重新接入并回放输出；同一终端被其他窗口接管时，面板顶部提供「重新接管」按钮
- 终端内搜索：面板内 `Ctrl+F` 打开搜索栏，支持上一个 / 下一个与结果计数，`Esc` 关闭
- 安卓终端新增虚拟按键条（Esc / Tab / Ctrl 粘滞 / 方向键 / Ctrl+C / Ctrl+D / 粘贴），并在软键盘弹出时自动避让

### 管理后台

- 系统设置新增「云端终端」配置分组（5 个运行参数）

---

## 0.0.96 (2026-09-03)

### 后端

- 会话压缩新增「历史消息归档」（仅云端 CLOUD 会话）：压缩成功后，本次被压缩区间的全部原始消息（含工具调用、metadata 等，不含 thinkingContent）按压缩批次写入会话 runtime 目录 `compaction/compaction-NNN.jsonl`（NNN 为压缩序号），内联图片 base64 替换为占位符并保留原图路径；交接消息由系统自动附加归档目录与回查指引，Agent 可用 `read_file` / `grep_search` / `shell` 回读被压缩细节，不再因有损摘要永久丢失信息。LOCAL 会话行为不变，归档随会话删除自动清理

- Shell 工具新增「等待语义」：`wait_for`（正则，命中输出即提前返回）与 `action:'await_async'`（继续等待未结束的命令）。长时构建、`npm run dev` 这类命令不再只能死等 `yield_time_ms` 或丢失输出——命中/超时后立刻拿到已有输出，命令继续在后台跑，后续用 `await_async` + `session_id` 接着读，`output_file` 始终是完整输出
- Shell 会话改为常驻读取输出：提前返回后剩余输出会缓冲在会话中而不再被丢弃；命令未结束时不再按 `keep_session` 回收会话（此前会 SIGKILL 掉进程组导致剩余输出永久丢失）
- `write_stdin` 在有命令正在运行时把输入直接喂给该命令，不再排一条只能在其结束后才执行的新命令
- 同一会话在上一条命令未结束前拒绝新的 `exec`，避免两条命令输出交织
- 后台任务支持主动领取：`await_async` + `task_id` 可主动等待 `async` 提交的任务，超时不消费（仍留给下一轮自动注入）；未结束的后台 shell 结果保留 `session_id` / `matched` / 续等提示
- `researcher` 与 `reviewer` 子代理现可使用 `write_file` 写入研究或审查产物，但仍禁止 `edit_file` 修改既有文件
- Shell 命令长度上限从 10000 字符提升至 20000 字符
- 修复后台子代理偶发在客户端显示为「空闲（IDLE）」的问题
- 修复 Shell 会话并发竞态：`await_async` / `write_stdin` 拿锁后改用实时会话状态，防止并行调用把已消费的结束标记重新登记成假命令导致会话永久卡死；`write_stdin` 在上一条命令已结束但结果未收取时明确报错并指引先 `await_async`，不再把旧输出误当成输入的应答

### 桌面 Electron

- 本地 Shell 与云端对齐：支持 `wait_for` 提前放行、`await_async` 续等（含 `yield_time_ms` / `wait_for`）、输出常驻缓冲、未结束不回收会话
- 本地 Shell 命令长度上限从 10000 字符提升至 20000 字符
- 修复本地 Shell 并发竞态：`write_stdin` 在用户审批通过后重读会话实时状态，命令在审批期间结束时不再把旧输出误当成输入的应答；`await_async` 被并行调用抢先收取结果时返回明确错误而非空结果，会话不再被假命令卡死

### 终端 CLI（mao-agent）

- 本地 Shell 同步上述等待语义；`await_async` 只领取已有输出，不会重复触发命令审批
- 同步修复本地 Shell 并发竞态：`write_stdin` 审批后重读会话状态、`await_async` 并发领取防护，与桌面 Electron 保持一致
- 本地 Shell 命令长度上限从 10000 字符提升至 20000 字符：子代理会话创建时即置为 RUNNING（与执行记录一致，对齐追问路径），消除「会话已创建、执行尚未启动」窗口期内详情接口把空 phase 映射为 IDLE、被前端元数据补拉覆盖且无后续事件修正的竞态；刷新后走子代理列表接口（有执行状态兜底）因此一直显示正常

## 0.0.95 (2026-09-03)

### 后端

- `ask_user_questions` 参数规范化：修复模型偶发把 `questions` 输出成字符串化 JSON（双重编码，含 `\uXXXX` unicode 转义）时，客户端收到空 questions 渲染出空白提问面板的问题。新增归一化层统一「解壳 + 透传」（支持双重/三重编码、参数整体被包成字符串字面量、options/metadata 为字符串化 JSON 等抖动形态），并在结果为空时记录 warn 日志便于排查；工具 schema 补充「严禁序列化为字符串」约束降低抖动概率

## 0.0.94 (2026-09-03)

### 后端

- LOCAL 会话技能同步失败不再统一显示为「超时」：失败原因（客户端未连接 / 客户端回报失败 / 客户端 60 秒内未回报 / 会话被取消）会原样带进会话失败原因与前端 `error` 事件，`error` 事件同时补上 `executionId`（此前缺失，客户端无法把错误归属到当前执行）
- 客户端回报技能同步完成但未带轮次标识 `syncId` 时立即失败并提示升级客户端：此前该信号被静默忽略，本轮同步空等到 60 秒超时，表现为「LOCAL 任务无任何回复、整 60 秒后失败」（旧版桌面端与 mao-agent ≤ 0.1.2 必然命中）

### 终端 CLI（mao-agent）

mao-agent 0.2.0 深度优化：交互层重写、参数解析重写、LOCAL 安全边界加固。此前 0.1.0–0.1.2 的迭代未记入本文件，本条一并覆盖当前行为。

- **修复长回复时整屏重绘 / 历史被清空**：流式输出超过一屏后终端会反复整屏刷新并清掉 scrollback（回滚不到之前的对话）。现在已定稿的内容逐行写入 scrollback 后不再重绘，只有输入框、状态行、未定稿的尾巴属于活动区且总高度恒小于终端高度。窄终端与小窗口（最低 8 行 × 20 列）自动收缩各区高度而不是溢出
- **`/clear` 真正清屏**：连同滚动缓冲一起清空并重建界面，此前只是把旧内容推上去，回滚仍能看到
- **输入框重写为真正的行编辑器**：← → 移动光标、Alt+← / Alt+→ 按词移动、Home/End、Ctrl+A/E 行首尾、Ctrl+W / Alt+Backspace 删词、Ctrl+U 删到行首、Ctrl+K 删到行尾、Ctrl+L 清屏、Ctrl+J 或 Alt+Enter 插入换行；emoji 与 CJK 按整个字符删除（不再删出半个字符）；粘贴多行文本按原样进入草稿（含 bracketed paste，粘贴时不会被误当成回车逐行提交）；草稿超出可见区时按光标位置纵向开窗、超宽时横向裁剪并用 `‹ ›` 标记；空输入框显示占位提示时也保留反色块光标，明确当前可输入位置
- **多行输入**：行尾 `\` 续行、未闭合的 ``` 代码块自动续行，判定逻辑与提示一致
- **输入历史持久化**：`~/.mao/agent-cli/history`（权限 0600，保留最近 200 条，多行原样保存）。↑↓ 翻历史、回到底部还原当前草稿、连续重复输入只记一条；多行草稿内 ↑↓ 先移动光标，到边界才翻历史
- **单独按 Esc 现在生效**：此前需要再敲一个键才会被识别。Esc 分级返回：关面板 → 清草稿 → 取消当前任务
- Agent 提问与 LOCAL 审批弹窗改为 FIFO 排队（同时来多个时显示「还有 N 个待处理」），不再相互覆盖；标注了危险原因的审批必须先选择再按 Enter 确认，不会被一次误按放行
- **斜杠命令**：`/help` 与补全面板同源（不再出现补全有、帮助没有）；新增 `/thinking` 切换思考内容展开；未知命令给出最接近的命令建议；`/queue` 非法参数明确报错；`/cancel` 空闲时提示而不是静默
- **`--thinking` / `--ascii` / `--color` / `--no-color` 真正生效**：`--ascii` 贯通所有边框与符号（直角边框、无 emoji/宽字符），适合不支持 Unicode 的终端与日志采集
- **`--help` 重做**：默认按「通用 / 会话 / 输出与诊断 / LOCAL 模式」分组只列常用项，`--help --all` 输出全部选项、环境变量、退出码表与 permissionLevel 矩阵；LOCAL 专属选项在非 `--local` 下使用会直接报错而不是静默忽略
- **参数解析修正**：缺少取值的选项立即报错（此前会把下一个选项吃掉当值）；支持 `--flag=value`、`-f=v`、`--` 终止符与负数取值；未知选项给出近似建议；选项顺序不再影响结果。修复 `mao-agent --local 你好` 丢 prompt：0.1.x 会把 `你好` 当成 `--local` 的取值，结果既没进 LOCAL 模式、也没发出任何消息
- **`--model` / `/model` 按显示名匹配**：优先匹配管理后台里看到的模型名（自动忽略首尾空格、再退一级大小写不敏感），厂商模型串只作兜底。此前显示名与厂商串并列比较，同一厂商串对应多条配置时会把唯一的显示名误判为「多个模型名为 X」而无法切换；显示名带尾随空格时则报「找不到名为 X 的模型」。确实需要消歧时提示会列出候选 `id=名称`，Tab 补全只给显示名
- **会话忙碌处理**：目标会话正在跑时，REPL 会提示「会话忙，已排队，占用的执行结束后自动重发」并在对方结束后自动重发；`-p` 打印模式按 `--if-running=wait|cancel|fail` 决定等待、抢占还是直接失败
- 管道输入（`echo ... | mao-agent -p`）读到 EOF 立即开始执行，不再固定等 2 秒
- 打印模式与 `login`/`ls`/`status`/`update` 等子命令不再加载 TUI 运行时，启动明显更快
- 项目级 `.mao/agent.json` 现在也能覆盖界面偏好（工具输出展开、回合分隔线、ASCII、排队输入），优先级为 命令行 > 环境变量 > 项目配置 > 用户配置；`MAO_AGENT_VERBOSE=0` 可显式关闭（此前只认开启）
- 排队提示、取消提示等临时信息不再混进对话记录，回滚历史时只剩真实对话
- 修复 `resume` 后首轮回复可能被上一轮残留文本污染、工具卡片在未收到结果时不落地、回合汇总里工具数与耗时不准等问题
- `--help --all` 补全环境变量（`MAO_AGENT_VERBOSE`、`MAO_AGENT_REPO` / `MAO_AGENT_REF`、旧名 token 兼容）、本地配置文件位置与优先级、互斥约束（`--local` 与 `--git-clone` / `--cloud-project`）与 `--permission-level` 默认值说明

LOCAL 模式（`--local`）安全边界加固：

- **文件与搜索新增路径沙箱**：读写编辑、`glob_search` / `grep_search` 的搜索根、`shell` 的 `workdir` 都必须落在已信任的工作区或本会话 runtime 目录内；`../` 越界、绝对路径逃逸、指向外部的符号链接一律拒绝。`read_file` / `glob_search` / `grep_search` 也纳入工作区信任门禁（此前只有写类与 shell 受限），`--yolo` 不能豁免
- **服务端下发的工作区不再能覆盖本地**：只接受等于本地工作区或位于其内部的路径，否则该次工具调用直接失败
- **shell 每个动作都要过一次审批**：复用已有 shell 会话的后续命令与 `write_stdin` 的输入不再沿用首次批准；危险命令黑名单开始检查 `write_stdin` 的内容，且不再因命令过长而只检查前 20000 字符；同时文件内容（`content` / `old_string` / `new_string`）不再参与命令匹配，写一篇提到 `mkfs` 的文档不会被误拦
- **`--approve-rule` 语义收紧**：必须写成 `tool:pattern`（拒绝 `*`、`*:*`、只写工具名、空 pattern，非法规则直接以退出码 4 结束）；通配符不再跨越分号、管道、重定向、`$`、反引号等 shell 元字符，且区分大小写。审批里选「总是允许」只对完全相同的工具 + 命令生效，不再放行整类工具
- **审批可交互性按 stdin 与 stdout 共同判定**：stdin 是管道或已关闭时不再把管道内容当作回答，而是按 `--on-approval=fail` 拒绝并以退出码 4 结束（此前可能挂到服务端超时）；工作区信任确认同样如此
- **工具结果不再无条件明文落盘**：仅在超过传输上限（900KB）时写入 `~/.mao/agent-cli/runtime/<会话>/shellOutput/`（0600）。此前每个结果都会留一份，包含读过的 `.env`、私钥内容
- **超限裁剪改为整棵结果树共享字节预算**：图片 `data_uri`、搜索 `matches` 数组、MCP 嵌套结果都会被按需截断，同时保留 `exit_code` / `session_id` / `async` 等控制字段（此前超限会整体丢弃 payload，后台任务信息随之丢失）
- **文件工具与云端对齐**：行数统计与 offset/limit 口径统一（CRLF 文件不再多出空行）、二进制与超 8MB 文本明确拒绝、写回保留原文件 BOM 与 CRLF、写入/编辑返回 diff 数据（LOCAL 模式下前端也能看到 diff 视图）、`edit_file` 在 `old_string === new_string` 或缺少 `new_string` 时报错而不是静默删除
- **搜索行为统一**：没有 `rg` 时的 Node 回退也支持 `context_lines`，输出结构与 rg 一致；`rg` 报错现在报「搜索失败」而不是「没找到」；非法正则明确报错
- **带技能的 LOCAL 会话不再必然失败**：技能同步完成信号补齐 `syncId`（此前后端校验不通过，等到 60s 超时把会话判为失败）
- **技能包同步加固**：改用内置解压（不再隐含依赖 python3），拒绝含 `../`、绝对路径或符号链接的条目，且只允许从当前服务端同源地址下载（跨源 `syncUrl` 会被拒绝，避免登录凭据发往其他主机）
- **MCP 加固**：本地 stdio 服务器启动前需要审批；子进程只获得白名单环境变量（不再继承登录 token）；传输改为标准的行分隔 JSON；工具报错会真实报错而不是当作正常内容喂给模型；超时或子进程退出会杀掉整个进程组并附带 stderr 末尾内容；多个服务器改为并发连接
- **LOCAL shell 从登录 shell 解析 PATH**（cron 或其他 Agent 拉起时也能找到 nvm / brew 安装的工具）；有意不向命令注入 `MAO_TOKEN`，在 shell 工具里用 `mao` 需自行 `mao login`。shell 固定通过 bash 执行，缺少 bash 会明确报错（此前探测结果与实际执行体不一致，表现为莫名失败）
- 工作区信任判定改为解析真实路径后比较：信任目录内指向外部的符号链接不再被视为已信任；`~/.mao/agent-cli/config.json` 改为原子写，多个 mao-agent 同时运行不会互相覆盖信任列表
- 工具参数不是合法 JSON 对象时直接拒绝执行（此前会当作空参数继续跑，可能写空文件或执行空命令）
- 工作区 `AGENTS.md` 注入系统提示时上限 100KB（与云端一致），超出部分截断
- 读取图片仍限 5MB，终端版不做缩放，超限提示改为明确要求先本地压缩或裁剪
- 一键安装脚本（`scripts/install-mao-agent.sh`）健壮性修复：`rm -rf` 清理旧目录时其 stdout 可能被外部 `rm` 包装（如 macOS trash 类工具）泄漏污染，导致命令替换捕获到杂音、误报「未找到 …/agent-cli/package.json」安装失败（克隆与检出本身始终成功）。现将该命令 stdout 丢弃，返回值捕获路径不再受外部环境影响

- 测试与 CI：新增 keydecode / 行编辑器 / 输入控制器 / 布局预算 / 弹窗 / TUI 渲染 / 斜杠命令 / 配置优先级 / token 刷新 / REST 客户端 / 剪贴板 / text 渲染器，以及 LOCAL 沙箱 / 审批规则 / 文件工具 / 搜索 / MCP / zip 解压 / 结果裁剪 / 模型名解析等单测（合计 473 例），CI 增加 Node 20 与 22 双版本矩阵与 `npm run typecheck`（此前 `test/` 不参与类型检查）

### 桌面 Electron

- 修复 LOCAL 模式带技能的会话在同步后仍被判失败：技能同步完成信号补齐服务端要求的轮次标识 `syncId`（此前服务端忽略该信号，等到 60 秒超时把会话标记为失败）

---

## 0.0.93 (2026-09-02)

### 后端

- 飞书通道表情包（sticker）消息支持度增强：入站占位符由 `[表情包]` 升级为 `[表情包 msg=om_xxx]`（私聊/群聊/引用消息一致），Agent 可感知消息身份并按需处理；归一化事件提取表情包 `file_key` 落群消息日志，`feishu_download_file` 对表情包返回明确错误提示（飞书开放平台暂不支持表情包资源下载，resources 接口实测返回空占位/500），避免 Agent 盲目重试或拿到无效文件

---

## 0.0.92 (2026-09-02)

### 后端

- 修复飞书群聊 @机器人 误触发：群里 @任意其他机器人（如 XXLJOB）也会唤起本机器人任务。现以 bot open_id 精确比对事件 mention 身份（线上实测 @机器人 的 mention id 即该机器人应用的 open_id），仅 @当前绑定的机器人才触发任务；bot open_id 未就绪时保留兜底命中，不影响原有 @机器人 漏判修复
- `/v1/admin/analytics/summary` 统计口径由「全表累计」改为「统计窗口内新增」：窗口以今日结尾、按半开区间 `[start 00:00:00, end+1 00:00:00)` 取数（避免 23:59:59 边界丢数），`days` 仍限制 1–90
- 新增环比与周期字段：`period`（窗口与上一等长窗口的起止日期）、`periodTotals`（会话/消息/对话 Token/后台 Token/Token 合计/后台调用数/活跃用户/完成与失败会话）、`previousTotals`（上一周期同口径，用于环比）
- `trends` 每日新增 `chatTokens`（消息 token）/ `backgroundTokens`（后台 LLM 调用 token）/ `totalTokens` / `backgroundCalls`，缺失日期补零；`phaseDistribution` 改为窗口内创建会话的阶段分布，`overview` 中运行中/待审批/失败/已取消仍为实时快照
- `modelStats` 拆分对话与后台 Token，并剔除窗口内完全未被调用的模型（按 Token 合计降序）；`agentStats` 增加 `totalTokens`；`userActivity` 剔除窗口内无任何会话与消息的用户
- summary 内十余条聚合查询改为并行执行，接口耗时约减半（真实库 90 天窗口从约 2 秒降至约 1 秒）

### 管理后台

- 用量分析页从纯表格重构为图表看板：4 张带环比与迷你走势的 KPI 卡（新增会话 / 消息数 / Token 消耗 / 会话失败率）、会话与消息双轴趋势折线、Token 消耗堆叠柱（对话 + 后台）、会话结局与模型 Token 占比环形图（Top 10 + 其他）、Agent Token 与用户活跃横向排行、模型用量明细（新增占比条，不再列出窗口内零调用的模型）
- 支持 7 / 30 / 90 天周期切换，页头显示实际统计区间与环比对照区间；90 天视图默认聚焦最近 30 天并可拖动查看全周期；KPI 卡可直接跳转会话列表与运行监控
- 引入 echarts 6.1.0（按需注册图表与组件）并新增通用 `BaseChart` 组件（自适应容器尺寸、keep-alive 切回重绘、空数据占位）
- 会话阶段 `RESUMING` 补上中文标签「恢复中」（会话列表 / 运行监控 / 用量分析共用），此前直接显示英文枚举
- 数据概览「Agent 使用排行」标注「近 7 天」，与后端窗口口径保持一致

## 0.0.91 (2026-09-02)

### 后端

- 彻底移除子代理执行超时逻辑：`delegate` 工具早已下线，所有子代理执行与崩溃恢复均无总时长限制，仅存的「恢复旧遗留前台委派执行按超时截断」分支一并删除（`executeVisibleWithTimeout`、按开始时间计时等）
- 废弃 `harness.delegate.timeoutSeconds` / `cancelGraceSeconds` 配置（V099 删行），崩溃恢复统一走无超时执行，LLM 调用与工具执行自身超时仍兜底

### 管理后台

- 集成配置移除「子代理执行」卡片（0.0.89 引入的两项配置不再需要）

## 0.0.90 (2026-09-02)

### 后端

- 废弃死配置 `ui.defaultPageSize`（V098 删除该行）：它无任何消费点——admin 列表页分页大小均为前端各页硬编码，后端列表接口也未读取，改了不会生效；「界面」设置分类随之移除

### 管理后台

- 系统设置页移除「界面」分类卡片

## 0.0.89 (2026-09-02)

### 后端

- Harness 调参后台化：上下文压缩（`harness.compaction.*` 5 项）、LLM 超时与限流重试（`harness.llm.*` 6 项）、网页抓取（`harness.webPage.*` 5 项）、Shell 会话（`harness.shell.*` 3 项）、子代理执行超时（`harness.delegate.timeoutSeconds/cancelGraceSeconds`）从 `application.yml` 迁入 system_setting（V097），管理后台可视化编辑；均为启动时构建，保存后需重启后端生效
- 修复死配置：yml 中 `app.harness.delegate` 此前无消费点（`SubagentExecutionRecoveryService` 内硬编码 3600/30），本次接线后后台修改可生效；触发比例（0~1 小数）与布尔项新增入库校验

### 管理后台

- 「集成配置」新增「上下文压缩」「LLM 超时与重试」「网页抓取」「Shell 会话」「子代理执行」五张卡片与目录锚点；顶部提示改为区分即时生效与重启生效配置

## 0.0.88 (2026-09-02)

### 后端

- Agent 运行参数与任务通知调度参数后台化：线程池三元组（`agent.threadPoolSize/Max/Queue`）、WS 空闲超时（`ws.idleTimeoutMs`）、任务通知轮询间隔/批量/重试次数（`notify.workerDelayMs/batchSize/maxAttempts`）迁入 system_setting（V095），升级时由环境变量自动导入，此后以管理后台为准
- 任务通知调度器改为每轮动态读取配置：轮询间隔、批量条数、最大重试次数保存后即时生效，无需重启
- Agent 线程池与 WS 空闲超时在启动时从 DB 构建，后台修改后需重启后端生效（UI 已标注）
- 废弃无消费点的死配置 `MAX_CONCURRENT_AGENTS`、`DEFAULT_MAX_ROUNDS`、`DEFAULT_CONTEXT_ROUNDS`（yml/env 不再读取）

### 管理后台

- 「集成配置」Tab 新增「Agent 运行」「任务通知」两张卡片（数值输入 + 范围校验），目录锚点同步
- 系统设置页普通分类（代码/会话/审计/微信/界面/运行环境）从「行展示 + 弹窗单条编辑」改为与集成配置一致的卡片表单：内联编辑、按分类卡片批量保存（batch 接口）、数值正整数校验、只读项（workspace.root/skills.dir）纯展示，移除旧编辑弹窗
- 模型编辑/复制弹窗回填 API Key（model:write 权限下后端返回明文，可查看、修改与复制带出）；掩码串仍不回填且提交时拦截

## 0.0.87 (2026-09-02)

### 后端

- `.env.example` 精简：移除已迁移至管理后台的集成配置环境变量（LDAP_*、FEISHU_ENABLED/APP_ID/APP_SECRET/REDIRECT_URI、UPLOAD_STORAGE_MODE/UPLOAD_BASE_URL、TAVILY_API_KEY、OSS_* 含 OSS_STS_*），补充 `SETTINGS_SECRET` 说明与旧版升级自动导入指引；本地磁盘路径与运行参数类变量不变



### 后端

- 修复定时任务触发后 Agent 重复创建相同任务而非执行任务本体的问题：触发投递的 prompt 现在会包装触发上下文（含"直接执行任务内容，不要创建/修改/删除定时任务"的系统提示与当前时间）
- 修复一次性定时任务执行后长期停留在 ACTIVE 的问题：新增 `once` 字段（V096 迁移，含存量自动完结修复），固定"月+日"的 cron 自动识别为一次性任务，执行一次后自动置为已完结并清空下次触发时间
- create_scheduled_task 工具：新增 `once` 参数与一次性任务规则说明；prompt 参数明确为"任务本体"，不得包含执行频率措辞、不得要求再次创建定时任务、不得引用会话临时路径；补充好/差示例
- 修正工具描述中"专属 Session"的失实说法（实际在创建时的会话中累积执行历史）

### 管理后台

- 定时任务列表：Cron 列为一次性任务显示"一次"标签

## 0.0.85 (2026-09-02)

### 终端 CLI（mao-cli）

- settings 新增 `batch`（批量保存）与 `test ldap|feishu|oss`（集成配置测试连接，留空参数回落已存配置），对齐 0.0.82 集成配置后台化的全部端点
- scheduled-task 新增 `list-all`（跨用户全量列表，需 `session:read` 权限）
- session 新增 `promote-side-task`（边路任务会话提升为主会话）
- 新增 `system-command` 管理端模块：系统指令 CRUD（需管理员权限），并同步知识库文档

## 0.0.84 (2026-09-02)

### 后端

- 修复任务通知 Webhook 投递调度器在 MySQL 短暂不可用时未捕获数据库异常、导致整个后端进程退出的问题

### 前端（桌面 / Web / 安卓）

- 修复 WebSocket 连接 Promise 残留：登出/强制下线/断线取消后再次发送消息不再因未 settle 的连接等待而永久挂起，并清理跨账号残留的回调与执行状态，避免换号后串用户
- 修复 Markdown 代码块复制按钮安全性问题：移除内联 onclick（防 XSS 注入），改为全局事件委托复制，交互不变
- 修复删除会话后顶部标签页仍残留该会话标签的问题
- 微信扫码登录轮询增加总超时（5 分钟）与连续失败上限，超限时明确提示二维码过期，不再无限轮询
- 修复快捷指令面板自动弹出时按 Enter 被面板劫持、消息发不出去的问题（用户主动浏览候选列表时 Enter 仍用于确认）
- 输入 URL / 文件路径 / 邮箱等含 `.` `:` `/` 的词不再误触发快捷指令自动补全
- 修复 401 刷新令牌期间晚到的 401 请求被永久挂起、不进入重试队列的问题
- 修复编辑消息重发在前置校验失败时输入框内容被乐观截断且未回滚的问题
- MCP 服务器编辑弹窗重新打开时不再残留上一次的表单校验错误
- 技能上传增加校验：最多 500 个文件、单文件 ≤ 20MB、总量 ≤ 50MB，跳过 node_modules/.git/dist 等目录并明确提示
- 修复打开指令抽屉新增指令时静默覆盖正在编辑内容的问题，改为提示先完成当前编辑
- 个人资料邮箱增加前端格式校验（失焦提示，与后端规则一致）
- 消息编辑框在输入法组词期间按 Enter/Escape 不再误触确认或取消
- 会话/任务区域滚动或窗口尺寸变化时自动关闭右键菜单
- 修复文件 diff 视图快速切换文件时旧内容覆盖新文件的乱序问题
- PDF 预览页数尺寸预取改为分批并发，大文档打开更快

### 管理后台

- Markdown 代码块复制按钮移除内联 onclick，改为事件委托（与桌面端一致，防 XSS 注入）
- 模型编辑弹窗不再回填 API Key 掩码值，留空表示不修改；误提交掩码值会被拦截提示

## 0.0.83 (2026-09-01)

### 后端

- 全网搜索（web_search 工具）支持 Tavily / TinyFish 双实现后台切换：新增 `tools.webSearchProvider` 开关（tavily 默认 / tinyfish）与 `tools.tinyfishApiKey` 密钥（AES 加密入库），选择后即时生效无需重启，默认 Tavily 保持向后兼容
- 新增 TinyFish 搜索引擎适配：走 `X-API-Key` 请求头 + GET 请求（与 Tavily 的 body 传参不同），返回 `snippet` 映射为结果 `content`，因 TinyFish 无服务端结果条数参数改为客户端裁剪 `max_results`

### 管理后台

- 集成配置 → 网络工具 新增「搜索实现」下拉（Tavily / TinyFish）与「TinyFish API Key」输入框，便于切换底层搜索引擎

## 0.0.82 (2026-09-01)

### 后端

- WS 流式接口改为首帧鉴权：连接建立后必须先发送 `auth` 帧携带令牌完成认证，未认证连接发送任何其他消息将被立即断开；URL 不再携带明文 token，避免令牌经访问日志、代理日志泄漏

### 前端（桌面 / Web / 安卓）

- 聊天发送、停止、重试全部走可靠 WebSocket 发送：网络断开时发送自动入队重连后补发，停止/重试失败会明确提示「网络连接不可用，请重试」，不再静默丢失操作
- 会话加载全面加竞态防护：快速切换会话时旧请求结果不再覆盖新会话数据，加载失败提示更明确
- 修复发送失败的"空气泡"残留：发送出错时自动移除空的助手占位消息
- 附件上传体验修复：预览 URL 生命周期统一管理（不再泄漏）、粘贴超限附件提示已忽略部分、上限文案统一为「最多上传 10 个附件」、队列面板防重复提交
- 工具调用输出超长时截断展示，新增「展开完整输出」按钮，复制始终复制完整结果
- 触屏适配：边路任务支持长按唤出操作菜单；终端面板拖拽条支持触摸调整高度并扩大热区；终端入口在非桌面客户端提示不可用
- 设置与认证健壮性：飞书扫码/登录轮询失败容错与清理、微信扫码确认失败保持轮询不再误报成功、弹窗关闭统一清理轮询定时器
- 界面细节：技能/指令/文件标签与状态点颜色统一走设计令牌（暗色模式同步修复）、窗口标题栏 Mac 平台缩进修正、深浅主题下多处硬编码颜色修复
- 修复文件浏览器刷新竞态与展开状态残留、文件树搜索防抖（短关键词/清空不触发）、任务列表刷新失败提示、重命名失败提示、窄面板删除边路任务入口缺失
- 任务面板偏好保存失败提示（原静默失败）、启动时多组件同时加载偏好去重；删除任务失败时明确提示并保留当前页面（不再误跳转）
- 版本检查监听器在安卓上正确注销；外部链接/飞书授权窗口以 `noopener` 打开
- 断线重连期间发送/停止/重试最多等待 15 秒即提示失败，不再长时间无响应；重连握手超时同样兜底

### 终端 CLI（mao-agent）

- WS 连接适配首帧鉴权：token 改为认证帧发送，URL 不再携带 token

### 管理后台

- 修复非管理员角色访问「系统指令 / 用量分析 / 运行监控」整页 403：这三个页面改为管理员专属门禁，与后端校验一致（菜单同步隐藏）
- 系统设置页按 `settings:write` 权限禁用全部写控件（含集成配置的测试/保存按钮），只读账号不再可点
- 修复集成配置「清空密钥」跨分组误清除、保存失败仍关闭编辑弹窗的问题；集成配置 URL 字段（LDAP 地址 / 飞书回调 / 上传地址）与模型 API 地址增加格式校验
- 运行监控首进页面不再重复请求两次；审计日志详情加载失败不再整页报错；分析页与 Agent 列表加载增加竞态防护；关键词/对象筛选清空后自动刷新
- 图表卡片支持键盘操作（Enter/空格）；若干下拉数据加载失败不再中断弹窗

## 0.0.81 (2026-09-01)

### 后端

- 追问/纠偏子代理时自动跟随主代理当前模型：子代理复用其既有上下文，但改用主代理正在用的模型；此前追问会一直沿用子代理创建时绑定的旧模型

## 0.0.80 (2026-09-01)

### 后端

- 提高飞书引用消息的内联注入长度上限：超过 100 字仅保留前 100 字，改为超过 500 字仅保留前 500 字（超出部分仍全文落盘至会话工作区 `quoted/`，落盘失败降级为 1500 字截断），减少长引用被截断时丢失的上下文
- 修复微信消息处理的会话级锁 Map 清理条件永假问题：锁条目按会话永久泄漏（与定时任务侧同型问题，实现已对齐）
- 修复微信连续发送消息时被新消息取代的旧消息残留问题：旧消息不再以「无回复的用户消息」形式留在会话历史中污染后续 AI 上下文
- 修复会话「重试」在数据库异常时会话被永久判定为运行中的问题：执行提交前的任何异常现在都会释放运行占位并向客户端回发错误
- 修复任务终态通知的重复投递竞态：WebSocket 已送达的通知在结果确认晚于投递窗口时，不再重复发送 Webhook（发送前二次确认 + 晚到结果直接抑制）
- 修复 grep_search 在无 rg 环境的回退分支行为偏差：`**/*.md` 类递归模式现在也能命中根目录文件，上下文输出结构与 rg 环境统一，并增加超大文件读取防护

## 0.0.79 (2026-09-01)

### 后端

- 修复定时任务触发时用扫描时刻的旧快照整行回写数据库的问题：执行前后可能覆盖用户在扫描与执行窗口内对任务名称/提示词/cron/状态的修改，现改为仅增量写入下次触发时间与执行结果字段
- 修复管理后台编辑用户时角色权限校验滞后的问题：校验失败（如试图变更最后一名管理员的角色）时用户的昵称/邮箱/状态修改已落库且无法回滚，现校验前置于持久化
- 修复工作区文件预览内存风险：文本预览原先将整个文件读入内存后才截断返回内容，超过 10MB 的文件现直接提示过大并引导下载，与图片/PDF 预览的防护对齐
- 修复任务通知 Webhook 投递的两大健壮性缺陷：发送请求增加 10 秒显式超时（原先仅依赖底层库默认空闲超时）；SENDING 卡死行的恢复逻辑由"进程启动后仅执行一次"改为每分钟周期执行，Webhook 对端异常时通知不再丢失至重启
- 修复定时任务会话级锁的清理条件永假问题：锁 Map 条目按会话缓慢泄漏（与 Git 操作锁的正确实现保持一致）
- 修复会话消息队列排序非原子的问题：相邻消息上移/下移改为事务 + 行锁执行，并发操作不再产生重复排序值或丢失交换；死锁/锁超时自动重试，失败也推送队列刷新保证前端状态收敛

## 0.0.78 (2026-08-31)

### 后端

- LLM API 请求异常时新增结构化错误日志：在抛错/回调 onError 前记录一条 `LLM request failed` 日志，包含模型、baseUrl、错误详情与完整请求体（payload 中 base64 图片数据自动脱敏为 `[REDACTED]`），便于排查上游报错原因

## 0.0.77 (2026-08-31)

### 后端

- 修复管理后台「OSS/STS 试签」报 `InvalidParameter.PolicyGrammar` 的问题：试签请求携带了空对象 `{}` 作为 Policy，不符合 RAM Policy 语法（缺 `Version`/`Statement`），现试签不再携带 Policy，仅校验凭证与角色有效性
- 修复飞书通道发送「图片+文字」时 Agent 收不到图片的问题：该类消息以富文本（post）到达，链路此前仅处理独立图片消息，图片 key 被丢弃。现从 post 内容递归提取全部内嵌图片（私聊下载为图片消息直传模型；群聊预下载落盘并在日志行追加引用），富文本占位文本同时保留 `[图片]` 标注
- 飞书入站图片/文件改存到会话工作区 `chat-files/{yyyy-MM-dd}/` 按日期归档，不再散落工作区根目录（含入站预下载、`feishu_download_file` 懒加载两条链路）
- 飞书私聊/群聊触发的图片在 data URI 直传模型的基础上同时落盘到 `chat-files/{yyyy-MM-dd}/`，消息文本追加「图片已保存到会话工作区：{路径}」提示，Agent 可用工具二次读取原图（同微信图片处理逻辑）；日期归档时区与微信对齐为 Asia/Shanghai

### 前端（桌面 / Web / 安卓）

- 修复会话输入框草稿在发送一条消息后失效的问题：发送成功清空草稿时误将草稿键标记为「已删除」，导致该会话之后重新输入的内容在切换会话后无法保存恢复，现仅删除条目不再标记，删除会话/边路任务等场景才标记防复活
- 修复新建子代理任务后跳转子代理 Tab 用户消息渲染两次的问题：历史回填合并时未把 `subagent-user-` 前缀的乐观占位消息识别为已被真实历史替换，占位消息残留导致重复
- 修复子代理 Tab 中乐观占位消息时间显示与真实消息相差 8 小时的问题：占位时间误用 UTC 的 `toISOString` 拼接，现改用本地时间

### 管理后台

- 系统设置页布局重构：取消 Tab 切换，全部分组（集成配置 + 普通分类）纵向平铺展示，集成配置分组卡片不再左右两列排布；新增左侧固定目录索引，点击平滑滚动到对应分组，滚动时自动高亮当前分组，移动端目录折叠为顶部横向

## 0.0.76 (2026-08-30)

### 前端（桌面 / Web / 安卓）

- 安全修复：Markdown 渲染链路接入 DOMPurify 统一消毒，链接 href 协议白名单（仅 http/https）、链接文本与原始 HTML 一律转义，封堵 assistant 消息经 prompt 注入执行脚本的 XSS 注入点
- 修复 Electron（file:// hash 路由）下设置页「返回工作台」整页跳转指向错误地址导致白屏的问题，统一改用 vue-router 跳转
- 修复断线场景下主发送链路静默丢消息：发消息/编辑重发/入队/创建边路任务等关键 WS 消息改用可靠发送并传导失败结果，失败时保留输入、回滚乐观气泡并提示，队列操作失败亦有提示
- 修复 401 刷新排队请求在 refresh 失败时永久挂起（现统一 reject）、登录失败无任何错误提示且回车可重复提交、提问面板与审批卡片可重复提交、边路任务删除失败仍本地删除、边路首发在父会话校验前插入乐观消息不回滚等交互问题
- 修复 FileViewer 文本读取无过期响应保护、MarkdownContent 流式渲染竞态内容回退、边路任务已读 watch 随宿主组件卸载永久失效、会话删除/登出后运行态缓存清理不全等缺陷
- 其他：Web/安卓端 Markdown 外链点击改走系统浏览器兜底；流式增量不再每字符复制消息数组；Ctrl+` 终端快捷键改为常驻注册；自动补全仅按指令名称前缀触发并支持 Esc 记忆关闭；暗色主题 hover 反馈、复制消息剥离内部标记、粘贴图片超限提示、WebGL 上下文丢失回落等细节修复
- 【逻辑 BUG 扫描】编辑重发改为连接成功后再乐观截断，失败时按快照整体回滚消息列表，不再永久丢失被编辑消息之后的会话内容；WS 首次连接失败后自动进入重连循环（未登录时放弃），后端恢复后服务端推送不再静默丢失；会话删除/登出补齐 11 个运行态 Map 清理并复用统一清理函数

### 管理后台

- 修复失效 token + 网络异常时登录页与工作台互相重定向死循环，并新增 404 兜底路由；定时任务管理页对非本人任务禁用启停/删除（后端仅允许归属人操作），避免必失败的入口
- 修复模型/用户/Agent 列表只读权限用户仍可见全部写操作按钮的问题，按权限显隐；删除末页最后一条后自动回退页码；列表请求增加竞态防护；MCP 关键词输入 300ms 防抖；角色权限页切换角色前对未保存修改二次确认；集成配置数值项增加范围校验
- 其他：登录回车双提交、会话详情重复请求、请求失败误报「页面发生异常」、keep-alive 缓存无界增长、统计卡片平板断点、主题变量硬编码、状态筛选不触发查询、标签/卡片键盘可达性等修复

### 后端

- 安全修复：技能 frontmatter `name` 增加 slug 校验，技能同步写入/删除前断言路径不逃逸工作区（防止路径穿越）；纯文本内容恰好为 JSON 数组时不再被误判为多模态消息；部署窗口内崩溃恢复不再绕过 defer 守卫执行子代理协调器
- WS 执行流：会话「停止」请求在执行提交前到达时记录待取消标记，注册时立即消费并落 CANCELLED 终态；自动消费出队后落库失败回补队首；消息提交异常路径释放执行占位；技能同步完成事件带 syncId 轮次校验，避免跨轮误配
- 调度任务执行收尾改为增量更新执行结果字段，不再整行回写覆盖用户对任务的修改；任务触发忙碌时入队失败不再将同会话正在运行的执行误标为 FAILED（仅真正进入执行阶段才落终态）；飞书排队卡片「立即插队/中断」状态机收敛（先中断后排空再接力，空闲时中断也触发排空兜底）；飞书入站队列 CAS 竞争时循环重取下一条，溢出摘要与实际保留消息口径一致
- 子代理 delegate 异常路径落 FAILED 终态并清理注册状态；后台子代理纠偏按数据库最新状态判定改写（已终态不再覆盖）；generate_image 补请求超时销毁
- LLM：SSE 行解析兼容 `data:` 无空格变体；非流式 200 响应内嵌 error 直接上抛；用量缺 total_tokens 时按 prompt+completion 兜底
- 微信扫码登录：回调 baseUrl 仅允许 https 且拒绝私网/环回/链路本地地址，botToken 非空校验；二维码会话 TTL 5 分钟惰性淘汰，非超时错误不再静默吞掉
- 工具类：glob `**/` 支持零段匹配且转义不再二次污染正则；文件写入 bytes_written 按 UTF-8 字节数统计；差异展示尾部上下文保留完整；exec 超时结果为「未完成/超时」；prompt 模板替换基于副本不再污染上下文；任务 update 状态白名单校验、重置 in_progress 前先探测目标存在

### 终端 CLI（mao-agent）

- REPL 问答题输入仅当整段为合法序号时走序号分支，自定义文本不再被数字前缀误判
- HTTP MCP（streamable）初始化后回传 `Mcp-Session-Id` 头并统一 Accept 头，会话状态不再丢失

## 0.0.75 (2026-08-30)

### 后端

- 集成配置后台化：LDAP 认证、飞书 OAuth 登录、上传配置（存储模式/访问地址/单文件上限）、Tavily API Key、OSS/STS 对象存储共 6 组配置从 `application.yml` + 环境变量迁入数据库，管理后台「系统设置 → 集成配置」分区可视化编辑，保存后即时生效、无需重启。secret 类配置（LDAP 密码、飞书 App Secret、Tavily Key、两组 OSS 密钥）以 AES-GCM 加密入库（新增 `SETTINGS_SECRET` 环境变量作主密钥），回显掩码、留空表示不修改；升级首次启动自动将仍在的环境变量值导入数据库（此后本批配置以数据库为准，对应环境变量可删除，`SETTINGS_SECRET` 必须保留）。新增 `POST /v1/system-settings/batch` 批量保存与 LDAP/飞书/OSS 三个测试连接接口；飞书 OAuth 端点固化为官方地址常量。系统设置接口新增 `settings:read`/`settings:write` 权限码（ADMIN 自动拥有），修复此前任何登录用户可读写全部设置的越权问题。飞书 bot 与微信 bot 配置不在本次范围，仍走原有配置

### 管理后台

- 系统设置页新增「集成配置」分区：LDAP/飞书登录/上传/OSS/网络工具按卡片分组表单化编辑，secret 掩码输入、开关/枚举控件、LDAP/飞书/OSS 一键「测试连接」；系统设置接口补充权限控制（无 `settings:write` 权限不可修改）

## 0.0.74 (2026-08-29)

### 管理后台

- 模型编辑/添加弹窗优化：「API 协议」选项移至「API 地址」上方并移除其下方冗余提示文案；「API 地址」输入框后新增只读后缀框，按所选协议实时展示实际调用路径（`/chat/completions`、`/messages`、`/responses`），直观呈现完整请求 URL

## 0.0.73 (2026-08-29)

### 后端

- 修复 OpenAI Responses 协议模型（`openai-responses`）前缀缓存命中率随机的问题：网关按 `prompt_cache_key` 做上游粘性路由，此前 Mao 请求未携带该字段，多上游负载均衡下缓存命中全凭概率（实测无 key 连发 4 次仅 1 次命中）。现按会话注入固定缓存键（`mao-session-{sessionId}`），会话内多轮请求稳定路由到同一上游，前缀缓存可稳定命中；仅 Responses 请求体新增该字段，其他协议行为不变

## 0.0.72 (2026-08-29)

### 后端

- 任务异常失败（FAILED，含崩溃恢复续跑后仍失败）后，会话队列不再自动消费下一条消息：上一条任务未执行完成时，排队消息保留等待用户手动处理（手动发送 / 立即发送 / 重试成功结束后自动恢复消费）。用户主动取消（CANCELLED）与成功完成（COMPLETED）后行为不变。桌面 / Web / 飞书队列统一生效，前端无感知改动

## 0.0.71 (2026-08-29)

### 后端

- 支持接入 OpenAI Responses API 协议模型：模型「API 协议」新增 `openai-responses` 选项（此前预留），新增 `ResponsesLlmAdapter`（`{baseUrl}/responses`，Bearer 认证）实现非流式与流式调用、工具调用多轮循环、限流重试/退避、取消与用量归一（含 cached tokens）；Responses 专属语义：`store:false` 无状态多轮、历史 assistant 回传 `function_call` 项与 `function_call_output` 项、推理思维链经 `reasoning.encrypted_content` 往返（流式与非流式均捕获，挂靠持久化 tool_calls，崩溃恢复可用）、并行多工具调用聚合、思考截断整轮重试、截断轮（incomplete）用量照常统计；连通性测试同步支持该协议（`ResponsesChatClient`），mid-system-message 探测自动跳过；多工具轮的 `reasoning` 引用仅写入 Responses 模型的会话数据，其他协议模型行为不变
- 修复飞书通道会话的定时任务执行完成后结果只留在会话内、不回流飞书的问题：现与微信通道一致，任务完成后将最终回复以「处理完成」卡片推送回原飞书会话（群聊/私聊均支持，非飞书会话不受影响）

### 管理后台

- 模型表单「API 协议」下拉启用「OpenAI（Responses）」选项（移除规划中置灰）

## 0.0.70 (2026-08-28)

### 前端（桌面 / Web / 安卓）

- 会话被「停止/取消」后支持一键继续：取消终态下，输入框为空时发送按钮变为绿色「继续」按钮（续跑语义与失败重试一致，基于已有上下文继续执行）；输入框有内容时仍为发送按钮，正常发送新消息。边路任务面板同步支持

## 0.0.69 (2026-08-28)

### 后端

- 修复 `write_file` 返回的 `total_lines` 在内容以换行符结尾时多算一行，与 `read_file` 统计口径不一致的问题（现统一按「末尾换行不产生空行」计算）
- 修复 `task_update` 传入缺失/非数字的 `id` 时静默失败：此前 `NaN` 会导致「重置其他 in_progress 任务」的 SQL 不生效且更新无效果，现直接返回无效 ID 错误提示
- 修复同一轮返回多个工具调用时，「停止/取消」请求在工具开始执行前不生效的问题（现与单工具路径一致，执行前即检查取消标记）
- 修复定时任务因会话忙碌而仅入队（QUEUED）时仍累计「已执行次数」并刷新「最后执行时间」的统计失真问题，现只有真正执行（成功或失败）才计数

### 前端（桌面 / Web / 安卓）

- 修复消息结束事件到达后若终端状态事件丢失，会话的「流式输出中/思考中」指示器可能一直残留的问题，现消息结束时会同步清理这两个状态

## 0.0.68 (2026-08-28)

### 前端（桌面 / Web / 安卓）

- 修复工具调用偶现「已完成却一直转圈」：任务被中止/失败/重试时，服务端不会为被中止轮次的工具补发结果事件，前端终态处理未就地终结这些工具，转圈状态残留且后续轮次继续追加到同一气泡中，只能刷新页面恢复。现在收到取消/失败终态（含断线重连后的状态校准）时会立即终结仍在执行中的工具卡片；同时移除 REST 历史回补时「运行中工具副本」合并逻辑——该副本永远等不到结果事件，是另一个永久转圈的来源

## 0.0.67 (2026-08-28)

### 后端

- 修复系统重启后飞书进度卡片不再更新：崩溃恢复自动续跑的任务此前只推送桌面/Web 前端，不感知飞书卡片，导致卡片永久停留在「正在处理」。现将会话的活跃进度卡片映射落库（新表 `feishu_progress_card`），恢复续跑时自动续更卡片进度直至终态（完成/失败/取消均会正确刷新终态，恢复后「取消任务」按钮继续可用）；任务终态后自动清理映射，不影响后续新任务的卡片

## 0.0.66 (2026-08-28)

### 后端

- 飞书进度卡片（任务执行中）新增「取消任务」按钮：点击即停止当前任务（置位取消标志并关闭会话 Shell，与桌面端停止按钮同语义），卡片就地转为「任务已取消」终态；仅任务触发者本人可操作，他人点击提示无权限；任务结束/失败后按钮随终态卡片自动消失

### 管理后台

- 模型列表移除「视觉」「默认」「状态」三列，改为在名称后内联展示图标：支持视觉显示图片图标，启用/停用分别显示绿/红状态图标，并按协议显示 OpenAI / Anthropic 品牌图标（悬停有提示）
- 修复模型编辑弹窗「API 协议」下拉不回显（始终显示「请选择」）：「OpenAI 兼容」选项原以空字符串作为 value，被 el-select 视为空值导致不显示选中标签；现改用 `openai-compatible`（后端归一为空串存储，行为不变），编辑回显时空串自动映射为该项

## 0.0.65 (2026-08-28)

### 后端

- 支持接入 Anthropic Messages 协议模型：模型新增独立「API 协议」字段（`api_protocol`，可选 `openai-compatible` / `anthropic`；`openai-responses` 待实现后放开，写入口暂拒绝），`provider` 保持渠道名语义仅作展示与分组；协议为 `anthropic` 的模型走原生 Messages API（`{baseUrl}/messages`，兼容官方与网关渠道的双认证头），支持流式对话、thinking、工具调用多轮循环、限流重试/退避、取消与用量归一（含 cache read tokens）；连通性测试同步支持 Anthropic 协议，mid-system-message 探测对该协议自动跳过；飞书群聊溢出摘要的 LLM 调用同步按协议路由，避免使用 Anthropic 模型时摘要静默失效；存量模型协议为空默认 OpenAI 兼容，行为不变

### 管理后台

- 模型表单「供应商」保持渠道名自由文本（例如: OpenAI, Anthropic），新增「API 协议」下拉（OpenAI 兼容 / Anthropic，OpenAI Responses 规划中置灰）

## 0.0.64 (2026-08-28)

### 前端

- 设置-定时任务列表展示任务具体内容（prompt），超出两行省略，悬停可查看全文

### 后端

- 飞书群聊上下文新增溢出摘要：注入窗口由最近 30 条收紧为 20 条（`FEISHU_BOT_GROUP_CONTEXT_MAX_ITEMS`），被窗口（条数上限或 120 分钟时间窗）淘汰且从未注入过的更早消息，会往前追溯最多 100 条（`FEISHU_BOT_GROUP_CONTEXT_OVERFLOW_ITEMS`，不限时间）由 LLM 生成一次性摘要，置于【群内最近消息】顶部，平衡 token 占用与上下文断层；摘要优先使用会话绑定的模型（未绑定时回退系统默认模型），失败时自动降级为不注入、不阻塞消息触发，并按会话缓存避免重复摘要
- 飞书群聊上下文中机器人（应用）发送者统一显示为「机器人_」+ open_id 前 8 位（如 `机器人_253023b9`），不再显示裸 open_id；同时触发消息与后台富化对机器人发送者跳过通讯录查询，消除无效调用报错
- 飞书引用消息超过 100 字时注入上下文仅保留前 100 字，全文落盘至会话工作区 `quoted/quoted-<消息id>.txt`，并在注入文本中以 `@{文件路径}@` 引用提示 Agent 按需读取完整内容；无法定位工作区或落盘失败时降级为 1500 字截断，不阻塞消息触发

## 0.0.63 (2026-08-28)

### 后端

- 修复飞书私聊会话被误分组为「飞书群聊」（标签显示为 `Agent名称:飞书群聊`）：建会话时显式传入的 `cloudProjectKey`（`feishu-{botId}-private-{userId}`）被工作区路径尾段推导覆盖，导致私聊判定不命中；现显式 key 优先，并新增迁移修复存量私聊会话的 `project_key`
- 修复飞书排队交互卡片发送失败（降级为纯文本提示、无按钮）：卡片 JSON 2.0 结构不支持 `tag: "action"` 交互模块且 `primary_primary` 非合法按钮类型，已按 2.0 规范改用 `column_set` 并排按钮（`primary`/`default`），排队卡片恢复正常渲染并支持「立即发送 / 取消本次任务」回调
- 修复飞书排队交互卡片在私聊（p2p）下显示「未知用户：」前缀的问题：私聊为一对一对话，对端即用户本人，不再显示发送者前缀；仅群聊保留发送者名以区分多人

## 0.0.62 (2026-08-27)

### 后端

- 对话模型列表（`/api/v1/models/active`）由按 modelId 排序改为按模型名称排序

## 0.0.61 (2026-08-27)

### 后端

- 飞书通道引入入站任务队列：Agent 执行期间收到新消息不再立即打断当前任务，而是进入按会话隔离的 FIFO 队列，当前任务完成后自动按序消费（无需用户干预）
- 排队消息即时推送交互卡片，提供「立即发送」（取消当前任务、该消息跳队首立即执行）与「取消本次任务」（将该消息移出队列）两个按钮；卡片一卡到底，排队的消息开始执行时在同一张卡上就地升级为进度卡片，全生命周期不刷屏
- 队列按会话持久化到 MySQL（`feishu_inbound_queue` 表），后端重启后自动恢复：在途执行的 Running 行按其消息是否已写入会话历史分支处理——已落库则清理（该消息由崩溃恢复重放，不重复处理），未落库则复位为排队重新消费（消息不丢失），随后接力消费排队消息；重启期间正在执行的任务仍由崩溃恢复续跑
- 群聊排队卡片按钮仅允许原消息发送者操作（open_id 比对），他人点击提示无权限

### 前端（桌面 / Web / 安卓）

- 飞书会话 Agent 执行中连续发送多条消息时，行为由「后一条打断前一条」改为「依次排队、逐条执行」，消息与卡片交互更可预期

## 0.0.60 (2026-08-27)

### 前端（桌面 / Web / 安卓）

- 修复飞书会话「工作区」显示原始 chat_id（如 `飞书群1·oc_bcdff9a`）的问题：右侧任务详情、聊天输入区指示器及边路任务面板现在优先显示群名称；飞书私聊显示"飞书私聊"而非临时工作区
- 修复后台子代理追问（`subagent_followup`）时面板看不到新一轮用户消息的问题：子代理 Tab 关闭期间收到追问，重新打开后新轮输出会直接续接在上一回合尾部且缺少追问内容，需刷新页面才恢复；现在前端实时接收 followup 通知并补插该轮用户消息，回合边界即时正确

### 后端

- 子代理追问创建后向前端广播 `subagent_followup_created` 事件（含真实消息 ID、任务文本与纠偏标记），并确保子会话已在 WS 订阅中——此前 followup 的 USER 消息仅落库无任何推送，前端无法感知新回合开始

- 修复飞书私聊发送文件完全无法被 Agent 处理的问题：私聊会话此前没有分配工作区，入站文件下载与 `feishu_download_file` 懒加载均因缺少落盘目录而失败，用户只看到"文件（接收失败）"；现在私聊会话按 mao 用户分配工作区，与群聊一致
- 修复飞书通道富文本/语音/视频/表情包等消息产生空用户消息的问题：入站占位文本统一复用引用消息预取的实现，支持 post 富文本正文提取及各类媒体占位符
- 飞书私聊媒体消息发送目标身份形态在建会话时确定（chat_id 前缀编码 union/open），移除发送时"猜身份失败再重试"的盲目双发；不可恢复错误（权限不足、频控等）直接返回真实错误码
- 飞书云文档读取：知识库（wiki）节点解析失败时直接报出真实原因（无权限/节点不存在），不再降级到必败的正文接口产生误导性错误

## 0.0.59 (2026-08-27)

### 后端

- 修复飞书群聊"图片 + @机器人"场景下 Agent 会话看不到任何图片消息：入站事件此前并发处理且图片先下载再入群日志，@ 触发消息会先读取上下文并推进增量水位线，晚落库的图片被永久跳过；现在群消息到达立即按顺序落日志（占位文本），发送人姓名解析与图片预下载改为后台异步富化（回填本地路径引用/显示名），同群内保证入库先于后续触发的上下文读取

### 前端（桌面 / Web / 安卓）

- 聊天输入 @ 引用文件时，搜索无匹配结果不再显示「未找到匹配的文件」空态框

## 0.0.58 (2026-08-26)

### 后端

- 修复引用 Agent 卡片消息时被飞书接口降级为“请升级至最新版本客户端”：获取消息详情时显式请求 `card_msg_content_type=user_card_content`，读取发送时的原始卡片 JSON 后再提取实际文本
- 飞书通道私聊用户消息移除 `【用户消息】` 前缀；群聊仍保留区块标题及发送人标识

- 飞书通道会话屏蔽 `ask_user_questions` 工具（工具列表不再暴露，模型无法在飞书对话中发起提问卡片）。
- 飞书通道会话终态不再标记未读（与微信通道一致）：DB `unread` 与 `session_status` 推送均排除飞书会话。
- 飞书私聊按 Agent 分组并显示 Agent 名称，飞书群聊分组显示为 `Agent名称:群名`。
- 修复前端本地分组逻辑覆盖飞书分组名称的问题，刷新任务列表后已有会话也按新名称显示。
- 飞书私聊和群聊分组使用云朵图标，与云端执行模式保持一致。

- `generate_image` 工具请求文生图 API 时按模型配置注入客户端伪装头（codex / claude_code），与主对话链路一致，修复部分要求 CLI 伪装头的中转网关拒绝图片生成请求的问题
- 修复引用/回复卡片消息时内容显示为"[卡片消息]"：卡片文本提取改为遍历卡片 JSON 树，兼容经典卡片（div/lark_md、markdown、note 元素）与新版 schema 2.0 卡片（header.title、body.elements），无文本元素的卡片才退化为占位符

- 飞书进度卡片实时性优化：工具触发时立即更新卡片并显示"执行中…"状态，不再等工具执行结束才刷新；长耗时工具执行期间用户可实时看到当前正在执行的工具
- 新增飞书通道专属工具（仅飞书会话可用，其他会话自动隐藏）：`feishu_send_image` / `feishu_send_file` 向当前会话对应的飞书用户或群聊发送图片、文件（支持本地路径与 http(s) URL，图片 ≤10MB 支持 PNG/JPG/GIF/WebP，文件 ≤30MB；先上传获取 image_key/file_key 再发送，私聊身份按 union_id 发送失败时自动按 open_id 重试一次）
- 飞书通道回复卡片移除 "Mao Agent" 标题行：进度/完成/失败/取消卡片不再显示标题，仅保留正文内容
- 飞书群聊图片改为入站即下载（非懒加载）：图片消息到达时自动下载到群工作区，上下文占位文本直接携带 `@{本地路径}@` 引用，Agent 免工具即可读取；预下载失败自动回退为原占位符，仍可通过 `feishu_download_file` 懒加载兜底（文件类消息保持懒加载不变）

- 飞书群聊会话标题改为群聊名称：新建群会话直接以群名作为标题（`im:chat:readonly` 权限缺失时降级为默认标题）；已存在的旧会话在下一次群消息触发时自动补更新为群名
- 修复会话压缩在高上下文占用下永不触发：压缩触发判断此前依赖内部 token 估算器（utf8 字节/4），对中文等场景显著低估（实测仅为真实用量的 ~40%），导致界面显示上下文已超 100% 而压缩仍被静默跳过；现触发判断优先采信锚点记录的真实 prompt 用量，估算器仅作无锚点时的兜底，且低于阈值跳过时记录日志便于排查
- 修复群聊回复/引用消息内容不可见：事件提取 parent_id，触发时预取被引用消息内容（群消息日志优先、消息详情 API 兜底，支持文本/富文本/图片/文件/卡片，超长截断）
- 触发消息结构化分块：用户消息按【引用的消息】/【群内最近消息】/【用户消息】区块拼接，与注入的上下文清晰区分
- `feishu_download_file` 增强：群消息日志未命中时（如引用机器人发送的文件）自动通过消息详情 API 获取 file_key 下载
- 群聊上下文改为增量注入：记录已注入水位线，每次触发只携带上次之后新增的普通群消息，历史不再反复进入用户消息；上下文时间前缀改为 `yyyy-MM-dd HH:mm` 避免跨日丢日期
- 新增飞书通道专属工具（仅飞书会话可用，其他会话自动隐藏）：`feishu_read_doc` 按链接读取飞书云文档（wiki 自动换取 obj_token，返回 Markdown）；`feishu_download_file` 懒下载群聊消息中的文件/图片到会话工作区（上下文占位符携带消息 ID，Agent 按需调用后用 read_file 读取）
- 修复飞书群聊 @ 机器人漏判：提及判定不再单纯依赖 botOpenId 与事件 mention id 的形态一致，`mentioned_type` 标记为 bot/app 的提及在无法确认是其他应用时倾向命中；群消息含提及但未命中机器人时打印原始 mentions 便于排查
- 修复飞书群聊消息中的 @ 提及占位符（`@_user_N`）未还原：现按事件 mentions 将占位符替换为 `@姓名`（含被 @ 的机器人），Agent 可感知提及对象
- 修复飞书群聊消息缺少发送人姓名：发送人姓名获取失败（如通讯录权限未开通）不再被永久缓存，并回退使用已绑定 mao 账号的用户名；群聊消息始终携带 `发送人：` 前缀（含无历史上下文的首条消息），Agent 可知消息发送者
- 飞书机器人回复升级为可更新进度卡片：任务接收后立即发送处理中卡片，Agent 每个 loop round 更新本轮模型摘要和工具执行摘要，完成、失败或取消时更新同一张卡片；卡片发送或更新异常时自动回退为文本回复
- shell 工具的 `workdir` 不再限制在工作区内，支持指定任意绝对路径
- 修复蓝绿部署期间崩溃恢复误判：延迟恢复复用启动快照前未重查会话状态，排空期间已正常完成的会话会被重放执行并误标为失败（表现为任务实际成功但会话显示"失败"）；现恢复前重查当前 phase，已进入终态的会话直接跳过

### 前端（桌面 / Web / 安卓）

- 飞书群聊会话不再归入"临时工作区"：按机器人×群聊的独立工作区分组展示（标签为"飞书群{botId}·{chatId 前缀}"）

## 0.0.57 (2026-08-25)

### 后端

- 修复飞书机器人长连接 ack 阻塞：入站事件处理器改为立即返回 ack，完整对话执行转入后台异步执行，避免 agent 长时间运行阻塞长连接导致飞书侧 ack 超时重发同一事件、造成消息重复执行（飞书已回复但任务一直显示运行中）
- 修复崩溃恢复误抓活跃会话导致的重复执行：蓝绿部署延迟恢复不再重新扫描 DB，改为复用启动时快照，避免把重启后新建的活跃会话（如飞书入站会话）误判为崩溃遗留 RUNNING 会话并并发重跑，从而消除消息重复回复、任务卡在运行中的问题
- 新增飞书长连接事件 ack 行为单测

## 0.0.56 (2026-08-25)

### 后端

- 新增飞书自建应用机器人通道（WebSocket 长连接接入，多机器人管理后台配置，私聊/群聊@机器人对话，绑定以 union_id 为身份锚，群聊上下文持久化与注入）
- 修复飞书机器人事件解析：兼容 Lark SDK 事件展开格式，群聊 @ 机器人判定基于 mentions 身份 key 与机器人 app_id 比对
- 修复设置页绑定飞书账号链路：绑定回调改为以扫码飞书身份绑定到发起绑定的目标用户
- 修复飞书会话执行并发：新消息到达时取消上一代执行并纠偏，执行结束后清理取消标记
- 新增飞书入站媒体处理：图片下载转 data URI 走多模态、文件下载落对应工作区以 `@{路径}@` 引用
- 修复长连接监控：重连成功/停机时清理退避重试定时器，reconcile 与 shutdown 竞态防护
- 飞书会话复用时按机器人配置热切换 Agent/模型；未配置 `APP_FEISHU_BOT_SECRET` 时明确告警/拒绝添加机器人
- 新增飞书模块单测（事件解析、入站处理、会话执行、群上下文）

### 管理后台

- 新增「飞书机器人」管理页面：机器人列表、添加/编辑（Agent/模型独立绑定）、启用/停用、删除

### 前端（桌面 / Web / 安卓）

- 设置页新增「飞书机器人」绑定项：展示绑定状态、发起授权绑定、解绑
- 统一对话中三圆点加载动效：主对话、侧边对话、子代理面板原存在缩放与上下浮动两种效果（圆点尺寸/颜色也有差异），现收敛为同一套缩放动画
- 修复边路/子代理面板加载指示器跨会话串扰：消息气泡内的三点指示器误读主会话的流式状态，主会话后台输出时会与本面板底部的三点同时显示（表现为两种 loading 叠加）；现按消息所属会话读取状态

### 终端 CLI（mao-cli）

- 知识库新增「飞书机器人通道」文档（`reference/feishu-bot.md`）：绑定流程、多机器人管理、环境变量与 REST 说明，并同步路由表与桌面/管理后台/配置文档

## 0.0.55 (2026-08-25)

### 前端（桌面 / Web / 安卓）

- 修复快捷指令/技能/文件引用在待发送队列与编辑回显中的展示：发送后队列消息与编辑回填输入框时，快捷指令、技能、文件引用现在以 Tag 形式展示，不再显示为 `${...}$` / `#{...}#` / `@{...}@` 占位符文本

### 后端

- 修复 DeepSeek 等 thinking 模式模型（如 ds-v4-flash）在多轮工具调用中频繁报 400「The reasoning_content in the thinking mode must be passed back to the API」：assistant 消息的思考内容（reasoning_content）现在会在上下文内存、序列化请求体、崩溃恢复/压缩重载历史三条链路上完整保留并原样回传，不再在工具调用后的后续请求中丢失

## 0.0.54 (2026-08-25)

### 后端

- 修复 ask_user_questions 取消判定：由子串匹配改为结构化应答结果（answered/resultJson），超时或无客户端时才广播取消事件，正常应答不再可能被误判为取消
- 修复 LLM 连续空响应重试耗尽后的错误处理：改用专用异常类型穿透适配器，不再被误判为可重试的流中断而叠加整轮流重试，用户直接看到真实的「自动重试已耗尽」错误
- 修复后台 shell 任务结果契约：异步命令完成后注入上下文的结果统一为与同步路径同构的结构化 JSON（exit_code/completed/output），整体限长提升至 10000 字符，不再出现 500 字符硬截断和截断成坏 JSON 的问题
- 修复会话消息队列自动消费的丢失问题：队列消息出队后若命中「模型不支持图片」「图片超过 10 张」「LOCAL 未连接」「服务器繁忙」等校验失败出口，现在会回补到队首并提示，不再出现消息已消费却永不执行
- 修复后台子代理进度统计在 LLM 整轮流重试后被清零的问题：工具调用计数改为跨轮累计，重试轮重复的工具调用自动去重，进度不再倒退

## 0.0.53 (2026-08-24)

### 后端

- 模型新增「客户端标识」配置：调用模型时按配置注入对应客户端的伪装请求头（Codex CLI / Claude Code CLI），取消原先根据模型名（gpt/claude 关键字）自动判断的逻辑。数据库迁移 V083 为 `llm_model` 新增 `client_impersonation` 列，存量模型统一置为 `none`（不注入）；此前依赖自动注入的 gpt/claude 模型，请在管理后台编辑模型将客户端标识改为 Codex 或 Claude Code

### 管理后台

- 添加/编辑模型弹窗新增「客户端标识」单选项（None / Codex / Claude Code），默认 None；连通性测试同样按该配置注入请求头

## 0.0.52 (2026-08-24)

### 前端（桌面 / Web / 安卓）
- 聊天「文件变更」列表支持右键菜单，与文件 Tab / Git 变更列表一致：复制绝对路径、复制相对路径、添加到聊天、下载此文件（CLOUD 模式），LOCAL 模式额外提供「在 Finder 中打开」

## 0.0.51 (2026-08-24)

### 前端（桌面 / Web / 安卓）
- 登录从弹窗改为独立登录页 `/login`：未登录访问任意页面只渲染登录页，不再挂载主布局；登录成功回到原目标路由（支持深链 `redirect`）
- 登录页支持记住用户名、飞书登录与主题切换；Electron 顶部保留可拖拽区并避让红绿灯，安卓适配 `100dvh` 软键盘场景
- token 失效（refresh 失败的 401）统一清本地会话并跳转登录页；403 改为普通错误提示，不再误当未登录踢出会话
- 移除登录弹窗 `LoginDialog.vue` / `useLoginDialog.ts` 及顶栏未登录「登录」按钮等死代码

## 0.0.50 (2026-08-24)

### 后端
- 会话输入框上传的文件保留原始文件名：runtime incoming 目录直接以清洗后的原文件名落盘（重名自动追加 -2、-3 序号），消息中的 `@{路径}@` 引用不再显示为 UUID

### 管理后台
- 合并单域名：`/admin/` 路径下托管管理后台，`base` 改为 `/admin/`
- 移除 `maoadmin.etarch.cn` 独立域名，改为 301 跳转到 `mao.etarch.cn/admin/`
- 修复 `/admin/` 子路径下站点图标不显示：favicon 改为随 `base` 引用 `app-icon-small.png`
- 修正单域 Nginx：`/admin/` 不可 rewrite 成 `/`，否则会返回桌面 index 导致白屏；验收须核对页面标题含 `Mao 管理后台`
- 侧栏按「能力 / 运行 / 安全 / 系统」分组，运行监控与用量分析提前到会话附近
- 数据概览改为健康看板：异常会话可下钻到运行监控，总量卡片可进入对应列表；周期报表入口指向用量分析
- 统一品牌为「Mao 管理后台」（登录页、侧栏 Logo、浏览器标题）；登录只记住用户名，不再把密码写入 localStorage
- 运行监控指标改为接口总数并可按阶段筛选；会话列表展示匹配总数；定时任务展示用户名与 Agent 名
- 侧栏、登录页与顶栏对齐桌面端浅色风格（#0066cc），侧栏可折叠；系统设置改为分类表单，Agent/模型/开关可直接改
- 移动端：刘海安全区、隐藏页签栏；会话/监控/Agent/模型/MCP/Skills/定时任务改为卡片列表；复杂筛选项收入「更多筛选」；Skill 上传提示改到电脑端操作

### 前端（桌面 / Web / 安卓）
- 修复 `useChat.ts` 中 `catch` 块引用块级变量 `sid` 的 TypeScript 错误

## 0.0.49 (2026-08-24)

### 管理后台
- 生产构建改为站点 `/admin/` 子路径，与桌面 Web 共用同一域名。旧的独立管理后台域名需 301 到新地址；Nginx 调整见 `docs/guides/single-domain-nginx-migration.md`。

### 后端
- 修复 LDAP 登录不校验账号禁用状态、角色接口可被普通用户提权、管理会话接口未等待管理员校验的问题。
- 修复模型/Agent 写接口缺少写权限校验、模型列表明文返回 API Key、项目文件列表路径穿越、工作区读文件跟随符号链接逃逸。
- 修复定时任务在等待会话锁期间被删除/暂停仍会执行或误计次数，以及队列占位窗内与手动发送双跑。
- 修复 Git 写操作锁 TOCTOU，并发 commit/pull/push 不再交叉执行。
- 修复微信监控循环退出误删其他实例、长轮询无法中断，以及多联系人时 `send_wechat_image/file` 发错人。
- 修复任务通知在 WebSocket 判定后无条件覆盖终态，可能导致 Webhook 重复投递。
- 修复会话插队先删队列再落库导致失败丢消息、编辑重发先截断历史、REST 编辑不校验最后一条用户消息、线程池拒绝后插队停消费与边路 claim 泄漏。
- 修复 `wait_subagents` 无法响应取消、LOCAL 边路不同步 skill、工具轮次把仍在跑的 BACKGROUND/FOLLOWUP 误标已投递、改 prompt/注入后台结果后仍使用旧的预构建请求。
- 修复子代理在等待审批/取消中仍可追问、后台任务 30 分钟清理误伤其他会话、过期 shell 清理漏维护会话配额、grep JS 回退按文件名匹配 glob 且不跳过依赖目录。
- 修复统计与分析接口仅校验登录、禁用用户接口不走停用保护。
- 修复消息队列并发排队时排序号重复的问题：入队改为事务加行锁，出队/重排增加稳定的二级排序。
- 修复队列自动消费在会话被手动发送抢占后消息已出队却永不执行的问题：占位提前到出队之前，提交失败时回补队头。
- 修复 `edit_file` 文件变更统计虚报增删行数的问题：改用与 `write_file` 一致的精确 diff 计算。
- 修复未安装 ripgrep 时文件名搜索结果被 node_modules 等依赖目录淹没的问题：JS 回退遍历现跳过依赖与构建产物目录。
- 兼容部分模型网关将流式工具名分片传输的场景，避免工具名被拼接损坏。
- 上下文压缩请求现在正确透传模型的思考开关（thinking/enableThinking）。
- 后台子代理执行队列满导致提交失败时，父代理现在能收到失败结果并作出反应，不再静默丢失。
- 修复微信入站处理器缺失类型 import、`wait_subagents` 取消标志类型不匹配导致无法编译。
- 微信会话对端改为落库，服务重启后多联系人账号仍能向正确联系人发图/文件。
- 取消中的会话视为忙碌，避免 WS 插队与定时任务在取消未完成时再次执行。
- 边路任务在线程池拒绝时标记失败，避免留下空任务页。
- 修复定时任务 `inFlight` 在异步执行完成前被释放，导致同一任务被重复触发、执行计数可能丢失；扫描周期重叠时跳过本次扫描。
- 修复后台任务已完成结果在所属会话不再消费时无法回收，造成跨会话堆积。

### 前端（桌面 / Web / 安卓）
- 修复弱网下 WebSocket 快速断连重连时心跳定时器被旧连接事件清除、死链不再被检测的问题。
- 修复快速切换会话时旧 `loadSession` 响应覆盖新会话、边路多个占位 Tab 绑错会话、后台会话完成误关前台发送态。
- 修复主会话乐观助手气泡未登记流式 ID 导致双气泡、入队判定漏掉恢复/取消中状态、入队消息未解析文件引用。
- 修复边路首条发送前未确保 WebSocket 已连接。

### 桌面 Electron
- 同步修复本地工具 `edit_file` 的文件变更统计口径（同后端）。

### 管理后台
- 会话详情页快速切换时不再出现旧数据覆盖新会话的竞态；列表页/运行监控页首次进入不再重复请求。

### 终端 CLI（mao-agent）
- **修复 LOCAL 并行审批卡死**：一轮多个工具同时需要审批时审批弹窗相互覆盖导致任务永久悬挂，现按顺序串行展示逐个处理。
- 修复 REPL 向执行中的会话发消息被静默吞掉的问题：等待当前执行结束后自动重发输入。
- 取消指令改走可靠发送通道，断线瞬间 Ctrl+C 不再丢失导致服务端任务跑满全程。
- 弱网下不再对非幂等 POST 请求自动重试，避免重复建会话等副作用。
- 修复本地工具结果 `sendReliable` 失败被忽略、WS `error` 事件不结束等待导致 REPL/`-p` 挂起。
- 修复 `grep_search` 在 Windows 路径下按冒号切分 rg 输出导致匹配丢失，以及 `read_file` 负 offset 与后端钳制口径不一致。
- 修复 LOCAL `grep_search` 使用 `rg --json` 后丢弃上下文行，与 CLOUD 行为对齐。

## 0.0.48 (2026-08-24)

### 后端
- 修复上游模型在流式响应中途触发限流（HTTP 200 已发出、429 内嵌在 SSE 事件里）时被误判为「LLM 连续返回空响应」的问题：现在能识别流内错误事件，限流与 5xx 按原有退避策略自动重试，重试耗尽后提示真实的限流原因；鉴权、参数等不可重试错误立即失败。
- 兼容部分模型不支持工具参数顶层 `oneOf`、`anyOf`、`allOf` 的限制，规范化 MCP 工具输入 schema。

## 0.0.47 (2026-08-23)

### 管理后台
- 修复登录态 24 小时后必掉线的问题：access token 过期时自动使用 refresh token 静默续期并重试原请求，7 天内活跃使用无需重新登录。
- 登录页新增「记住密码」：勾选后在同一浏览器自动回填账号密码。

## 0.0.46 (2026-08-22)

### 前端（桌面 / Web / 安卓）
- 新增草稿（Draft）系统：输入框的文本和待发文件按会话/新建任务/边路任务键隔离，发送成功或会话关闭时自动清除，切换会话/任务不丢失输入内容。
- 退出登录时清空所有草稿，避免残留。
- 删除/晋升边路任务时同步清理对应草稿。
- 修复编辑并重发（edit_and_resend）时 `@文件` 引用显示为原始文本的问题：文件引用的路径改写现在在消息渲染前完成，不再晚于乐观 UI 更新。

### 后端
- 安全加固：JWT 增加类型区分（access / refresh / shell），refresh token 不再能当作访问凭据调用受保护 API；终端 `mao-cli` 使用的 MAO_TOKEN（shell 类型）鉴权能力保持不变。
- 修复编辑并重发按 `created_at` 定位目标消息导致可能误删大量历史消息的问题：现改为按自增 id 定位最后一条用户消息。
- 修复 Agent 循环中空响应计数跨轮次泄漏的问题：恢复"连续 3 次空响应才中止"的原语义，有效输出轮次会正确清零计数。
- 修复本地 Shell 会话自动生成的会话 ID 在同一毫秒内并发创建时会撞车的问题：ID 中加入随机段。
- 修复 Prompt 引擎行内命令标记（如 `#{...}#`）一条消息出现多次时只替换第一处的问题：现全部替换。
- 修复队列消息自动消费时，队头消息若仍被执行中的任务占用会被错误搬到队尾的问题：现保持在原位等待下一轮。
- 修复 ripgrep（rg）搜索结果含冒号的行被错误切分、导致匹配内容错乱或漏计的问题：改用 `rg --json` 结构化输出解析。
- 修复定时任务在锁排队期间连环补发的问题：同一任务同时只允许一个执行实例进入，且获得锁后会先重读最新任务定义与状态，已停用/已删除的任务不再执行。

## 0.0.45 (2026-08-21)

### 后端
- 修复 `check_subagent` / `wait_subagents` 查询运行中的后台子代理时，轮次、工具调用数、token 消耗恒为 0 的问题：这些统计原先仅在子代理终态时才落库，现改为运行中优先读取内存中的实时执行上下文，终态后仍以落库值为准。

## 0.0.44 (2026-08-21)

### 后端
- 微信通道会话（定时任务等机器触发）进入终态时不再计未读：DB `unread` 不置 1，WebSocket 终态推送携带 `unread: false`，避免聚焦模式任务列表被无人工关注的微信Bot会话置顶；其余会话行为不变。

## 0.0.43 (2026-08-21)

### 前端（桌面 / Web / 安卓）
- 会话输入框草稿与会话绑定：在会话 A 输入的内容切换到会话 B 后不再跟随显示，切回 A 时自动恢复草稿（含待发图片/文件）；「新建任务」与边路任务的输入框同样按各自维度独立保存恢复；发送成功或删除对应会话后草稿自动清除。

## 0.0.42 (2026-08-21)

### 前端（桌面 / Web / 安卓）
- 消息队列支持撤回编辑：Agent 执行中排队的消息可点击「编辑」撤回到输入框，修改文本与图片后重新发送（忙则排队尾，闲则直接执行）；输入框已有草稿时先确认再覆盖。

### 后端
- 日志时间戳改为东八区（UTC+8）：`time` 字段由 UTC ISO 格式（`...Z`）改为 `2026-08-21T15:37:02.152+08:00` 格式，便于按本地时间排查问题。
- 兼容 OpenRouter 风格思考字段：LLM 响应解析现支持 `reasoning` / `reasoning_details`（含流式 `delta.reasoning`），归一化到内部思考通道，修复部分模型（如 `stealth/ox-alpha`）执行任务时思考过程不显示的问题。

## 0.0.41 (2026-08-21)

### 终端 CLI（mao-agent）
- 新增 `mao-agent update` 自更新命令：复用安装脚本逻辑（git 拉取 `~/.mao/agent-cli/src` → 构建 → 全局重装）；`--check` 仅检查远端新版本；支持 `--ref` / `--repo` 指定分支与仓库源。MCP clientInfo 版本改为随包版本。
- 修复非 TTY 环境下所有命令无条件读 stdin 导致挂起的问题（Agent 持久 shell 中 `mao-agent update --check` 等子命令会卡死）：子命令不再读 stdin；chat 命令保留管道提示词输入并加 2s 空闲超时兜底。

## 0.0.40 (2026-08-21)

### 后端
- 移除 Agent「标签」功能：删除 `agent_tag` 表（Flyway V081 自动清理）及后端相关字段、接口与存储代码。标签此前仅用于管理后台列表展示/筛选，无运行时消费。

### 管理后台
- 移除 Agent 表单中的「标签」输入项与列表中的「标签」列、标签筛选器。

## 0.0.39 (2026-08-21)

### 后端
- 修复管理后台删除 Skill 报「服务内部错误」的问题：清理 agent 关联技能名的 SQL 误用了驼峰列名 `skillNames`（实际列为 `skill_names`），导致删除必然 500；同时清理失败不再阻断删除本身。
- 清理 `llm_model` 表历史遗留字段 `max_tokens`、`temperature_max`（代码零引用），启动时 Flyway 自动删除。

### 终端 CLI（mao-agent）
- 交互 TUI 视觉分层：会话欢迎圆角卡片、用户消息 `❯` 前缀、工具 `⏺`/`⎿`、Markdown 标题/代码着色；底部圆角输入框（占位符弱化）与按模式着色的状态栏（LOCAL 黄色 / CLOUD 蓝色）。
- 输入 `/` 弹出斜杠命令快捷选择（↑↓ 选择，Enter 执行，Tab 填入，Esc 关闭）；`/model` 可继续点选模型名。

### 后端
- 修复 WS 推流 diff 私有字段名不一致导致实时 diff 失效、`tool_call_result` 经 WS 泄漏文件全文的问题。
- 修复用户技能删除/读取路径穿越、技能 sync-package 越权下载他人私有技能、消息编辑 IDOR 等安全漏洞。
- 运行中会话禁止删除；会话记录不存在时 AgentLoop 正确停止执行，避免孤儿任务继续跑。
- 修复推理模型思考被输出上限截断时被误判为正常完成的问题：当流式响应 `finish_reason=length` 且未产出正式回答（仅思考内容）时，丢弃该轮思考并像网络异常一样自动重试；重试耗尽后向前端发送明确错误「模型思考被输出上限截断，自动重试已耗尽，请重试」，不再出现「子代理未产生文本输出」的假象。
- 修复 Agent Loop 在 LLM 返回空响应（无 content、无 tool_calls）时静默退出的问题：注入系统提示让模型重试，最多 3 次，超限后抛出明确错误「LLM 连续返回空响应，自动重试已耗尽，请重试」，不再静默标记 COMPLETED。
- 子代理执行失败（如 LLM 重试耗尽）时向前端补发 WS `error` 事件，子代理面板可显示异常提示与重试按钮。

## 0.0.38 (2026-08-20)

### 后端
- 修复 Agent 未配置「关联 Skills」时系统提示词中会加载全部系统级 skills 的 bug，改为未配置时不再加载任何系统级 skills（`skillNames` 为空 = 不加载）；同时修复 prompt-engine 中 `buildSkillCatalog` 的相同 fallback 逻辑，以及 quick commands 接口中同款问题。
- LLM 上游 DNS 解析失败（`ENOTFOUND` / `EAI_AGAIN`）现在与连接失败一样进入自动重试；重试耗尽后会向客户端发送可恢复的执行错误。
- 新增聊天输入区任意文件上传：`POST /v1/files/upload-incoming` 将文件保存到会话 runtime 临时目录（`runtime/{userId}/{sessionId}/incoming/`），返回绝对路径供 Agent 引用；会话删除时级联清理该 runtime 目录，并清理会话工作区内 `mao-runtime-` 前缀的临时产物（不影响用户项目文件）。
- 工具路径沙箱放行会话 runtime 目录，Agent 的读文件 / Shell / 搜索等工具可直接访问用户上传到 runtime 的临时文件。
- 系统提示补充「用户上传的文件」说明：`@{绝对路径}@` 引用指向 runtime 临时文件（非工作区相对路径），文件类型不固定，由 Agent 自行探测与处理。
- 上传文件存储文件名改为按安全字符集生成（剥离原文件名的路径分隔符/URL 编码等危险字符），避免恶意文件名造成存储路径逃逸；incoming 文件不出现在通用文件列表中（由会话删除统一回收），本地模式会话拒绝服务端上传。
- 文件下载/预览/删除接口补充属主校验：仅上传者本人可访问，防止跨用户枚举 file id 访问他人上传的文件。
- 会话删除的数据库删除不再被文件清理失败吞掉异常：数据库删除失败会正常上报，文件清理失败仅记日志，不影响会话删除结果。
- LOCAL 模式 `shell` 的 `async` 现在先确认桌面端已连接并完成命令下发（会话已存在），再把等待输出提交到后台任务；未连接时立即返回与同步路径相同的错误，不再误报「已提交」。
- WebSocket `client=cli` 不再被静默归一为 `browser`，服务端日志可区分终端 CLI 流量；`cli` 与 `electron` 一样可作为 LOCAL 本机执行端（`tool_execute` / 技能与 MCP 同步会投递给 CLI）。

### 前端（桌面 / Web / 安卓）
- 聊天输入框「+」按钮支持上传任意文件（图片仍走原有多模态链路）：选择/拖拽/粘贴文件后暂存为待发条目，发送消息时自动上传到服务端 runtime 目录，并将文件引用（`@{绝对路径}@`）追加到消息正文中。新会话无需先创建会话即可上传文件。
- 本地模式（LOCAL）下隐藏文件上传入口（提示使用 @ 引用工作区文件）。

### 桌面 Electron
- 本地 `edit_file` 与云端对齐：默认唯一匹配，出现多次时拒绝写入；`replace_all=true` 才替换全部出现。
- 本地模式 shell 对齐云端能力：默认一次性会话（`keep_session` 才保留）、合并 stderr、回传真实 `exit_code`、复用会话时执行 `cd` 并刷新 `current_workdir`、默认等待 5 分钟、关闭时回收整个进程组、`write_stdin` 也会刷新 `MAO_TOKEN`、输出全程落盘，并按空闲 30 分钟 / 最长 2 小时清理过期会话。
- 本地 shell 的 `async` 会先创建会话并写入命令再返回 `session_id`；兼容旧参数 `timeout`（秒）映射为等待上限。

### 终端 CLI（mao-agent）
- 本地 `edit_file` 与云端对齐：默认唯一匹配，出现多次时拒绝写入；`replace_all=true` 才替换全部出现。
- 新增第五端 `mao-agent`（`agent-cli/`）：终端对话式 Agent 客户端。支持交互式 REPL、`-p` 打印模式、`text|json|stream-json` 输出、`login/logout/status/ls/resume/--continue`，以及 `ask_user_questions` 交互（打印模式 `--on-question=fail` 会先发 `cancel`）。
- 事件按 sessionId / executionId 本地过滤，避免 desktop 其它会话的终态让 `-p` 误退出；重连后按 `tool_call_id` / `requestId` 去重。
- `--permission-level` 仅写入会话记录：CLOUD 下不产生工具审批。
- 与 `mao-cli` 共用 `~/.mao/auth.json`；`MAO_AGENT_BASE_URL` 到 `/api` 为止（自动剥掉误粘贴的 `/v1`）。
- 修复 REPL：上下文占用把 token 数当成百分比显示成 `13936%`；状态栏 `\r` 会擦掉正在流式输出的回复；执行中未暂停输入导致第二行把第一轮 `executionId` 冲掉。
- 支持 LOCAL 模式（`--local`）：本机执行 shell / 读写文件 / glob / grep，复用 `localShell.cjs`；工作区信任、默认拒绝清单、`--yolo` / `--approve-rule` / TTY 审批；拒绝时同时发 `tool_approval` 与 `tool_error`。非 TTY 默认 `--on-approval=fail`（退出码 4）。技能 zip 同步与 MCP stdio/HTTP 代理已接通。
- 支持一条命令安装：`curl -fsSL https://raw.githubusercontent.com/DC-ET/mao/main/scripts/install-mao-agent.sh | bash`（Node.js ≥ 20）。包内自带 `vendor/localShell.cjs`，不必再克隆完整仓库。
- 交互体验：启动即显示会话身份与最短帮助；流式输出时状态栏仍可见（半行输出时钉在底行）；执行中可预输入下一条（回车排队）；工具输出默认折叠（`/verbose` 展开）；`ask_user_questions` 与 LOCAL 审批改为同一输入通道上的模态（方向键 / y n a）；登录密码无回显；`ls` / resume 显示相对时间与可读摘要。`--no-queue` / `--ascii` / `--verbose-tools` 可覆盖。
- REPL 润色：流式正文不再半截渲染 Markdown，回合/工具边界再整段着色；斜杠命令支持 Tab 补全（`/model` 可补全模型名）；状态栏显示 `Todo 2/5`；新增 `/copy` 复制上一回合回复；resume 历史摘要按角色对齐。
- REPL 对话帧对齐 Claude Code / Codex：底部圆角输入框（滚动区钉底，矮终端回退 `❯`）；用户回合 `❯` 回显；工具 `⏺ name  args` + `⎿` 摘要（JSON 参数收成 command/path）；回合结束只留短 footer，身份信息放到输入框底栏。
- 交互 REPL 顶栏固定、对话在中间滚动、底栏输入框钉底，不再和 bash 提示符叠在每一行上；用户消息改为整行深灰块，输入框提示「继续对话」。不使用备用屏，终端原生 scrollback 可回看历史对话。
- 修复交互态状态栏：排队/斜杠提示不再粘在 spinner 同一行；状态行按终端宽度截断避免换行残留；半行等待不再用换行把旧状态推进历史；无流式增量时回退打印终态文本，仍为空则显示「(无文本回复)」。执行中粘贴或一次写入整句（含中文）会正确入队，不再被当成无效按键丢掉。
- 修复 REPL 底栏输入框把对话「吃掉」的问题：改用 DEC 存/恢复光标（`ESC 7/8`）并与正文同一路输出，等待 LLM 时仍能看到上方的用户消息、工具卡片和流式回复。

### 终端 CLI（mao-cli）
- 将 `mao-user-cli` 与 `mao-admin-cli` 合并为统一的 `mao` / `mao-cli`，并移除这两个旧 Skill 目录。默认请求 `https://mao.etarch.cn/api/v1`，JWT 仍缓存在 `~/.mao/auth.json`。个人会话用 `session`，全站检索用 `admin-session`；个人技能用 `skill`，全局技能用 `skill-docs`。旧命令名 `mao-user` / `mao-admin` 仍可用（`mao-admin session/skill` 会自动映射到管理端模块）。
- **Skill 升级为产品知识库与代理入口**：`skills/mao-cli` 收录项目说明、安装/部署/配置、管理后台/桌面/Electron/安卓/mao-agent 手册与排障；可独立分发。仓库 `README.md`、`USER_GUIDE.md`、`DEPLOY.md` 等改为引用 mao-cli 正文。
- 新增 MCP REST CLI：`mao mcp`（偏好、`/me` 用户级 MCP、全局 CRUD/测试），见 `reference/mcp.md`。

---

## 0.0.37 (2026-08-18)

### 后端
- 修复 CLOUD Shell 工具未捕获命令标准错误输出的问题；CLI 校验错误等 `stderr` 内容现在会正常返回并写入 `output_file`，日志不再混入内部命令完成标记和工作目录探测结果。
- 会话标题生成模型可配置：新增系统设置 `session.titleModelId`，用于指定生成会话标题所用的模型；未配置时统一使用默认模型生成标题，不再沿用当前会话选择的模型。
- Git 提交信息生成模型可配置：新增系统设置 `git.commitMessageModelId`，用于指定生成 Git 提交信息所用的模型；未配置时使用默认模型。LLM 调用时禁用思考（reasoning/thinking/enableThinking 三字段同发）。

### 管理后台
- 系统设置页「会话」分类新增「会话标题生成模型」配置项，支持从模型列表下拉选择，留空表示使用默认模型。
- 系统设置页「代码」分类新增「Git 提交信息生成模型」配置项，支持从模型列表下拉选择，留空表示使用默认模型。

### 桌面 Electron
- 飞书登录改为内嵌窗口授权：不再生成二维码让手机扫码，改为在 Electron 内打开飞书授权页面直接完成登录，解决手机飞书无法访问内网 redirect_uri 导致白屏的问题。

### 前端（桌面 / Web / 安卓）
- 修复快速切换会话后立即发送消息时，消息可能被发送到上一个会话的问题；发送前会校验当前会话，切换中的输入不会被误清空。
- 飞书登录 UI 同步调整：移除二维码展示，改为点击「飞书登录」按钮后直接打开授权页面（Electron 内嵌窗口 / Web 新标签页）。
- 用户菜单的「退出登录」项补充图标，和个人信息、设置保持一致。

---

## 0.0.36 (2026-08-17)

### 后端
- 修复 `WEIXIN_BOT_ENABLED=false` 未能彻底禁用微信 Bot 的问题：禁用后不再允许确认绑定或启动账号监控，避免已有或后续绑定继续接收并回复微信消息；微信 Bot 默认配置改为开启。
- 修复删除模型并迁移历史会话的模型配置时改写会话 `updated_at`、导致旧会话被错误排到列表前部的问题；迁移不再影响会话排序时间。
- 修复子代理完成事件恰逢客户端断线时，重新订阅未回传终态、界面会持续显示“执行中”的问题；订阅现在会同步当前会话终态供客户端校准。
- 新会话与边路任务的标题改为根据首条用户消息异步调用当前会话模型生成，模型不可用或调用失败时自动回退为消息前缀，不阻塞 Agent 执行且不覆盖用户手动标题。
- 工具执行结果事件补充工具名称，避免客户端漏收开始事件后无法识别工具类型。
- `wait_subagents` 新增可选超时参数，默认等待 30 分钟；超时后返回当前后台子代理状态，避免主代理被无限期阻塞。
- 移除会话因长时间无活动自动终止的定时清理逻辑，不再在后台把长时间未交互的运行中会话标记为 FAILED。
- 新增边路任务升级为主会话接口：将已结束的边路任务复制为独立主会话，并沿用现有删除语义移除原边路任务入口。
- 修复边路任务升级为主会话后，再新建会话默认跑到临时工作区的问题。
- 修复快捷 Skill 标记未命中日志始终显示 `${label}$` 且对字面模板变量刷 WARN 的问题。
- 会话上下文压缩改为内部流式调用 LLM 并聚合交接内容，避免大上下文压缩时因非流式等待完整响应而更容易触发 504 超时。
- 修复蓝绿部署排水脚本在 5 分钟等待期内无任何日志、失败后难以排查的问题；排水任务启动时立即写入日志。
- 修复蓝绿「冷启动」路径（活跃端口进程已退出时）不清理对端孤儿实例，导致 9080/9081 长期双开的问题。
- 蓝绿部署摘流（排水）默认等待时间由 5 分钟缩短为 **1 分钟**（`MAO_BLUE_GREEN_DRAIN_SEC`，默认 `60`）。
- 蓝绿排水改为优先经 `systemd-run` 定时调度（`atd` / `nohup` 兜底），避免 Agent 在云端 shell 内执行 `restart.sh` 后会话关闭 SIGKILL 进程组导致排水任务丢失、9080/9081 长期双开。
- `backend-ts/restart.sh` 在云端 shell 调用时自动 `setsid` 重新执行，避免部署流程本身被 shell 会话回收中断。
- 修复主任务进入终态（完成 / 失败 / 取消）时未下发 `session_tree_status`，导致前端任务列表的 `treeRunning` 聚合状态停留在旧值，蓝色“执行中”圆点不转绿的问题。
- 修复回答 Agent 提问后未重新同步会话运行态，导致左侧任务列表圆点错误变为空闲的问题。

### 前端（桌面 / Web / 安卓）
- 修复断线期间子代理完成后重新订阅仍显示“执行中”的问题：客户端会按服务端终态清理流式执行状态。
- 会话与边路任务标题统一实时展示后端大模型概括结果，移除客户端消息前缀命名，多个客户端之间保持一致。
- 修复刷新会话时 WebSocket 重复发送相同 `subscribe` 请求的问题。
- 修复任务执行中偶发把工具摘要降级显示为 `tool` 的问题；工具结果可独立恢复名称，迟到的开始事件也会纠正占位名称。
- 修复“我的技能”抽屉中已上传 / 本地技能名称字号偏大的问题，列表标题现在与系统技能保持一致。
- 边路任务列表的“升级为主会话 / 编辑标题 / 删除”操作改为右键菜单，减少列表行内按钮干扰。
- 修复后台任务完成后左侧任务列表圆点偶尔仍显示运行中的问题，列表状态事件和父任务聚合状态现在会优先纠正过期的边路任务运行态缓存。
- 修复子代理切换后右侧上下文偶尔退化为 token 值的问题：子代理模型现在优先按自身元数据解析，不再依赖上下文缓存是否已回填。
- 边路任务列表新增“升级为主会话”入口，升级成功后自动跳转新主会话并移除原边路任务入口。
- 修复聚焦模式任务列表完成后仍显示“运行中”的问题，并将运行中时长改为按本次任务开始时间计算。
- 边路任务触发上下文压缩时新增进行中提示，避免任务执行期间看起来静默卡住。
- 修复发送子代理纠偏消息后，旧执行的思考块和工具调用偶尔持续显示“运行中”的问题。

---

## 0.0.35 (2026-08-16)

### 后端
- 会话运行态新增可恢复快照：刷新页面后仍能恢复显示上下文压缩中、LLM 等待与上游重试提示；压缩用非流式 LLM 调用也会向前端同步等待/重试进度。
- 子代理工具体系统一为后台异步 `subagent` 系列：移除运行时 `delegate` / `delegate_followup`，新增 `subagent_followup` 支持对子代理空闲追问和运行中纠偏。
- 后台子代理工具补齐用户可见执行摘要，`spawn_subagent`、`check_subagent`、`cancel_subagent`、`wait_subagents` 不再只显示英文工具名。
- 修复微信渠道连续发送多条消息时不触发纠偏、逐条各回复一次的问题：此前消息接收循环串行等待前一条完整执行完毕才处理下一条，导致纠偏取代逻辑永远不生效；现改为批内消息并发触发，后续消息可在前一条 Agent 执行期间重入并取代它，只对最新一条消息回复。
- 微信会话创建按用户串行化，避免并发消息触发时重复创建会话。
- 新增后台子代理能力：主代理可通过 `spawn_subagent` 在后台异步派发子代理并继续主线，通过 `check_subagent` 查看进度、`cancel_subagent` 取消指定任务、`wait_subagents` 等待全部完成。
- 主循环结束时若仍有后台子代理运行，自动挂起等待全部结束后继续产出最终结论；后台子代理完成时主动将结果注入主代理上下文。
- 后台子代理接入现有崩溃恢复与结果交付体系，后端重启后可恢复未完成任务并交付结果。
- 后台子代理产生的文件变更归集到主会话的完成通知卡片上。
- **蓝绿部署**：`backend-ts/restart.sh` 在 9080/9081 间交替启动新实例、健康检查后切换 Nginx upstream，默认 5 分钟后异步停止旧实例；避免 Agent 自部署时会话被硬中断后陷入无限重启循环。
- 崩溃恢复在蓝绿切换期间跳过仍在旧实例上活跃的会话，排水结束后再做延迟恢复。
- 修复蓝绿部署 `deploy.flock` 被新启动的 Node 进程继承导致后续重启永久报「deploy already in progress」；部署互斥锁改为启动新实例前释放，排水脚本在 upstream 已回切时不再误杀当前实例。
- 用户 HOME 目录默认改为 `/opt/mao-data/users`（`MAO_USER_HOME_DIR`），与 `MAO_RUNTIME_DIR` 等数据路径统一，避免误写入仓库旁 `/opt/mao/data/`。

### 前端（桌面 / Web / 安卓）
- 刷新运行中的会话后恢复显示上下文压缩与上游等待/重试提示，避免任务仍在运行时页面看起来静默卡住。
- 子代理工具展示新增 `subagent_followup` 中文名称、专用图标与子代理详情跳转，历史 `delegate` 展示继续兼容。
- 后台子代理工具调用补齐中文名称与专用图标，移动端/桌面端工具列表不再回退显示齿轮和英文工具名。
- 主会话支持展示「后台子代理完成」通知卡片，点击可跳转查看对应子代理详情。

### 安卓原生
- Android 冷启动加载远程页面时新增原生加载页：弱网下不再长时间白屏，连接超时会提示检查网络并提供重新连接入口。
- 修复 Android 启动器图标上下被系统自适应图标蒙版裁剪的问题。

---

## 0.0.34 (2026-08-16)

### 前端（桌面 / Web / 安卓）
- 修复编录等边路任务执行完成后，末尾偶尔重复展示本轮用户消息的问题。
- 修复右侧文件列表右键「添加到聊天」插错对话框的问题：此前会固定插入主会话输入框或被边路任务输入框顶替，现在跟随当前激活的对话框 tab（主会话 / 编录等边路任务）插入对应输入框；在文件 / 变更等无输入框的 tab 上点击时仍回退到主会话。

### 后端
- 移除 Docker 部署能力（删除 `docker/`、`docker-compose.yml`、`.dockerignore`、`.env.docker.example`），部署方式统一为手动部署，详见 [DEPLOY.md](DEPLOY.md)。

---

## 0.0.33 (2026-08-15)

### 后端
- **移除 Java 版后端**（`backend/`、`pom.xml`、Maven 构建、`Dockerfile.backend` 与 CI 中的 `mvn test`），由 TypeScript 版后端完全替代。
- TypeScript 后端默认监听端口由 9081 改为 **9080**（`MAO_TS_PORT` 可覆盖），与既有 Nginx 反代 / 前端默认地址保持一致；Flyway 迁移脚本迁入 `backend-ts/db/migration/`，后端启动时自动执行。
- 本地/服务器启停脚本统一为 `scripts/start-backend.sh` / `stop-backend.sh` / `restart-backend.sh`（原 Java 版脚本删除，`*-backend-ts.sh` 更名为无后缀版），PID 文件统一为 `.backend.pid`。
- 部署说明与配置模板更新：见 [DEPLOY.md](DEPLOY.md) 与 `backend-ts/.env.example`（原 Java `application-example.yml` 随源码移除）。
- 修复云端 shell 持久会话通过 `write_stdin` 执行命令时不注入 `MAO_TOKEN` 的问题：现在与 `exec` 一致，每次写入命令前都刷新注入短效令牌，避免 `mao-user` / `mao-admin` CLI 在复用会话时出现未登录（401）。

---

## 0.0.32 (2026-08-15)

### 前端（桌面 / Web / 安卓）
- 缩小待发送队列消息删除确认框的标题、正文与操作按钮，改善移动端显示比例。
- 修复任务执行工具期间刷新页面后，进度中缺少当前正在调用工具的问题；重连订阅时恢复运行中工具及其最新参数。
- 「我的技能」抽屉新增「系统技能」页签，展示系统预置 Skills（仅可查看详情），与个人上传技能区分展示。
- 修复新建会话开始执行后左侧任务圆点偶尔仍显示空闲：防止迟到的会话详情响应覆盖 WebSocket 实时运行状态。
- 修复任务完成后偶尔在最终回复下方重复展示本轮用户消息：历史刷新时用落库消息替换对应的本地乐观消息。
- 修复刷新后切换多个运行中子代理时，后打开的子代理缺少用户消息和部分工具摘要：首次打开始终回补完整历史，并保留请求期间到达的运行中工具状态。

### 后端
- 修复 TypeScript 后端并行工具调用时 `tool_call_id` 串到其他工具：改为 `AsyncLocalStorage.run` 隔离，避免并行 `delegate` 绑错父工具或报缺少调用 ID。
- 修复 TypeScript 后端系统提示「当前日期」使用 UTC：改为 `Asia/Shanghai` 日历日，与定时任务时区一致。
- 取消 `delegate` / `delegate_followup` 子代理的固定运行时长限制，改为持续运行直至完成或被主动取消。
- 修复子代理执行期间重启后端后父任务重复委派的问题：恢复原子会话与 execution，幂等回填委派结果后再继续父任务。
- 修复 shell `write_stdin` 不回显结束标记导致交互式命令必定空等到超时的问题，现在输入后立即返回。
- 修复 CLOUD shell 未注入 `MAO_TOKEN`，`mao-*-cli` 在云端会话中无法鉴权的问题。
- shell 执行结果补回 `exit_code`，命令失败或超时不再显示为成功；退出码也不再泄漏进下一条命令的输出。
- 修复复用已有 shell 会话时 `workdir` 参数被忽略、`current_workdir` 始终停留在初始目录的问题。
- shell 会话关闭时改为回收整个进程组，避免 bash 退出后 gradle 等后代进程残留。
- 修复流式响应末行未以换行结尾时最后一个 SSE 事件被丢弃，导致整轮请求重试并重复输出的问题。
- 修复会话与子代理时间戳按 UTC 落库比服务器本地时间早 8 小时，造成会话列表排序错乱的问题。
- 修复 Agent 线程池打满被拒绝时执行占位未回滚，导致该会话此后一直提示「任务仍在运行」的问题。
- 修复子代理恢复未校验抢占结果，多实例下同一子代理可能被并发恢复的问题。
- 修复 CLOUD 技能同步只比对技能目录顶层修改时间，仅改动嵌套文件时不触发同步的问题。
- 修复 `read_file` 对以换行结尾的文件多算一行，以及本地工具错误信息被二次转义的问题。
- 数据库 schema 迁移改由 TypeScript 后端启动时执行（共用原 Flyway SQL 与 `flyway_schema_history`）；Java 默认不再跑迁移，可用 `SPRING_FLYWAY_ENABLED=true` 临时打开。

---

## 0.0.31 (2026-08-13)

### 管理后台
- 修复会话阶段 Tag、定时任务开关、角色权限勾选在控制台刷 Element Plus 校验警告的问题（运行中阶段改用 `primary`，完结字段按布尔禁用开关，权限勾选改用 `value`）。

### 前端（桌面 / Web / 安卓）
- 修复 LOCAL 模式点击命令「执行」后任务仍停在待审批：审批卡片使用后端 requestId，点执行/拒绝后立刻通知服务端退出 WAITING_APPROVAL。
- 修复右侧边栏 Git 提交在自动生成提交信息超过 30 秒时被前端提前判定为超时的问题。
- 工具卡片回退文案改为中文名称（如 `edit_file` 显示为「编辑文件」），与分组标题一致。
- 任务完成后不再立刻用 REST 覆盖待发送队列，避免把刚自动出队的消息又刷回面板。
- 修复队列自动消费后用户气泡消失：任务结束拉取历史时保留尚未入库响应的尾部用户消息。
- 修复定时任务触发后当前打开的会话不出现气泡：把 `scheduled` 来源的用户消息按远程入站插入并进入流式。
- 修复 TypeScript 后端任务完成后执行过程展开时重复最终回复，以及折叠标题出现但过程仍平铺在外的问题。

### 桌面 Electron
- LOCAL 工具审批卡片改用服务端 `tool_execute` 的 requestId，便于点「执行」后立刻恢复会话状态。

### 后端
- 修复 TypeScript 后端工具参数流式传输结束后未刷新完整参数，导致 Shell、文件等工具卡片摘要退化为通用名称的问题。
- 文件编辑工具遇到完全相同的 `old_string` 与 `new_string` 时不再执行无意义写入，并向 Agent 明确返回参数相同的错误提示。
- Shell 异步命令提交后立即返回 `session_id` 与 `output_file`，Java 和 TypeScript 后端行为保持一致，便于任务运行期间回查日志。
- 修复 TypeScript 后端未兼容 Java 的 `/api/uploads/**` 静态资源路径，导致已发布 Electron 客户端检查更新时获取 `latest-mac.yml` 返回 404 的问题。
- 修复 TypeScript 后端关闭持久 Shell 会话时管道异步触发 `ECONNRESET`，因未处理 Socket `error` 事件导致服务进程退出的问题。
- 修复 TypeScript 后端并行工具调用参数分片交错时输入丢失或串到其他工具，进而导致工具卡片回退为错误齿轮图标的问题。
- 修复 TypeScript 后端 LOCAL 审批通过后任务树「待审批」残留：`tool_approval` 立即注销待批请求，并丢弃过期的 `session_tree_status` 异步推送。
- TypeScript 后端统一输出带 ISO 时间的单行 JSON 日志，覆盖业务日志、Harness、Nest 启动日志与 Fastify 请求日志。
- 修复 TypeScript 后端 OSS STS SDK 的 ESM 构造器解析，并让开发启动脚本加载 `backend-ts/.env`，恢复对话图片上传。
- 修复 TypeScript 后端 Git 提交信息生成过慢：该轻量摘要场景显式关闭深度思考，避免兼容供应商在省略参数时默认启用重推理；同时修正 Node LLM 建连超时语义，并在超时后取消底层请求。
- 新增 TypeScript 后端（`backend-ts/`，NestJS + Fastify，默认端口 9081），REST / WebSocket / 鉴权密文协议与 Java 对齐，可与 9080 双跑；验收后将 Nginx `/api/`、`/api/ws/` 切到 9081，Java 进程保留作回滚。切流瞬间进行中的 Agent 与 WebSocket 需重连。
- 修复任务面板偏好接口在 mysql2 将 JSON 列解析为数组后 500 的问题；连接池改为按字符串读取 JSON 列，与 Java 对齐。
- 修复新会话首条回复完成后状态又回到「运行中」：助手消息落库不再整行回写 session（避免把 COMPLETED 覆盖成 RUNNING），并等待落库完成后再标记终态。
- 修复新建会话后侧栏 Agent 名称显示为占位「Agent」：创建接口将字符串形式的 agentId 转为数字后再填充 agentName，与 Java / 前端对齐。
- 修复部分模型流式工具调用每包都带同一 `id` 时，`tool_call_start` 被重复推送、同一工具被拆成多次执行的问题。
- 修复执行中工具卡片摘要显示原始 JSON：WebSocket 改用与 Java 一致的 ToolResultSummarizer（如「编辑 src/App.vue (+3行 -1行)」）。
- 修复 CLOUD shell 未注入 `GIT_TOKEN_*` / `GIT_ASKPASS`：创建会话时写入用户 Git 凭据环境变量，与 Java 对齐。
- 修复 CLOUD shell 会话过期清理未挂定时器：每分钟执行 idle 30 分钟 / 最长 2 小时回收，与 Java `@Scheduled` 对齐。
- 子代理整体超时改回 3600 秒；超时后置位 cancelFlag 并等待 30 秒宽限，宽限内仍不退出才失败。
- Agent 执行器按 core=20 / max=100 / queue=200 排队，队列满后再扩到 max，超出则拒绝，与 Java 线程池一致。
- 启动时校验 `APP_GIT_CREDENTIAL_SECRET`，未配置则拒绝启动。
- 修复发送图片时 OSS STS 临时凭证生成失败：Aliyun SDK 需传入 `AssumeRoleRequest` 实例，不能传普通对象。
- 修复子代理（coder/explorer/reviewer）执行报 `buildContext` 空引用：Delegate 工具对 HarnessService / AgentLoop 改为延迟解析，对齐 Java `@Lazy`。
- 修复 `open_web_page` 抓取超大网页（如微信公众号约 3MB HTML）时因截断后流未结束而误报 timeout；达到上限后立即解析，并跟随 HTTP 重定向。
- 修复子代理创建后不自动打开 Tab、侧栏不刷新：推送 `subagent_session_created`（含 title），并在执行时挂上 WS 流式监听，与 Java 对齐。
- 修复刷新后用户消息中的图片不回显：多模态内容按 `image_url` 落库，读取时兼容旧的 `imageUrl` 字段。
- 修复任务结束后待发送队列不自动消费：出队不再被残留的 RUNNING 状态拦住，崩溃恢复完成后也会继续消费队列。
- 修复 LOCAL 模式任务启动失败：`SkillSyncService` 补齐 `getRemovedSkillNames`，与 Java 对齐。
- 修复 LOCAL 模式关联 MCP 后调用模型 400：桌面上报的工具 schema 写入 `inputSchema`，并把 `type: null` 规范成 `object`，避免 DeepSeek 等严格供应商拒绝。
- 修复 LLM HTTP 4xx 被当成网络失败连着重试：仅对超时/断连等 IO 错误重试，与 Java 对齐。
- 修复上传本地技能报「No valid skill folders found」：multipart 解析保留 `技能目录/SKILL.md` 路径，与 Java 对齐。
- 修复定时任务触发后当前打开的会话收不到实时消息：改为走 WebSocket 流式推送（含用户气泡），无需刷新。

### 桌面 Electron
- LOCAL Skill 同步 zip 改为跟随前端 `VITE_API_BASE_URL` 下载，并把 `localhost` 落到 `127.0.0.1`，避免 Electron 走 IPv6 连不上只监听 IPv4 的 TypeScript 后端。

## 0.0.30 (2026-08-11)

### 后端
- 移除主 Agent 与子 Agent 的最大执行轮次限制，任务不再因达到 100/300 轮而被强制中止
- 会话压缩改为基于完整正常请求的全量任务交接摘要，请求开始与工具轮中途统一使用严格 `<handoff>` 契约和虚拟 user 接续，移除旧的轮次保留、分批滚动摘要与目标水位规则
- 新增压缩请求前缀缓存 token 明细观测并通过会话压缩事件 API 返回；非流式压缩调用支持用户取消，压缩请求确定性超窗时给出明确提示且不截断历史
- 修复 LLM SSE 流式请求仍受 OkHttp 180 秒总调用超时限制、长推理在持续输出时被定时中断的问题；流式调用改为仅受响应头超时与连续无数据空闲超时保护，并补充总调用超时诊断分类
- 新增系统预置指令列表接口，供客户端独立展示只读系统指令；允许用户创建或重命名同名个人指令覆盖系统指令，执行时个人指令优先
- LLM API 返回 HTTP 404 时纳入现有有限重试与指数退避策略
- 修正 Git 凭据提示解析，优化 AskPass 环境变量处理
- 修复 Shell 命令超时后工作目录污染问题，确保后续命令在正确目录执行
- 修复微信媒体上传因 IPv6 连接失败导致的 "Network is unreachable" 错误，强制使用 IPv4
- 修复微信 CDN 上传 SSL 握手失败问题，启用 Java 21 默认禁用的 TLS_RSA cipher 以兼容微信 CDN

### 前端（桌面 / Web / 安卓）
- 缩小定时任务页面的 Tab 标题字号
- “我的指令”抽屉新增“我的指令 / 系统指令”双 Tab，可查看系统预置指令内容，并优化 Tab 字号
- 优化聊天输入区内边距，提升普通任务与新任务输入体验
- 网络超时重试时展示实时状态；等待模型响应或流式暂时无新数据时不再显示额外提示
- Git 变更列表新增右键菜单，支持复制路径、添加到聊天、下载等操作
- 安卓 App 与移动端微信浏览器下载文件时弹出可操作的下载链接和复制按钮，修复 Git 变更树非根层级右键菜单无响应
- 修复多仓库工作区 Git 变更列表下载文件时缺少仓库路径、将“文件不存在”错误响应下载为文件的问题

## 0.0.29 (2026-08-10)

### 后端
- 多仓库工作区发现新增内置排除名单，不再把 `.nvm`、`.pyenv`、`.rbenv`、`.nodenv`、`.jenv`、`.tfenv`、`.sdkman`、`.oh-my-zsh`、`.zprezto`、`.zim`、`.zinit`、`.antigen`、`.fzf` 等工具目录展示为业务 Git 仓库
- LLM 流式响应连续无 SSE 数据的空闲超时由 120 秒放宽至 300 秒；已输出部分数据后发生网络中断也会按既有策略自动重试，重试前丢弃主流程与子代理结果收集器中的本次不完整输出，避免内容与工具调用重复；仅元数据 chunk 不触发输出重置
- Git 面板手动刷新会带凭证 fetch 最新远端引用并计算当前分支 ahead/behind；远端刷新失败时仍返回本地变更状态并禁用同步操作
- 修复 Shell 命令超过等待时间仍在运行时，后续编译日志被误识别为 `current_workdir` 的问题
- 管理端运行时重启接口的脚本路径改为基于 `app.root-dir` 定位 `${app.root-dir}/backend/restart.sh`，不再使用写死的生产路径
- 子代理委派（delegate / delegate_followup）整体执行超时默认值从 15 分钟放宽至 60 分钟（`app.harness.delegate.timeout-seconds` 仍可配置，宽限期 `app.harness.delegate.cancel-grace-seconds` 不变）
- LLM 流式调用防卡死增强：生产环境响应头超时缩短至 120 秒，网络异常与 HTTP 429/5xx 统一有限重试并加入指数退避抖动；响应头到达后解除首包/总调用期限，改由 120 秒 SSE 连续空闲超时保护，正常长时间流式输出不再被误杀；等待响应头、流式静默与重试阶段新增结构化日志和 WebSocket 状态事件，取消时快速终止底层 HTTP Call
- 会话提交增加单飞保护：同一 Session 已处于 RUNNING / RESUMING / WAITING_APPROVAL 或已有提交占位时，立即返回 `session_already_running`，不再保存重复用户消息或让线程排队等待会话锁；队列“立即发送”会先取消旧执行并等待其释放后再接管

### 前端（桌面 / Web / 安卓）
- 模型流式响应中断时在异常提示中提供“继续”按钮，点击后自动发送“继续”消息发起新一轮请求
- Git 拉取与推送按钮根据最新远端状态动态启用：没有远端更新时禁用拉取，没有本地待推送提交时禁用推送，点击刷新或完成 Git 操作后立即重算
- 修复任务页和聊天面板首屏响应式计算引用未初始化变量导致页面逻辑中断、WebSocket 反复重连的问题；心跳改为按任意服务端消息确认连接存活，避免流式事件繁忙时因 pong 延迟误判断线
- 修复回答交互式询问时恰逢 WebSocket 断开会静默丢失答案、关闭面板并让会话持续等待的问题，现会重连后可靠提交并仅在服务端确认完成后关闭询问
- Git 多仓库选择器支持按仓库名或分支搜索，并优化输入框焦点态与下拉选项样式
- 修复 Agent 仍在执行时偶发提前显示复制按钮且等待输出的三点 loading 消失的问题
- 修复聊天输入框中粘贴多行文本后行间距异常、发送内容及数据库记录额外插入空行的问题
- 网络超时重试时展示实时状态；等待模型响应或流式暂时无新数据时不再显示额外提示；重复点击继续执行时明确提示旧任务仍在运行

### 桌面 Electron
- LOCAL 多仓库发现同步过滤工具链与 Shell 配置目录，Git Tab 下拉列表和工作区摘要不再出现内置排除名单中的仓库
- LOCAL Git 手动刷新新增 `fetch --prune` 与 ahead/behind 计算，远端访问失败时保留本地状态并禁用拉取、推送按钮

---

## 0.0.28 (2026-08-09)

### 后端
- Git 面板新增云端提交、拉取与推送接口：提交信息由主会话模型根据脱敏且限流的变更 diff 自动生成，支持仓库级操作互斥、自动 stash、upstream 建立、Git 凭证、活动审计及独立 LLM 用量统计
- 微信语音朗读自然度优化：TTS 会忽略换行符，行尾无标点的换行处此前完全不停顿；现语音文本清洗在行尾无句读标点时自动补句号（已有标点不重复），表格行、段落、列表项之间均产生自然停顿
- 微信语音有序列表朗读条理化：有序列表序号（1. 2. …）此前被剥离导致语音无条理，现保留序号转「1、2、…」格式朗读，条目间层次清晰
- 微信语音文件名固定为「语音回复.mp3」，不再带时间戳后缀（CDN 上传每次生成独立媒体引用，文件名仅展示用）
- 新增会话恢复（取消归档）接口 `PUT /sessions/{id}/unarchive`：已归档会话可恢复回主列表
- LOCAL 模式审批状态服务端化：新增会话级待审批注册表（`ApprovalRegistry`，sessionId → 待审批请求集合），需要审批的工具请求登记后会话进入 `WAITING_APPROVAL`，批准/拒绝/超时/取消后计数归零才条件恢复 `RUNNING`（条件更新不覆盖 `FAILED/CANCELLED/COMPLETED` 终态）；`pendingApprovalCount` 直接来自注册表计数（支持 AgentLoop 并行工具调用的多个并行审批）
- 会话列表 VO 新增待审批/待回答计数与任务树聚合信号：`pendingApprovalCount` / `pendingQuestionCount` / `treePendingApprovalCount` / `treePendingQuestionCount` / `treeUnread` / `treeRunning` / `treeFailed`（主会话聚合其全部边路任务信号，列表接口一次性批量查询避免 N+1）
- 新增 `session_tree_status` WebSocket 事件：边路任务 phase / unread / 审批 / 待回答变化时实时推送父任务的最新任务树聚合信号，前端聚焦模式无需重新拉取列表即可实时重排
- 已归档列表排序修正：`status=ARCHIVED` 时按 `updated_at DESC, id DESC` 排序（忽略活跃阶段优先与置顶）
- 边路任务 VO（`/sessions/{id}/side-tasks`）补充 `updatedAt` 与 `pendingApprovalCount` / `pendingQuestionCount` 字段

### 前端（桌面 / Web / 安卓）
- 任务右侧 Git 面板新增提交、拉取、推送三个图标：提交信息自动生成并直接提交，操作期间显示加载状态，并按变更、远端和 detached HEAD 状态禁用不可用操作
- 任务列表新增右键菜单（桌面右键 / 移动端长按）：编辑标题 / 归档 / 删除；已归档区为 恢复 / 编辑标题 / 删除
- 新增归档与恢复：任务可归档至面板底部「已归档」折叠区（按最近活动时间倒序），支持恢复回主列表；运行中/待审批任务归档弹出确认提示；归档/恢复不影响已打开的会话
- 任务面板新增「标准 / 聚焦」模式切换：改用单个铃铛图标按钮循环切换以节省窄屏空间（聚焦时实心蓝色激活态）；聚焦模式全量平铺所有任务，按 待审批/待回答 > 失败 > 运行中 > 未读 > 空闲/已完成 优先级排序（空闲与已完成同档，服务端 `tree*` 信号 + 实时事件驱动），列表项采用标题与工作区/状态两行布局；已完成且超过 3 天无更新的任务自动折叠进「历史」区；右侧边路任务列表同步应用优先级排序；切换模式时自动刷新对应任务列表
- 会话状态模型重构：会话实体缓存与列表投影（标准/已归档/聚焦）分离，归档当前会话不再破坏聊天面板，聚焦全量拉取不污染标准模式分组分页
- 引入 vitest 前端单元测试（`npm run test:unit`）并纳入 CI；后端单测（含审批并发）纳入 CI

### 桌面 Electron
- LOCAL 模式新增 Git 自动提交、拉取与推送：本机完成敏感 diff 过滤、提交、自动 stash 恢复与 upstream 建立，沿用本机 Git/SSH 凭证并对同仓库写操作加锁

### 管理后台
- 模型管理列表不再显示“调用消息”列

---

## 0.0.27 (2026-08-09)

### 前端（桌面 / Web / 安卓）
- 修复非 HTTPS（如 `http://IP:端口`）访问时所有复制按钮失效的问题：统一改用复制工具函数 `utils/clipboard.ts`，`navigator.clipboard` 不可用时回退到 `execCommand('copy')` 方案（消息复制、代码块复制、工具调用输入/结果复制、命令描述复制、工作区/文件路径复制）

---

## 0.0.26 (2026-08-08)

### 后端
- 多 Git 仓库发现接口重新提供各仓库的增删行摘要：保留每仓库一次 `git status --porcelain=v2` 的轻量发现，仅对有变更的仓库追加 `git diff --numstat` 统计已跟踪文件，并直接读取未跟踪文本文件计入新增行，接口返回 insertions/deletions 供工作区摘要统一显示 `+N -N`
- 多 Git 仓库发现接口性能优化：仓库统计由每仓库 4 次 git 进程（rev-parse + name-status + numstat + ls-files）合并为 **1 条 `git status --porcelain=v2`** 命令（36 仓库从 144 次进程降到 36 次），`parallelStream`（受 CPU 核数钳制）改为固定 8 线程专用池（含停机回收）；stderr 不合并（DISCARD）、stdout 由独立 daemon 线程逐行读取统计（内存 O(1)，超时强杀后管道关闭自动结束，无线程/进程泄漏）；统计失败/超时的仓库返回 `unavailable` 占位而非静默消失；`workspace-git-repos` 不再返回 insertions/deletions（行数统计保留在 Git Tab 明细的 workspace-git-status）；空仓库（无 commit）下 getStatus 分支用 symbolic-ref 兜底、变更统计回退 `--cached` 计入已暂存文件，与发现接口口径一致
- 新增多 Git 仓库工作区支持：工作区本身不是 git 仓库、但一级子目录中存在多个 git 仓库时，右侧边栏可展示各仓库的 Git 信息。新增 `GET /files/workspace-git-repos` 发现接口（返回工作区是否本身为 git 仓库 + 一级子仓库的目录名/分支/变更统计）；`workspace-git-status` / `workspace-git-diff` 新增可选 `repoPath` 参数，指定仓库相对工作区根的目录名，缺省时行为与之前完全一致（兼容单仓库工作区）；repoPath 仅允许工作区一级子目录名，拒绝 `..`、绝对路径与多级路径，并过路径沙箱校验
- 修复 LLM 调用偶发永久卡死的问题：HTTPS 请求体写入被底层阻塞（SSL 写锁）时，OkHttp 的 readTimeout/writeTimeout 依赖 Okio Watchdog 关闭 Socket，而 `SSLSocketImpl.close()` 需要等待同一把 SSL 写锁，导致 OkHttp 内部超时机制整体失效、Agent 无限等待；现新增应用层硬超时（默认 10 分钟，可配 `app.harness.llm.call-timeout-seconds`），超时后在独立守护线程主动取消请求并报错（不阻塞业务线程），同时为 OkHttp 增加 callTimeout（默认 15 分钟，可配 `app.harness.llm.http-call-timeout-seconds`）兜底；同步调用 `chat` 路径同样受应用层硬超时保护
- 子代理委派（delegate / delegate_followup）新增整体执行超时（默认 15 分钟，可配 `app.harness.delegate.timeout-seconds`）：子代理 LLM 请求卡死不再无限拖住父 Agent，超时后置位子代理取消标志请求其退出，并给予宽限期（默认 30 秒，可配 `app.harness.delegate.cancel-grace-seconds`）等待其响应取消；宽限期后仍卡死则放弃等待、将子代理标记失败并返回父 Agent
- 修复对话页任务耗时统计偏差：任务进入终态（完成/失败/取消）时，将会话最后一条消息的 `updated_at` 刷新为任务结束时刻，为前端提供「用户消息发出 → 任务执行结束」的准确耗时依据（此前仅记录消息创建时间）
- Shell 工具输出文件收敛：移除单次命令的切片输出文件，仅保留会话累积输出文件，并将对外字段 `session_log` 统一命名为 `output_file`（其内容为整个会话从创建起所有命令的完整输出，支持随时回查）

### 前端（桌面 / Web / 安卓）
- 多 Git 工作区摘要中的所有变更仓库统一显示具体 `+N -N`，不再仅当前选中仓库显示行数、其他仓库显示「N 变更」
- 修复工具审批卡片接入统一复制能力后遗漏导入导致前端构建失败的问题
- 修复多 Git 工作区切换会话时偶发弹出「路径访问被拒绝」的问题：Git provider 变化时同步清空旧会话的仓库列表与选中路径，避免发出「新 sessionId + 旧 repoPath」的错误请求；会话列表点击不再提前切换全局活跃会话，改由详情与工作区字段加载完成后统一切换，避免新旧会话状态短暂混用
- 优化窄屏右侧工作区 Git 摘要：仓库名与分支改为分层排版，空间不足时单行省略，不再逐字符断行；当前选中仓库复用已加载的 Git 详情显示具体 `+N -N`，无需增加 Git 命令或接口请求，其他仓库仍显示「N 变更」
- 多 Git 仓库工作区加载提速：打开会话的 Git 信息不再受「每仓库 4 次 git 进程」拖累，36 仓库工作区从约 20 秒以上降至秒级；多仓库摘要行不再显示 +N/-Y（改为「目录名 · 分支 · N 变更」，行数统计保留在 Git Tab 明细）；切到 Git Tab 只刷新选中仓库明细、不再重复扫描仓库列表；统计失败/超时的仓库在摘要行与 Git Tab 展示「不可用」提示（附重试），不再静默消失
- 右侧边栏新增多 Git 仓库工作区展示：工作区本身不是 git 仓库但一级子目录含多个 git 仓库时，Git Tab 工具栏出现仓库下拉（单选，项目名与分支合并显示为「目录名 · 分支」，并缩小下拉字体以适配窄屏），切换即切换该仓库的变更列表与 diff；工作区摘要行逐仓库列出有变更的仓库（目录名 + 分支 + `+X -Y`），全部干净时显示「N 个仓库 · 全部干净」，点击仓库项可直接跳到 Git Tab 并选中该仓库；默认选中第一个有变更的仓库（不记忆上次选择）；工作区本身是 git 仓库时界面与之前完全一致，不出现仓库下拉；diff Tab 在多仓库模式下以「仓库目录/文件路径」区分，避免不同仓库同名文件冲突
- 修复对话页任务耗时统计偏少的问题：起点由「首个工具步骤的创建时间」改为「用户消息发出时刻」，终点改为「任务结束时刻」（任务终态时由后端刷新最后一条消息的 `updated_at`），覆盖此前漏计的发送后初始化与首次 LLM 思考耗时
- 修复切换会话后页面停留在非底部的问题：Markdown 渲染为异步（含代码块 Monaco 高亮），会话恢复时仅在 `restoreSession` 完成后滚动一次，会因高度尚未定型而停在非底部；现 Markdown 渲染完成后派发全局 `mao:markdown-rendered` 事件，主会话在恢复阶段持续监听并按 300ms 防抖校准滚动到底部，直到高度稳定
- 修复子代理对话页面执行过程中不自动滚动到底部的问题：此前仅在消息条数变化时滚动，assistant 内容持续增长（条数不变）时不会跟随；现聚合消息数、末条内容长度、工具状态、流式状态、打字指示作为滚动锚点驱动自动滚动，并新增用户上滚检测（离开底部暂停、滚回底部恢复）与 Markdown 渲染完成校准
- 修复边路任务切换 tab / 首次加载时 Markdown 异步渲染完成后不校准到底部的问题：为边路任务面板补齐 `mao:markdown-rendered` 事件监听，渲染完成且用户未上滑时重新滚动到底部
- 修复安卓 App 退后台后回前台页面卡死的问题：删除原生后台保活机制（前台服务 / 唤醒锁 / 磁盘事件缓冲 / 回前台恢复协议），WebSocket 回退为与 Web 端一致的纯 JS 连接；回前台时检测到连接已断开则自动静默刷新页面（10 秒防抖，避免快速前后台切换连环刷新），刷新后自动回到原会话并拉取最新状态；后台任务不再实时收流，任务结果以回前台刷新后为准

### 安卓原生
- 删除后台保活机制：移除前台服务（dataSync）、WakeLock、OkHttp WebSocket 保活、磁盘事件缓冲与回前台恢复协议等 6 个相关类，移除对应权限声明与 okhttp 依赖，后台不再有任何常驻原生活动，明显降低耗电与发热

### 桌面 Electron
- 多 Git 仓库发现结果重新包含各仓库增删行摘要：仅对有变更仓库追加 numstat 统计并计算未跟踪文本文件行数，与云端模式一致支持全部仓库显示 `+N -N`
- 多 Git 仓库发现性能优化：`git-repos` IPC 每仓库由 4 次 git 进程合并为 1 次 `git status --porcelain=v2`（spawn + readline 逐行统计，内存 O(1)，无输出上限），并发由无上限 Promise.all 改为 8 路 mapLimit；统计失败/超时的仓库返回 `unavailable` 占位，不再静默消失
- 新增多 Git 仓库工作区支持：新增 `git-repos` IPC（扫描工作区一级子目录中的 git 仓库并返回分支与变更统计）；`git-status` / `git-file-diff` IPC 支持可选 `repoPath` 参数（仅允许工作区一级子目录名，越界路径拒绝），缺省时行为与之前完全一致
- 新增回前台 WebView 无响应兜底：页面卡死（后台冻结 / 渲染进程异常 / 主线程无响应）时，App 回前台后自动探测并刷新页面，无需用户退出进程重开
- 取消任务终态系统通知与通知点击跳转会话能力（与保活机制一并移除）

---

## 0.0.25 (2026-08-07)

### 后端
- 优化 LLM 调用流程：当调用模型 ID 以 `gpt` 开头时，在请求头附加 `User-Agent: codex_cli_rs/0.146.0 ...`、`originator: codex_cli_rs`、`x-codex-window-id` 三项，用于识别调用方来源
- LLM 调用重试间隔改为指数退避：瞬时错误（429/5xx）第 n 次重试前等待 1×2^(n-1) 秒（第 1 次 1s、第 2 次 2s、第 3 次 4s…），单次重试间隔上限 60 秒，最多重试 10 次；服务端返回 Retry-After 时优先采用但同样受 60 秒上限约束（此前为固定 5 秒），缩短了低次重试的等待时间、避免高次重试长时间空转
- LLM 调用遇到瞬时错误（429 限流 / 5xx 服务端错误，含 502/503/524）进入重试时，新增向客户端推送 `llm_retry` 事件（含 HTTP 状态码、当前重试次数、最大重试次数、等待秒数），前端可实时展示重试进度
- 微信通道执行任务时屏蔽 `ask_user_questions` 工具：微信无交互式提问面板，此前 Agent 调用该工具会向桌面端 WebSocket 推送提问并阻塞等待回答直至超时；现微信通道会话的工具列表直接移除该工具（LLM 不可见、执行层兜底拦截），Agent 改为在回复中直接给出选项或请求用户补充
- 新增会话消息搜索接口 `GET /v1/sessions/search`：按用户发送的消息内容（纯文本，多模态消息仅匹配文本部分）检索会话，覆盖主会话与边路会话，排除子代理、已归档及父会话不可达的孤儿边路；结果固定最多 20 条，按会话最近更新时间倒序；命中片段围绕关键词生成（含上下文与省略号），SQL 层 LIKE 候选 + 纯文本二次校验避免图片 URL / JSON 字段假命中，LIKE 通配符转义防注入
- 新增内置工具 `delegate_followup(child_session_id, task)`：主代理可对既有子代理会话（如 reviewer）追加任务并复用其完整历史上下文做增量核查（审查 → 修复 → 再审查闭环），避免新建子代理全量重扫；追问内容以普通 USER 消息落库、与聊天记录一致，每轮独立写入 subagent_execution 审计（含 completed_rounds 区分有效轮次），支持连续多轮追问；原子抢占防并行竞态，取消/跳过时回滚本次追问消息并恢复原终态，追问前自动清理子会话不完整尾部与孤立 USER 消息避免连续 USER 触发 LLM 400
- 委派/追问子代理工具结果摘要优化：保留 child_session_id 供连续追问，修正「已取消」任务误标「(错误)」的判定顺序
- `delegate_followup` 与 `delegate` 一致仅服务端执行（LOCAL 模式不可用），避免本地端误路由

### 前端（桌面 / Web / 安卓）
- 工具调用卡片适配新增的 `delegate_followup` 工具：显示「追问子代理」标签与连接图标，与 delegate 一致支持「查看过程」入口并解析 child_session_id
- 修复模型服务瞬时错误（如 502/503）重试期间界面长时间「无响应」的问题：Agent 调用大模型遇到 429/5xx 进入重试时，消息区实时显示「模型服务繁忙（HTTP xxx），正在重试 n/10，x 秒后继续…」提示条，模型恢复输出或任务结束后自动消失；主会话、边路任务与子代理会话均支持，重试条出现时自动滚动到底部，取消/断线/终态均会清理提示状态
- 会话搜索由顶栏旁的小型浮窗改为页面居中的完整搜索面板：扩大搜索输入与结果列表空间，增加功能说明、结果数量、关闭按钮和键盘操作提示，提升长标题及命中消息片段的可读性；修复 Electron 窗口中弹窗受布局样式影响贴在左上角的问题，未搜索和搜索中不显示空结果框，搜索完成后结果区按实际内容自适应高度并在达到上限后滚动
- 顶部操作栏新增会话搜索：放大镜图标（或 Ctrl/Cmd+K 快捷键）打开搜索浮窗，输入关键词即按会话内用户消息内容检索（300ms 防抖），最多 20 条按最近更新时间倒序展示（会话标题、类型标签、相对时间、命中片段关键词高亮），支持 ↑/↓/Enter/Esc 键盘导航；点击主会话结果直达该会话，点击边路会话结果跳转父会话并打开对应边路任务 Tab；快速输入旧请求自动取消不覆盖新结果，未登录时点击唤起登录对话框
- 修复刷新页面或安卓 App 退后台重开后停留在其他会话的问题：此前无会话路由冷启动时回退到侧栏排序第一的会话（活跃任务、置顶或最近更新的会话优先，并不等于最后查看的会话）；现前端将最后查看的会话 ID 持久化到本地存储，重新打开 / 刷新后优先恢复原会话，原会话已删除或失效时自动回退到最新会话

---

## 0.0.24 (2026-08-06)

### 后端
- 修复微信语音回复内容不完整的问题：Agent 回复的 Markdown（表格、代码块、链接等）此前原样送入 TTS，语音模型对表格等结构鲁棒性差，往往只合成表格之前的内容；现合成前先剥离 Markdown 语法（表格转「列1：值1，列2：值2」自然语言、代码块整体移除、链接只保留展示文字、剥除标题/列表/粗斜体/HTML 标记），纯文本再入 TTS，语音内容不再丢失
- 微信语音超长文本截断优化：截断改为落在句子边界（句号/叹号/问号/换行），不再从字符中间硬切；`weixin.bot.voice-max-seconds` 默认由 60 放宽到 300 秒（微信侧语音为 MP3 文件消息，无语音条 60 秒限制），长回复也能完整朗读，仍可通过配置调整
- 新增 `PUT /users/me/profile` 接口：当前用户自助更新个人资料（显示名称、邮箱、头像）。头像所有登录方式均可上传/移除（使用显式列更新保证置空真正落库）；显示名称与邮箱仅本地（LOCAL）账号可修改，LDAP / 飞书账号提交时返回明确错误提示；邮箱变更时校验格式、长度（≤128）与唯一性（排除自身），头像地址校验格式与长度（≤512）
- `GET /users/me` 与登录返回的用户信息新增 `authSource` 字段（LOCAL / LDAP / FEISHU），`UserService.resolveAuthSource` 同步识别飞书账号（此前飞书用户被误判为 LDAP）
- 飞书登录不再覆盖已存在用户的头像（不做飞书头像自动同步，头像由用户自行上传/移除管理）
- 新增工作区 PDF 预览接口 `GET /files/workspace-preview`：校验会话归属与沙箱路径后以 `application/pdf` 内联返回字节流；仅接受 `.pdf` 扩展名且文件头为 PDF 魔数（`%PDF-`，兼容 UTF-8 BOM），伪 PDF / 损坏文件返回明确错误
- 微信通道支持接收文件消息（file_item）：用户发送 PDF/文档等文件不再被忽略，文件下载解密后保存到会话工作区 `weixin-files/YYYY-MM-DD/` 目录，并以 `@{路径}@` 文件引用形式注入 Agent，Agent 可读取文件内容后处理任务；纯文件消息（不带文字）同样会触发 Agent 处理
- 微信入站文件大小上限默认 100MB（可配置 `weixin.bot.max-inbound-file-mb`），超限或下载/解密/保存失败时向用户回复明确错误提示，不再静默忽略；多文件部分失败时会在消息中告知 Agent 哪些文件接收失败；同名文件自动追加时间戳重命名并以原子写入避免并发覆盖
- 微信文件消息失败处理完善：失败消息同样执行"新消息接管"（取消同会话在途 Agent 执行）；仅当消息无任何可处理内容（无成功文件、无文字、无图片）时才直接回复失败错误，带文字/图片时继续处理有效内容并附失败说明；下载参数与 AES key 按同一媒体节点（media→thumb_media→item 级）回退解析；文件名清洗剔除 `{}@` 等文件引用保留字符，避免特殊文件名破坏 `@{路径}@` 引用解析
- 沙箱路径拦截日志降级：`SecurityException`（PathSandbox 阻止越界路径）属正常安全机制，此前会被工具层重复记为 ERROR；现降级为 WARN（PathSandbox 已自带一条 warn，工具层不再重复堆栈），仅真实异常保留 ERROR
- 修复 shell 工具输出读取竞态：会话被并发关闭（清理/超时/close）时读输出流抛 `Stream closed`，此前会丢弃已读到的全部输出并记 ERROR，导致成功执行的命令被误判为失败；现保留已收集的部分输出返回给模型，`Stream closed` 降级为 warn 日志
- `send_message` 支持携带 `modelId`：发送时若消息指定模型则先持久化到会话再执行，修复边路任务切换模型后立即发消息/发图片仍按旧模型校验（如图片视觉校验误报「当前模型不支持图片」）的问题
- `/sessions/{id}/side-tasks` 接口补充返回 `unread` 字段（边路任务后台完成未读标记），供桌面端刷新后仍能显示左侧任务栏未读圆点

### 前端（桌面 / Web / 安卓）
- 修复安卓 App「我的技能 / 我的指令」管理抽屉使用固定宽度导致内容超出屏幕且无法滑动的问题：安卓端改为全屏宽度并启用独立纵向触摸滚动，编辑指令与技能详情弹窗同步适配窄屏
- 设置页新增「个人信息」页（默认首页）：支持上传/移除头像（jpg/png/webp，≤5MB，选择后预览再保存）、编辑显示名称与邮箱（LDAP / 飞书账号只读）、展示用户名与登录方式；头像上传复用本地/OSS 双模式链路；优化页面标题、说明、表单标签与控件的字号层级，提升信息密度
- 顶栏用户区域仅展示真实头像（未设置时回退占位图标），隐藏用户名以精简空间；下拉菜单新增「个人信息」直达入口
- 文件浏览器支持预览 PDF：新增 `PdfViewer` 组件（pdf.js 渲染，滚动逐页加载 / 缩放（适合宽度、实际大小、放大缩小）/ 页码显示 / 下载兜底），CLOUD 模式走独立二进制预览接口，LOCAL 模式（Electron）经 IPC 传 base64；加密或损坏的 PDF 显示错误提示并保留下载入口
- 边路任务发送消息时携带当前选择的模型 ID，确保切换模型后立即发送（含图片）即按新模型执行，不再依赖异步 PATCH 的时序
- 修复边路任务发送图片被前端误拦截的问题：ChatInput 的模型视觉能力 prop 未传时被 Vue 布尔默认值强制为 false，导致切换任意模型发图都提示「当前模型不支持图片」；现默认视为未知（不拦截、交后端校验），并加载模型列表计算真实的视觉能力
- 边路任务后台完成时，左侧任务栏在其所属会话标题后显示青色未读圆点（与主任务完成提示一致）；刷新页面后状态保持。已读时机：仅在实际打开对应边路任务 Tab 时清除该边路任务未读；查看主聊天或其他 Tab 不会误清，打开父会话也不代表已查看其下边路任务
- 修复边路任务未读圆点偶发无法消除：占位 Tab（创建时 sideSessionId 未定）经 `side_session_created` 更新为真实 ID 时，此前仅监听 Tab 对象引用导致未读状态未同步、已查看的圆点残留；现改为监听激活 Tab 的 `sideSessionId` 变化并即时清除未读。同时「已读」改为后端确认成功后再清除本地标记（避免 read 请求与 side-tasks 拉取竞态导致圆点刷新后复活），拉取边路任务列表时保留本地已读状态
- 修复安卓 App 刷新后进入最近对话页时内容需等待约 15 秒才加载的问题：刷新只重建 WebView 的 JS 环境，原生保活 WebSocket 连接仍保持，但新桥收不到「连接已就绪」通知，`connect()` 一直 pending 至超时才放行，阻塞了其后的历史消息加载。现前端对原生补发的 open 事件做幂等保护，并在连接超时后销毁失效桥，下次连接直接重建

### 安卓原生
- 修复安卓 App 刷新后对话内容长时间（约 15 秒）才加载出来：页面刷新后原生连接仍保持（后台保活设计），但不再向新 JS 桥通知就绪。现 `ensureKeepAlive` 在连接已建立时主动补发 `open` 通知，新桥立即感知连接就绪，`connect()` 不再空等超时

## 0.0.23 (2026-08-06)

### 后端
- WebSocket 空闲超时改为可配置（`APP_WS_IDLE_TIMEOUT_MS`，默认 90 秒），适配安卓原生 25~30 秒心跳，不影响原有 Web/桌面端行为
- WebSocket 握手支持 `client=android` 客户端类型识别
- LLM 调用对 5xx 服务端错误（含 524 网关超时）新增自动重试：与 429 限流共用 `app.harness.llm.rate-limit-max-retries` / `rate-limit-retry-delay-seconds` 配置，重试耗尽后错误信息会标明已重试次数

### 前端（桌面 / Web / 安卓）
- 修复桌面客户端「远程资源有更新」提示失效的问题：`version.json` 版本号未随发版同步时不再提示。现同时比对构建时间（buildTime），只要有新部署即可在右上角刷新按钮亮起更新提示；并同步 `desktop/package.json` 版本至 0.0.23
- 移除右侧边栏任务标题旁的「边路任务 / 子代理」类型徽标，仅保留任务标题
- 修复右侧边栏上下文显示：切换到边路任务 / 子代理时仍显示主会话的上下文大小。根因是子会话的上下文占用仅在实时执行时经 WS 推送，页面刷新或会话停止后前端缓存缺失，回退显示主会话上下文；现切换时补拉会话元数据会同步写入其 contextTokens 上下文缓存
- 安卓端新增 WebSocket 兼容桥接层：Capacitor 平台下流式事件收发交由原生连接承载（Web/Electron 零影响）
- 安卓 App 退到后台 / 锁屏后实时会话不再断线；回前台自动执行恢复流程（缓冲事件重放 + REST 快照校准 + 去重 ACK）
- 退出登录时停止原生保活连接与服务

### 安卓原生
- 新增前台保活服务 `WsKeepAliveService`：持有唯一 OkHttp WebSocket 连接（25 秒心跳 / 40 秒超时、任务执行时段持唤醒锁），任务全部终态且回前台后自动降级，后台无任务时断开并停止服务
- 新增事件缓冲与任务跟踪：活跃/待恢复任务集合、父子会话级联跟踪、事件 JSON 持久化（冷启动续放）
- 回前台恢复协议：重放缓冲事件 + REST 权威快照校准 + 水位 ACK 去重；Android 15 后台限制触发时自动切换恢复模式
- 任务终态发送独立系统通知；通知点击可跳转至对应任务详情页

## 0.0.22 (2026-08-06)

### 前端（桌面 / Web / 安卓）
- 修复切换到边路会话 Tab 时消息区停留在顶部的问题：切回边路会话自动滚动到底部，流式输出 / 消息变化时自动跟随滚动（用户手动上滑查看历史时暂停自动滚动）

## 0.0.21 (2026-08-05)

### 后端
- 新增工作区文件下载接口 `/files/workspace-download`（单文件原始字节流）与 `/files/workspace-download-zip`（目录打包 zip，上限 1GB，超限拒绝）
- WebSocket 断开不再取消进行中的 `ask_user_questions`：Agent 继续等待回答；重连或刷新页面后，后端重新推送问题，用户可继续提交（等待总时长仍为 15 分钟超时，会话取消/打断仍立即终止询问）

### 前端（桌面 / Web / 安卓）
- 云端工作区（CLOUD）文件树右键菜单新增「下载此文件 / 下载此目录」，可将工作区文件与目录打包下载到本地
- 会话页面右侧边栏随中心 Tab 联动：切换到边路任务 / 子代理 Tab 时，标题、状态、上下文、进度自动切换为对应会话的信息，并显示会话类型标识；子代理标题为只读

## 0.0.20 (2026-08-05)

### 安卓原生
- 最终版软键盘避让：仅保留原生 IME Insets 方案，移除无效的 `adjustResize` 与前端动态视口处理

## 0.0.19 (2026-08-05)

### 安卓原生
- 原生 WebView 同时处理系统栏与软键盘 Insets，键盘弹出时内容区直接收缩到键盘上方，修复输入框仍被遮挡

## 0.0.18 (2026-08-05)

### 前端（桌面 / Web / 安卓）
- 修复 0.0.17 在部分安卓 WebView 中引发的页面白屏

## 0.0.17 (2026-08-05)

### 安卓原生
- 尝试通过软键盘 `adjustResize` 模式解决输入框遮挡（后由 0.0.19 的 IME Insets 方案取代）

## 0.0.16 (2026-08-05)

### 安卓原生
- 改为远程加载 `https://mao.etarch.cn`（与 Electron 一致），前端更新部署 Web 后刷新即可，无需每次打 APK
- APK OTA 仅用于原生壳（Capacitor 插件、系统栏、WebView 配置等）更新
- 构建链路简化：`web-stub` + Capacitor `server.url`，不再打包 `desktop/dist` 进 APK

## 0.0.15 (2026-08-05)

### 前端（桌面 / Web / 安卓）
- 上下文窗口显示优化：基于 LLM 真实 prompt_tokens 锚点 + 增量估算，进度条更准确
- 对话时间线展示上下文整理标记（回复前 / 执行中整理及节省 token 提示）

### 后端
- 活跃上下文 token 统一计算（prompt_tokens 锚点 + 增量消息）
- 压缩事件持久化（`session_compaction_event`）并在消息 API 下发
- Token 估算改为 UTF-8 bytes/4，移除 jtokkit 依赖

## 0.0.14 (2026-08-04)
- 应用图标更换为桌面端同款（app-icon-small.png）

## 0.0.13 (2026-08-04)
- 修复点击顶部刷新后卡在 loading、图标裂图：Capacitor 改用 hash 路由，刷新强制从应用根路径加载，避免相对路径 `./assets` 在深路由下 404

## 0.0.12 (2026-08-04)
- 修复系统状态栏仍重叠：改用 Capacitor `adjustMarginsForEdgeToEdge=force`（WebView.setPadding 对页面内容无效）
- 父布局背景与状态栏同色，避让区域不再露黑边

## 0.0.11 (2026-08-04)
- 预留手机系统状态栏 / 手势导航栏空间：WebView 按 WindowInsets 加 padding，顶栏不再与系统栏重叠
- WebView 背景与状态栏同色，避免避让区域露出黑边

## 0.0.10 (2026-08-04)
- 修复顶部残留原生标题「Mao」：Splash 增加 postSplashScreenTheme，应用/Activity 统一 NoActionBar，启动后强制隐藏 ActionBar
- 原生层注入兜底 CSS：强制 `.top-nav` 显示，移除 HTML splash，避免升级后旧 WebView 缓存继续隐藏操作栏
- versionCode 变化时清 WebView 缓存并重载，确保加载 APK 内最新前端资源

## 0.0.9 (2026-08-04)
- 修复安卓顶部操作栏仍然空白的问题：强制恢复 TopNav 显示，并去掉 macOS 红绿灯 78px 左缩进
- 安卓顶栏改为紧凑单行布局（与手机浏览器全屏一致）：左右面板、终端、技能、指令、刷新等按钮可见
- 收起用户名文字，避免窄屏挤掉操作按钮；`android-capacitor` 仅在 Capacitor WebView 生效，不再误伤手机浏览器

## 0.0.8 (2026-08-04)
- 彻底修复安卓启动屏残留问题：启动主题改用纯色窗口背景，移除 splash PNG 与 HTML splash 双层图标叠加
- 恢复安卓端顶部导航栏显示：技能、快捷指令、左右面板切换等操作按钮正常展示
- 每次启动禁用 WebView 缓存并重载，确保 APK 升级后加载最新资源，避免旧页面残留

## 0.0.7 (2026-08-04)
- 安卓端彻底隐藏桌面专用顶部工具栏，正文直接从系统状态栏下方开始，消除顶部大块空白与压缩变形图标

## 0.0.6 (2026-08-04)
- 修复安卓顶部导航保留 macOS 红绿灯空位导致的大块空白
- 安卓顶部导航改为紧凑单行布局，隐藏不可用的终端按钮并收起用户名

## 0.0.5 (2026-08-04)
- 修复安卓 OTA 弹窗触发顺序，发现新版本后会正确主动提示
- 修复点击顶部更新入口时安卓无法直接下载安装的问题

## 0.0.4 (2026-08-04)
- 修复安卓状态栏信息不可读的问题：状态栏/导航栏改为浅色背景与深色图标
- 修复页面高度没有避让系统栏的问题：原生 WebView 不再延伸到状态栏和手势导航栏下方，避免顶部按钮与底部输入框被遮挡

## 0.0.3 (2026-08-04)
- 修复安卓 OTA 普通更新没有主动弹窗提示的问题，发现新版本后自动提示用户立即更新

## 0.0.2 (2026-08-04)
- 修复安卓状态栏遮挡顶部导航的问题，为 Capacitor 安卓环境预留顶部安全区空间

## 0.0.1 (2026-08-04)
- 初始版本：核心聊天闭环（登录/会话/Agent 选择/聊天/流式输出/CLOUD 工具/提问/Side Task）
- 应用内更新（OTA）：启动时 + 手动检查，支持强制/普通更新，APP 内下载安装
